# Town OS UI

React + Vite frontend for Town OS.

## Development

Run `make dev` from the project root to start the API server in a container and the Vite dev server. Once running, access the UI at `http://<hostname>:5173`.

Ports 8080 (backend API) and 5173 (Vite dev server) must be accessible on the host.

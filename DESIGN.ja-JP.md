# Town OS 設計

Town OS がどう動くか: アーキテクチャ、各サブシステムの挙動、API の全体像、そしてそれらを
支える不変条件。ビルド手順、テストの規則、コードスタイルは
[CLAUDE.md](CLAUDE.md)（日本語訳は [CLAUDE.ja-JP.md](CLAUDE.ja-JP.md)）にある。

挙動の変更は、それを行うのと同じコミットの一部として本ファイルに書かれるべきである。
リポジトリのビルド方法やテスト方法の変更は CLAUDE.md に属する。

> **本ファイルは [DESIGN.md](DESIGN.md) の日本語訳である。英語の原文が正典である。**
> 中国語訳は簡体字 [DESIGN.zh-CN.md](DESIGN.zh-CN.md) と繁体字
> [DESIGN.zh-TW.md](DESIGN.zh-TW.md)、スペイン語訳は
> [DESIGN.es-ES.md](DESIGN.es-ES.md)（スペイン）と
> [DESIGN.es-MX.md](DESIGN.es-MX.md)（メキシコ）にある。
> 訳文が原文と食い違う場合は英語の原文が正しく、直すべきは訳文である。コード識別子、
> ファイルパス、コマンド、環境変数、API パス、YAML のキー名は原文のまま訳さない。

## アーキテクチャ上の不変条件

コードではなく設計を縛る規則である。これを破ってもビルドやリンターが失敗するわけではない
——起動はするが、その後おかしな振る舞いをする機械ができあがる。しかも大抵は、変更した
場所からはるか遠くで。

- **ストレージ層はボリュームを管理し、オブジェクトストレージは gfeh が提供する。** `src/storage` が扱うのは btrfs のサブボリュームとクォータだけであり、それ以外は扱わない —— オブジェクトストレージには一切関与しない。オブジェクト、ファイルごとのメタデータと権限、階層的なユーザー／ACL のデータベース、共有、ファイルごとの HTTP での公開、フェデレーション、そしてすべてのプロトコルのビュー（S3、IPFS、Google Drive、素の HTTP —— および SMB/CIFS。これは gfehd が実装しているが Town OS は提供しない）は gfeh のものであり、責任を負うのは gfeh である。`src/storage` や `/storage/*` にオブジェクト／blob／ファイル単位のエンドポイントを追加してはならず、`storage.Storage` や `storage.Controller` にユーザー、権限、プロトコルを教えてはならない。[ストレージ](#ストレージ)を参照。

- **pages の機能は常に有効である** —— pages のサブシステム（Caddy による静的サイトのホスティング）は起動時に無条件で初期化される。`TOWN_OS_PAGES` のような環境変数による関門は無い。通常の起動では pages のマネージャは非 nil なので、pages の API は常に利用できる。ハンドラは今も「pages not configured」を返す防御的な nil マネージャの番人を残しているが（`ServerConfig.PagesMgr` 無しでサーバーを組み立てるテストがそれを行使する）、実際の起動でそこに到達することはない。

- **バージョン変更の検出とユニットの再起動** —— systemcontroller は、動作中のコンテナのイメージ SHA（`/proc/1/cgroup` → `podman inspect`）を `<btrfsPath>/town-os-version` に永続化されたバージョンファイルと比較して、イメージのアップグレードを検出する。バージョンが変わった場合: (1) すべてのコンテナイメージを pull し、(2) NC イメージを再ビルドし、(3) reconcile がすべての systemd ユニットを再生成し、(4) 内容が変わったユニットを順に再起動し（NC のユニットが先。ネットワークを所有しているため。次に依存関係のサービス、最後に親／単独のサービス）、(5) ユニットが変わったコンテナパッケージについて、更新後のコマンド（`post_update` フィールド）を `podman exec` 経由で実行する。バージョンファイルは reconcile の成功後に書かれる。ユニットの内容は `ReadUnit()` によって前後で比較され、内容が変わっていないときに不要な再起動をしないようにしている。

- **ネットワークコントローラのイメージは pull されるのであって、起動時にビルドされるのではない** —— NC のイメージは公開されている兄弟イメージ（`quay.io/town/networkcontroller:<tag>`、タグは `resolveImageTag()` による）であり、UI、rolodex、ingress のイメージとまったく同じように、他のコアのイメージと並んで pull される。起動時に `podman build` で**ビルドされることはない**。かつての起動時ビルド（`localhost/town-os-networkcontroller:local`、alpine ベース、`--dns=8.8.8.8`）はもう無い。`NC_IMAGE` は導出される既定値を上書きするもので、統合テストのハーネスがローカルにビルドしたイメージを注入するために設定するのがこれである。この pull は非致命的である。各パッケージの NC ユニットは `ExecStartPre` に `--pull=never` のネットワーク作成のフォールバックを持つので、pull の失敗は次回の起動で回復できる。

- **監視のサービスはすべてシステムサービスである** —— Prometheus、Node Exporter、Monitoring UI はすべてシステムサービスの名前空間（`town-os-system--` の接頭辞）の下で動き、reconcile の前に `main.go` から直接起動される。パッケージリポジトリのシステムを通じてインストールされることは決してない。インストール可能な「monitoring」パッケージは存在しない。三つのサービスとは: `town-os-system--node-exporter.service`（ホストネットワーク、ポート 9100）、`town-os-system--prometheus.service`（ポート 9090、`{btrfsBase}/monitoring/` から設定とデータをバインドマウント）、`town-os-system--monitoring-ui.service`（ポート 5308）である。monitoring UI のサービスは、`monitoring_backend` の設定に応じて socat のフォワーダー（uPlot モード、既定）か Grafana（grafana モード）のいずれかを動かす。Prometheus の設定はディスクへ直接書かれる。Prometheus、Grafana、uPlot の socat フォワーダーは `PackageUnitConfig.SystemServiceKey` を設定したうえで `systemd.GeneratePackageUnits` によって生成されるので、通常のパッケージと同じ配管 —— 完全なネットワークコントローラ、ソケットの起動、プライベートな podman のネットワーク —— を、システムサービスの命名で得る。

- **ホストのボリュームの所有権は `HostVolumeMount` 上で宣言的に書かれ、再帰的ではない** —— 内部の uid がハードコードされているコンテナイメージ（Grafana の `472`、Prometheus の `65534` など）は、バインドマウントされたホストのパスへ書き込む必要があり、バインドマウントはホストの所有権をそのまま通すので、コンテナが起動する前にホストのパスがその uid:gid に所有されていなければならない。名前付きの podman のボリューム（podman が初回作成時に chown してくれる）ではなくバインドマウントを使うのは、データをクォータ付きの btrfs サブボリュームに置きたいからである。`src/systemd/unit.go` の `systemd.HostVolumeMount` 構造体は任意の `UID *uint32` と `GID *uint32` のフィールドを持ち、両方が設定されているとき、ユニットの生成器はそのマウントについて `ExecStartPre=/bin/mkdir -p` の行の直後、`podman run` の前に **`ExecStartPre=/bin/chown <uid>:<gid> <hostpath>`**（`-R` は付けない）を出力する。これがシステムサービスにおけるホストのバインドマウントされたボリュームの所有権についての唯一の宣言的な出所であり、`GrafanaPackageConfig` と `PrometheusPackageConfig` にかつて手で書かれていた `ExecStartPreExtra` の chown の項目を置き換えるものである。

  chown が意図的に非再帰であり、それで十分なのは以下の理由による:
  1. **書き込み可能なマウント**（`grafana-data` → `/var/lib/grafana`、`prometheus-data` → `/prometheus`）は、コンテナがその中に自分のサブディレクトリを作れるようにするため、最上位の所有権だけがあればよい。コンテナのプロセスはそれらの子を自分の uid（472 や 65534）で作るので、既に正しく所有されており、ずれることは決してない。再帰は要らない。
  2. **読み取り専用のマウント**（`grafana-provisioning` → `/etc/grafana/provisioning`）は UID/GID を宣言しないので、chown の行も出力されない。ホスト側の権限が 0755/0644 である限り（`WriteGrafanaProvisioningFiles` がそう設定する）、誰が所有していようとどの uid でも中身を読める。

  `EnsureGrafanaStorage`（`src/monitoring/monitoring_ui.go`）は今やディレクトリを作って戻るだけであり、chown は一切しない。`WriteGrafanaProvisioningFiles` はデータソースとダッシュボードの YAML/JSON のファイルを誰でも読める権限で書くので、あとから所有権を直す必要は無い。起動のたびに `grafana-data` を歩いていたプロセス内の `filepath.WalkDir` による chown はもう無い。systemd が出力する単一の `chown` システムコールが正典の修正である。uid/gid の定数はそれぞれのファイルに残っている（`monitoring_ui.go` の `grafanaUID = 472`／`grafanaGID = 472`、`prometheus.go` の `prometheusUID = 65534`／`prometheusGID = 65534`）。上流のコンテナイメージと合わせずにこれらを変えてはならない。

- **ネットワークの状態のディレクトリはホストと共有されなければならない** —— `-network-state` の既定値は `/run/town-os`（`src/svc/systemcontroller/cmd/systemcontroller/main.go` の `DefaultNetworkStatePath`）である。systemcontroller はコンテナの中で動くが、NC のコンテナは `CONTAINER_HOST` を通じてホスト上に作るので、バインドマウントの元のパス（各 NC ユニットの `-v /run/town-os:/run/town-os:ro`）はホストのファイルシステム上に存在しなければならない。install リポジトリの systemcontroller の systemd ユニットは `/run/town-os:/run/town-os` をバインドマウントし、systemcontroller が起動する前にホストのディレクトリが存在することを保証しなければならない（`ExecStartPre=/usr/bin/mkdir -p /run/town-os` または `RuntimeDirectory=town-os`）。そのマウントが無いと、systemcontroller の `os.MkdirAll` と状態ファイルの書き込みはコンテナの tmpfs の中に着地し、ホストのディレクトリは存在せず、NC のコンテナは `Error: statfs /run/town-os: no such file or directory` で起動に失敗する —— そして Prometheus、monitoring UI、ネットワークを使うすべてのパッケージを道連れにする。`/var/run/town-os` や `/var/run`・`/tmp` の下のパスを既定にしてはならない。パスは `/run` の下（またはホストと共有される別のバインドマウント）になければならず、マウントの両側で同じパスでなければならない。

## システムコントローラの起動シーケンス

`src/svc/systemcontroller/cmd/systemcontroller/main.go` におけるシステムコントローラの起動は、まさにこの順序に従う。**(非致命的)** と書かれた各手順は stderr にログを出して続行する。それ以外はすべて致命的であり、起動を中止する。

起動は**観測可能**である。何らかの作業が始まる前に `:5309` がバインドされ、進捗を流す最小限の起動ステータスのスタブがそれを受け持つ。完全な Echo のルーターは最後に、listener を一度も閉じることなく差し替えられる。進捗は五つの粗い段階（`boot_controller`、`boot_dns`、`boot_services`、`restart_packages`、`ready`）として報告される —— [起動ステータスと更新](#起動ステータスと更新)を参照。

1. **`CONTAINER_HOST` を設定する** —— `setupPodmanEnv()` が `CONTAINER_HOST=unix:///run/podman/podman.sock` を設定し、以後のすべての `podman` の呼び出し（および子プロセス）が、systemcontroller のコンテナの隔離されたストレージではなくホストの podman のソケットを経由するようにする。
2. **CLI のフラグと環境変数を解析する** —— `-db`、`-btrfs`、`-repo-dir`、`-network-state`、`-listen`。環境変数による上書き: `TOWN_OS_LISTEN`。
3. **起動ハンドラで `:5309` をバインドする** —— `NewBootStatus()` + `NewRootHandler(NewBootHandler(bs))` が、起動の作業を何もしないうちに直ちに listener をバインドする。手順 30 の差し替えまで、このソケットは `GET /status/ping`（`{booting, step, done, boot_id}` を伴う 503）と `GET /boot-status`（SSE）だけに応答し、それ以外はすべて 403 である。
4. **段階 `boot_controller`** —— 一時作業ディレクトリ。btrfs のベースとネットワークの状態のディレクトリを作る。古い配備が btrfs のルートに残した `town-os.db` を除去し（`cleanupStaleRootDB`）、それを再び作ってしまうような `-db` のパスを拒否する（`validateDBPath`）—— 実行時の DB は `<btrfsBase>/data/db/system.db` にあり、ルートには決して置かない。
5. **SQLite のデータベースを開く** —— `-db` が設定されていれば永続的なもの、そうでなければ一時ファイル。
6. **アカウントのマネージャを初期化する** —— accounts のテーブルを作り、旧来のものを移行する（capability の列は grant になり、`smb_nt_hash` は破棄される）。続いて `PurgeLegacyServiceAccounts` **(非致命的)** が、アップグレード後の最初の起動で一度だけ、オブジェクトストレージのデーモンの古い管理者アカウントと保存されたパスワードを除去する —— [サービスアカウントは存在しない](#サービスアカウントは存在しない)を参照。
7. **一時的な JWT 署名鍵を生成する** —— `crypto/rand` による 32 バイトのランダム値。`TOWN_OS_SIGNING_KEY` で上書き可能。セッションのマネージャを初期化する。これは以前のセッションをすべて消す（新しい鍵では古いトークンは無効である）。
8. **監査、設定、pages、ネットワークの各マネージャを初期化する** —— 設定には既定値が投入される（`default_quota`、`max_archive_size`、`locale`、`dns_tld`、`dns_resolution_mode`、`peer_ttl` など）。pages は常に初期化される。ネットワークのマネージャは WireGuard のネットワークと peer のテーブルを所有し、**home ネットワークを投入する**ので、この時点以降それは常に存在する（[home ネットワークは常に存在する](#home-ネットワークは常に存在する)を参照）。
9. **リポジトリを投入する** —— `repositories.json` が存在しなければ既定のリポジトリを書く（`TOWN_OS_TEST`/`DEBUG` のときはテスト用のリポジトリ）。`TOWN_OS_REPO_USERNAME`/`TOWN_OS_REPO_PASSWORD` の資格情報を適用する。
10. **リポジトリのルートを初期化し、強制的に更新する** —— 設定されたすべてのリポジトリを go-git 経由で clone／fetch する。
11. **インストールのマネージャ、btrfs のストレージ、systemd のマネージャを初期化する**。
12. **イメージのタグを解決する** —— `resolveImageTag()`: `TOWN_OS_TAG` の環境変数（install のビルドシステムが設定する）、無ければ `rc.latest-<arch>`（`defaultVersionTag()`。アーキテクチャは `runtime.GOARCH` を `archTag()` で `x86_64`/`aarch64` に対応付けたもの）。`/town-os.tag` のファイルも、コンパイル時の `Version` の固定も無い。兄弟のイメージのタグ（UI、rolodex、ネットワークコントローラ、ingress）はすべてこの一つの値から導かれる。push のタグはアーキテクチャ別なので、導かれる兄弟のタグもそうである。
13. **NC のイメージを導出する** —— `quay.io/town/networkcontroller:<tag>`。`NC_IMAGE` で上書き可能。pull される（手順 18）のであってビルドされることはない。
14. **背景でのリポジトリの更新を開始する** —— goroutine が 5 分ごとにポーリングする。
15. **段階 `boot_dns`: rolodex のマネージャを組み立て、DNS を待つ** **(非致命的)** —— Rolodex は systemd が管理する起動サービスであり、ここに来た時点ですでに応答している。インストールイメージが書いた `rolodex.yml` を読んで systemcontroller より先に起動するからであり、そうでなければならないのは、名前を解決するものがないうちはコントローラがイメージを一つも取得できないからである。**ここでは `rolodex.yml` を書かないし、サービスを再起動もしない** —— 後述の「rolodex.yml は起動用の設定にすぎない。設定は gRPC で書き込む」の項と、その書き込みを終わらせた障害については [凍りついた設定](#凍りついた設定) を参照。この段階が組み立てるのは `rolodex.Manager` であり、その中身は Town OS が持つ設定から来る。`resolution.mode` は `dns_resolution_mode` から（保存されている値が解析できない場合は、rolodex が拒否するようなモードを書き込むのではなく既定値にフォールバックする）、フォワーダーの一覧は `dns_local_forwarders` から（有効なとき、この一覧は起動のたびにホストのリゾルバから発見されるので、ネットワークを移った機械はオペレーターが何もせずとも新しいものを拾う —— [ローカルのフォワーダー](#ローカルのフォワーダー)を参照）、ブロックリストはその保存された設定から来る。そのいずれもが、この起動の後の段階で、reconcile の中の `ProgramRolodex` によって**動いている**サーバーへ書き込まれる。rolodex のコンテナは `--net host` で動き、DNS を直接 `127.0.0.2:{port}` にバインドする。続いて DNS の準備完了を待ち（TCP の接続によるポーリング）、systemd-resolved がその TLD を rolodex へ向けるよう設定する —— **`TOWN_OS_DNS_PORT` が rolodex を `:53` から動かしている場合は飛ばされる**。resolved のドメインごとのサーバーアドレスはポートを持たないので、その TLD へのすべてのクエリがブラックホールに落ちてしまうからである。
16. **監視のバックエンドを読み、btrfs のディスクのデバイスを発見する** —— `monitoring_backend`（既定は `uplot`）。`monitoring.BtrfsDevices(btrfsPath)` **(非致命的)** が裏にあるブロックデバイスを `/monitoring/status` を通じて見えるようにする。
17. **段階 `boot_services` の最初の一歩: 自己更新** **(非致命的)** —— `SelfUpdate` はコントローラ*自身*のイメージを取り直し、動いているタグが別のイメージを指すようになっていたときは systemd に `town-os-systemcontroller.service` の再起動を求め、この起動はそこで終える（*コントローラは自分自身を更新する*を参照）。
18. **段階 `boot_services`: コアのコンテナイメージを pull する** **(非致命的)** —— NC のイメージ、Prometheus、Node Exporter、UI のイメージ、オブジェクトストレージ（gfeh）のイメージ、ingress のイメージ、そしてそのバックエンドが選ばれているときは Grafana を、`parallelEnsureImages` で並行に pull する（**流動的な**タグは起動のたびに pull し直し、固定されたタグは無いときだけ取得する —— *起動は流動的なイメージのタグを取り直す*を参照）。起動時のユニットが参照するイメージはすべてここに属する。イメージがローカルに無いユニットは `podman run` の内側で自分で pull するので、その準備完了の待ちがレジストリからのダウンロードと競走することになる。gfeh が、次いで ingress が、順にこの一覧から漏れており、そのたびに単にサービスが起動していないようにしか見えなかった。monitoring UI に独自の項目は要らない —— uPlot のバックエンドでは NC のイメージを動かしており、それは既にこの集合の先頭にある。
19. **監視のシステムサービスを起動する** **(すべて非致命的)** —— 以前の設計による古い NC／ソケットの監視のユニットが先に片付けられる（それらは今も `-p 9090`/`-p 5308` を握っており、新しいサービスをクラッシュループさせてしまう）。Node Exporter、Prometheus、Monitoring UI はすべて `--net host` で動く。node-exporter と Prometheus はループバックにバインドし、LAN に面するのは monitoring UI の `:5308` だけである。三つのポートはすべて `monitoringPortsFromEnv()` から来る。そのゼロ値が本番の既定値である（[システムサービスのホストポート](#システムサービスのホストポート)）。続いて毎晩の podman の prune のタイマーをインストールする **(非致命的)**。日次の更新のタイマーはここではインストールしない —— それはインストーラが同梱する。[自動更新](#自動更新)を参照。
20. **ローカルの TLS の CA を用意する** **(非致命的)** —— reconcile の前に `tls.EnsureCA(<btrfsPath>/tls)` を行い、reconcile がインストール済みのパッケージを歩きながらリーフ証明書を発行できるようにする。
21. **ingress と pages のサービスを起動する** **(非致命的)** —— `ingressctl.Manager` が `town-os-system--ingress`（共有の `:443` の SNI + `:80` の Host によるルーター）をインストールして起動する。デュアルスタックになるのはホストがグローバルな IPv6 を持つときだけである。pages の Caddy のサービスもそれと並んで起動する。`INGRESS_IMAGE` が明示的に空に設定されている場合（dev モード）は両方とも飛ばされる。
22. **オブジェクトストレージを reconcile する** **(非致命的)** —— `ReconcileGfeh` がネットワークごとに一つの gfeh の区画を用意する。`gfeh/<network>` のサブボリューム（uid 2000 に chown される）、描き出された `gfehd.yaml`、そして `town-os-system--gfeh-<network>` のユニットであり、ユニットは描き出された内容が変わったときにだけ再起動される。`GFEH_IMAGE` が明示的に空のときは丸ごと飛ばされ、ingress が無効のときも飛ばされる（四つの HTTP のビューはそれを通してしか到達できない）。区画の*名前*はもっと後で非同期に公開される —— 手順 31 を参照。[オブジェクトストレージ（gfeh）](#オブジェクトストレージgfeh)を参照。
23. **バージョンの変更を検出する** —— 動作中のコンテナのイメージ SHA（`/proc/1/cgroup` → `podman inspect`）を `<btrfsPath>/town-os-version` と比較する。reconcile のために `versionChanged` を設定する。
24. **reconcile** —— インストール済みのすべてのパッケージを走査し、実行時の状態を復元する:
    - ルートの btrfs サブボリューム（`installed`、`uninstalled`、`archives`、`pages`、`vm-images`、`user`、`tls`、`gfeh`）を作る。
    - インストール済みの各パッケージ（リポジトリ／名前ごとに最新のバージョン）について: YAML を読み、保存された回答でコンパイルし、クォータ付きの btrfs のボリュームを作り、空のボリュームをアーカイブ／git／proton から初期投入し、ファイルテンプレートを適用し、そのパッケージの TLS のリーフを発行し、ネットワークの状態のファイル（解決済みの `fqdn` を含む）を書き、systemd のユニット（サービス + NC + ソケット）を生成してインストールし、サービスを起動する。
    - `versionChanged` の場合: 内容が変わったユニットを再起動し（NC が先、次に依存関係、最後にサービス）、続いて `post_update` のコマンドを実行する。
    - pages を reconcile する: サブボリューム、シンボリックリンク、page の内容を用意する。
    そのうえで現在のイメージ SHA を `<btrfsPath>/town-os-version` に永続化する。
25. **DNS とネットワークを reconcile する** —— rolodex の gRPC のソケットへ接続する（最大 30 秒まで再試行）。`RebuildDNS` は rolodex を一度消してゼロから作り直すので、前回の実行がクラッシュして生じたずれは捨てられる。`RebuildNetworkDNS` は既定でないネットワークのパッケージについて、LAN に面するグローバルなレコード（と DANE のピン）を登録し直す。続いて `ReconcileNetworks` が home ネットワークの TLD を `dns_tld` に対して reconcile し、有効なすべてのネットワークの WireGuard のインターフェイスを立ち上げる。その際 rolodex のクライアントを渡すので、各ネットワークの TLD のスコープが所有される —— DNS だけの home のスコープも含めて。すべて非致命的である。その後オブジェクトストレージが**二度目に** reconcile される（冪等である）ので、この手順が立ち上げたネットワークは再起動を待たずに自分の区画を得る。
26. **ingress をプログラムする** **(非致命的)** —— 準備完了を待ち、その gRPC のソケットへ接続し、`RebuildIngress` が経路の全集合（HTTP のパッケージ + pages + オブジェクトストレージのビューとインデックス）を宣言的に押し込む。`RebuildDNS` と同じ模型である。同じ走査の中で、区画が生成する二つのページ —— その[インデックス](#区画のインデックス)と[公開ファイルのインデックス](#公開ファイルのインデックス) —— を、それらの経路が組み立てられる元となったサイトの集合そのものから描き出す。経路は、それが提供するバイト列が存在するより前にプログラムされてはならないからである。
27. **UI のコンテナを起動する** **(非致命的)** —— `town-os-system--ui.service`。`UI_IMAGE` が明示的に空のとき（bun が UI を提供する dev モード）は飛ばされる。
28. **段階 `restart_packages`: 鮮度の段階** —— 前のプロセスが更新のマーカーを残していた場合、インストール済みのすべてのパッケージのユニットを直列に再起動し、パッケージごとの進捗のイベントを発行して UI がそれぞれ一行を描けるようにする。クラッシュによって残った古いマーカーは無害である。
29. **HTTP のハンドラを作る** —— すべてのマネージャを `ServerConfig` へ配線し、背景のポーラー（外部 IP を毎時、DNS のずれの修復、期限切れの peer の刈り取り）を起動し、Echo のルーターを CORS、フェイルクローズドな grant の許可リスト、認証、監査のミドルウェアとともに構成する。
30. **段階 `ready`: ルートのハンドラを差し替える** —— 起動のスタブが、既にバインドされている listener の上で完全な Echo のルーターへ原子的に置き換えられるので、ポートの瞬断は起きず、進行中の `/boot-status` の SSE の購読者は引き継ぎを生き延びる。続いて `BootStatus.Done()` がストリームを閉じる。**ここでシステムは準備完了である。**
31. **オブジェクトストレージの名前を公開する** **(非致命的、背景)** —— `publishGfehNames` は少なくとも一つの区画が自分の admin のソケットに応答するまで待ち、そのうえで DNS と ingress の再構築を走らせ直して、各区画の `/v1/names` の出力が A レコード、TLSA のピン、リーフの SAN、ingress の vhost になるようにする。これは差し替えの**後**に、しかも非同期に走る。gfehd は認証の前に `/status/ping` をポーリングするが、それは手順 29 まで 503 を返すので、これをその場で待つと、待っている当の起動をデッドロックさせてしまうからである。時間内に何も準備完了にならなければ、名前は次回の reconcile で公開される。
32. **グレースフルシャットダウン** —— SIGINT で: context をキャンセルし、HTTP サーバーを 30 秒のタイムアウトで停止する。背景のすべての goroutine は context のキャンセルによって終了する。


# Town OS 機能仕様

Town OS は家庭のユーザーのための自己ホスト型のクラウドプラットフォームである。USB ドライブから RAM 上で丸ごと動き、システムのストレージはすべてユーザーのデータに使う。パッケージング、ストレージ、ネットワークは完全に統合されている。ウェブ UI が技術者でないユーザーのための管理手段を提供する。

## Git のライブラリ

内部の git の操作はすべて、`git` の CLI を起動するのではなく純 Go のライブラリ（`go-git/go-git/v5`）を使う。

### クライアントのインターフェイス

`git.Client` のインターフェイスがすべての git の操作を抽象化する:

- **Clone** -- 親ディレクトリの中の名前付きのサブディレクトリへリポジトリを clone する。
- **Pull** -- rebase 付きで pull する。
- **Diff** -- 作業ツリーにコミットされていない変更があるかを報告する。
- **Stash / StashApply** -- コミットされていない変更を stash し、適用し直す。
- **Fetch** -- origin のリモートから fetch する。
- **Checkout** -- ブランチ、タグ、コミットのハッシュを checkout する。
- **Init** -- 新しいリポジトリを初期化する。親ディレクトリが存在しない場合はエラーを返す。
- **Add** -- pathspec でファイルをステージする（すべてのファイルを表す `"."` に対応する）。
- **Commit** -- ローカルの git のユーザー設定を使ってコミットを作る（`Town OS <town-os@localhost>` にフォールバックする）。
- **RevParse** -- 参照を SHA のハッシュへ解決する。
- **Run** -- 任意の git のサブコマンド（`config`、`branch`、`rev-parse --abbrev-ref`、`log`、`init`、`status`）を発行する。

### 実装

`GoGitClient` が `go-git` を使ってこのインターフェイスを実装する。以下に対応する:

- URL に埋め込まれた資格情報（`scheme://user:pass@host/...`）。取り出して `http.BasicAuth` として渡される。
- すべての操作における context によるタイムアウトとキャンセル。
- 隔離された操作のために HOME ディレクトリを上書きする `Home` フィールド。

### モックのクライアント

`MockClient` はユニットテストのためのスレッドセーフなモックの実装を提供する。すべてのメソッドの呼び出しを引数とともに記録し、メソッドごとに注入可能なエラーと戻り値に対応する。

### 使いどころ

- **パッケージリポジトリ**: リポジトリの更新のための clone、pull（汚れたツリーの周りで stash／apply する）、fetch（`GoGitClient` 経由）。
- **ボリュームの初期投入**: インストールと reconcile の際に、空のボリュームへ git のリポジトリを clone する（`GoGitClient` 経由）。
- **Pages**: 静的サイトのリポジトリの clone と更新（`GoGitClient` 経由）。
- **git ソースの再ビルド**: インストール済みのパッケージの git のボリュームを更新し、依存するサービスを再起動する（`GoGitClient` 経由）。

## リポジトリの管理

### リポジトリの模型

リポジトリは名前、URL、任意の資格情報（ユーザー名とパスワード）で定義される。ベースディレクトリの `repositories.json` ファイルに保存される。何も設定されていない場合は既定のリポジトリが投入される。

### リポジトリ API

- `POST /repository/add`（管理者が必要）-- 新しいリポジトリを追加する。名前、URL、任意のユーザー名／パスワードの資格情報を受け取る。資格情報が渡されない場合はシステムの既定の資格情報が使われる。リポジトリは go-git 経由で clone され、更新が起動される。
- `POST /repository/remove`（管理者が必要）-- 名前でリポジトリを削除し、更新を起動する。
- `POST /repository/move`（管理者が必要）-- リポジトリの優先順位の位置を変える。名前と目標の位置のインデックスを受け取る。
- `POST /repository/refresh`（管理者が必要）-- すべてのリポジトリを強制的に更新する。更新のエラーがあれば返す。
- `GET /repository`（認証が必要）-- すべてのリポジトリを検索、並べ替え、ページネーション付きで一覧する。各項目には名前、URL、ユーザー名、更新のエラーがあればそれが含まれる。

### リポジトリの更新

リポジトリは定期的に（既定では 5 分間隔で）go-git 経由で origin から fetch して更新される。更新の間、汚れたツリーの周りでは stash／apply が使われる。更新のエラーはリポジトリごとに追跡され、一覧と status の ping のエンドポイントを通じて公開される。
## パッケージシステム

### パッケージの定義

パッケージは以下の構造の YAML で定義される:

- `image` -- コンテナイメージの参照（`vm` とは排他）。
- `vm` -- 仮想マシンの設定（`image` とは排他）。下記の **VM の設定**を参照。
- `proton` -- Windows の実行ファイルのための Proton/Wine のランナーの設定（`vm` および `command` とは排他）。下記の **Proton の設定**を参照。
- `entrypoint` -- podman の run の時点でイメージ組み込みの `ENTRYPOINT` を置き換える文字列のリスト。`podman run --entrypoint='["..."]'` として出力される（JSON の配列であり、systemd がそのまま転送するようシングルクォートで囲む）。上流の ENTRYPOINT が任意のコマンド引数を拒否するラッパースクリプトであるイメージには必須である（例えば `matrixdotorg/synapse` の `/start.py` は最初の引数を「モード」として解釈し、知らない値ではエラーになる —— `command: [sh, -c, "…"]` を使いたいパッケージは、podman が `/start.py` を丸ごと置き換えるよう `entrypoint: [sh, -c]` も設定しなければならない）。コンテナのランタイム専用であり、VM のパッケージでは拒否され（`ErrEntrypointVMNotSupported`）、Proton のパッケージでも拒否される（Proton は自分でコマンドを自動生成する）。
- `command` -- コンテナの CMD になる文字列のリスト（entrypoint の**後**に渡される argv）。コンテナのランタイム専用であり、`proton` とは排他。空白やシェルのメタ文字を含む複数語の引数は、生成されるユニットファイルの中でシングルクォートで囲まれるので、systemd の ExecStart のトークナイザはそれを一つの argv の要素として転送する —— 連結された `"a && exec b"` の文字列は一つの引数のままであり、その `&&` は systemd に分割されるのではなく（entrypoint が `[sh, -c]` のとき）`sh -c` へ転送される。
- `environment` -- キーと値の環境変数（テンプレートの置換に対応。コンテナのランタイム専用）。
- `network` -- 外部と内部のポートの対応付け（テンプレートの置換に対応）。
- `volumes` -- 名前付きのボリューム。マウント先、任意のクォータ、任意のアーカイブの出所、任意の git の初期投入の URL、任意の UID/GID を持つ。
- `questions` -- インストール中にユーザーへ提示される名前付きの質問。
- `notes` -- インストール後に表示される型付きのメタデータ（URL、電話、メール）。型はコンパイル中に検証される。URL は有効な URL として解析できなければならず、メールは `user@domain.tld` の形式に一致しなければならず、電話番号は任意の整形用の文字を含む数字に一致しなければならない。
- `description` -- 人が読めるパッケージの説明。
- `supplies` -- このパッケージが提供する機能の一覧。
- `archives` -- インストール時にボリュームを埋めるためのコンテナイメージのアーカイブの一覧（コンテナのランタイム専用）。
- `templates` -- Go の text/template によってボリュームへ描き出される名前付きのファイルテンプレート。各テンプレートは対象のボリューム、ファイルのパス、テンプレートの内容を指定する。
- `post_update` -- reconcile 中にイメージ SHA の変更が検出されたあと、動作中のコンテナの中で実行するシェルコマンドの一覧（コンテナのランタイム専用。VM のパッケージには対応しない）。下記の**更新後のコマンド**を参照。

### ランタイムの種別

各パッケージにはランタイムの種別がある: `container`（既定）または `vm` である。ランタイムはどのトップレベルのフィールドがあるかで決まる。`image`（または `proton`）はコンテナのランタイム（podman）を選び、`vm` は VM のランタイム（QEMU）を選ぶ。パッケージは `image`/`proton` か `vm` のちょうど一方を指定しなければならない。両方あるいはどちらも指定しないのは検証エラーである。Proton のパッケージはコンテナのパッケージの特殊な形である —— コンテナのランタイムを使いつつ、コマンドを自動生成し、Windows のアプリケーションのファイルを別のコンテナイメージから取り出す。

### VM の設定

`vm` のセクションは QEMU の仮想マシンを設定する:

- `image` -- VM のディスクイメージの URL またはローカルのファイル名（必須）。リモートのイメージのための HTTP/HTTPS の URL でもよいし、`vm-images` のサブボリュームにキャッシュされたイメージを指すファイル名でもよい。`@variable@` のテンプレートの置換に対応する。
- `memory` -- 人が読めるバイト列としての VM のメモリ（`2gb`、`512mb` など）。既定は `1gb`。`@variable@` のテンプレートの置換に対応する。
- `cpus` -- 仮想 CPU の数。既定は `1`。非負でなければならない。

### Proton の設定

`proton` のセクションは、Proton/Wine の互換レイヤーを通じて動かす Windows のアプリケーションを設定する:

- `app_image` -- Windows のアプリケーションのファイルを含むコンテナイメージの参照（必須）。コンパイル中に正規化される。`@variable@` のテンプレートの置換に対応する。
- `app_directory` -- コンテナ内でアプリケーションがインストールされている絶対パス（必須。`/app` など）。`@variable@` のテンプレートの置換に対応する。
- `volume` -- アプリケーションのファイルの展開先となる、定義済みのパッケージのボリュームの名前（必須）。`@variable@` のテンプレートの置換に対応する。
- `exe` -- 実行する Windows の実行ファイルへのパス（必須。`/app/myapp.exe` など）。`@variable@` のテンプレートの置換に対応する。
- `args` -- 実行ファイルへ渡す任意のコマンドライン引数。各要素が `@variable@` のテンプレートの置換に対応する。

インストール時、システムは `app_image` を pull し、`app_directory` を名前付きのボリュームへ展開し、コンテナのコマンドを `proton run <exe> [args]` として自動生成する。アプリケーションを動かすのに使うコンテナイメージはシステム全体の `proton_image` の設定（既定では `quay.io/town/proton:latest`）であり、パッケージごとに `image` を設定して上書きできる。reconcile の際、アプリの展開は対象のボリュームが空のときにのみ繰り返される。

### テンプレート変数

テンプレートの置換は `@variable_name@` の構文を使う。変数はパッケージのコンパイル中に質問の回答で置き換えられる。置換が適用されるのは: environment の値、network のポート名と宛先、ボリュームのマウント先、ボリュームのクォータ、ボリュームのアーカイブの参照、ボリュームの git の URL、VM のイメージの URL、VM のメモリの値である。組み込みの変数も二つ使える: `@LOCAL_EXTERNAL_HOST@` と `@LOCAL_INTERNAL_HOST@` である。

`@@` の並びはリテラルの `@` のエスケープである。リテラルの `@` に続けてテンプレート変数を出したい場合は `@` を三つ使う: `@@@variable@` である。例えば `ssh://git@@@PACKAGE_DNS@:@sshport@` は `ssh://git@gitea.default.home:2222` に解決される。単独の `@@` は `@` に解決される（例: `admin@@example.com` → `admin@example.com`）。

note のコンパイルは単一パスの解決器（`ApplyTemplates`）を使い、文脈の変数（`PACKAGE_DNS`、`LOCAL_EXTERNAL_HOST`、`LOCAL_INTERNAL_HOST`）とユーザーの回答を一つのパスにまとめ、`@@` のエスケープを正しく扱う。他のフィールド（environment、ポート、ボリューム）はキーごとの解決器（`applyTemplate`）を使い、複数のパスを通して `@@` を保ち、`Compile` の最後で `@@` → `@` の解決を行う。

### 質問

質問はパッケージのインストール中にユーザーへ問いかける。各質問は `query`（表示するテキスト）、任意の `type`（検証のための出力の型）、任意の `default` の値を持つ。質問の名前は英数字で始まらなければならず、英数字とアンダースコアだけを含みうる（`port`、`dbpass`、`registration_secret` など）。ダッシュ、ドット、その他の記号は拒否される。アンダースコアが許されるのは、質問の名前が `@template@` の目印として使われ、`registration_secret` のような複数語の識別子が実際のパッケージでよくあるからである。

#### 出力の型

- **port** -- 検証されたポート番号（1--65535）。回答が空か `"auto"` のとき、10000--60000 の範囲で空いているポートをランダムに自動生成する。
- **hostname** -- ダッシュを含む小文字の英数字。空のとき `<package-name>-<4-char-hex>` を自動生成する。
- **volume** -- ダッシュとアンダースコアを含む英数字。
- **bytes** -- 人が読めるバイトのサイズ（`mb`、`gb`、`tb` の接尾辞）。
- **archive** -- アーカイブのファイル名。
- **duration** -- 時間の長さ（`s`、`m`、`h`、`d` の接尾辞）。
- **secret** -- 回答が空か `"auto"` のとき、暗号学的に安全な値を自動生成する。`crypto/rand` によって 32 バイトを生成し、64 文字の 16 進文字列（256 ビットのエントロピー）として返す。パスワード、暗号鍵のソルト、その他の秘密の値に適する。ユーザーは明示的な回答を与えて上書きできる。
- **boolean** -- はい／いいえの選択肢。インストールの質問のダイアログではテキスト入力ではなく**チェックボックス**として描かれる。検証は `strconv.ParseBool` であり、yaml.v3（YAML 1.2）が真偽値として扱う綴りに `1`/`0`/`t`/`f` を加えたものを、大文字小文字を区別せず受け付ける。`yes`/`no` は受け付け**られない**。回答は文字列の `"true"` または `"false"` へ正規化されるので、`@variable@` の置換とファイルテンプレート（`{{.Responses.key}}`）は常に一つの正典の形を見ることになり、`{{if eq .Responses.key "true"}}` で検査できる。

  チェックの入っていないチェックボックスは何も送らないし、依存関係の真偽値の質問は親から回答されないままであることが多い —— そのままではどちらも `Compile` の空の回答の検証に引っかかってしまう。そこで `autoGenerateResponses`（`controller_install_preview.go`）は、欠けているか空の真偽値を、その質問の `default`（正規化したもの）へ、`default` が宣言されていなければ `"false"` へ解決する。フォームからの明示的な `"false"` は常に `default: "true"` に勝つので、既定で入っている選択肢を実際に切ることができる。`strconv.ParseBool` が解析できない `default` はパッケージのバグであり、その選択肢を切った状態で黙ってインストールするのではなくインストールを失敗させる。

  パッケージの情報のダイアログは、保存された真偽値の回答を生の `"true"`/`"false"` の文字列ではなく Yes/No として描く。また真偽値の質問は、インストールのダイアログにおけるキャッシュ済みの値／消去ボタンの経路を通らない —— 保存された回答は単にチェックを入れた状態にするだけで、そのまま直接編集できる。

- **oauth** -- 打ち込むのではなく、インストールのダイアログからデバイスフローを走らせて得るトークン。secret と同じように検証され（空でない任意の文字列）、自動生成されることは決してなく、パッケージの情報のダイアログではマスクされる。インストールのダイアログはテキストの欄の代わりに **Connect** のボタンを描く。以前のインストールでキャッシュされた回答があれば既に接続済みとして描かれるので、再インストールでオペレーターを提供元へ送り返すことはない。

#### OAuth の質問

アプリケーションによっては、提供元にしか発行できない資格情報で設定される —— Plex のアカウントのトークン、GitHub の個人トークンなど —— そして、それを得る唯一の方法は、シェルスクリプトを手で走らせてその出力を貼り付けることだった。`oauth` の質問は、その手順をダイアログから走らせる。

**提供元の登録簿は存在しない。** 質問は提供元自身の URL を名指しする `oauth:` のブロックを持つので、パッケージは Town OS を変更することなく、デバイス風のフローを持つどの提供元でも使える:

```yaml
questions:
  plextoken:
    query: "Plex account"
    type: oauth
    oauth:
      start: { method: POST, url: "https://plex.tv/api/v2/pins?strong=true", headers: { X-Plex-Client-Identifier: "{{client_id}}" } }
      extract: { id: id, code: code }
      approve: "https://app.plex.tv/auth#?clientID={{client_id}}&code={{code}}"
      poll: { url: "https://plex.tv/api/v2/pins/{{id}}", headers: { X-Plex-Client-Identifier: "{{client_id}}" } }
      token: authToken
      interval: 2s
      timeout: 10m
```

`start` がフローを開き、`extract` がその応答から取り出す JSON のフィールドを名指しし、`approve` がブラウザの開く URL であり、`poll` は `token` が名指しする JSON のフィールドが「無い、あるいは null」でなくなるまで繰り返される —— それがまさに、通信の上で「ユーザーがまだ承認していない」という状態の見え方である。`{{...}}` のプレースホルダは、取り出された値と `{{client_id}}` に対して解決される。`{{client_id}}` はコントローラがすべての手順で送るフローごとのランダムな識別子である（Plex は pin をそれに結び付ける）。取り出された JSON の数値は `1.234567e+06` ではなく数字として描き出される —— 浮動小数点で整形された pin の id は poll の URL を 404 にし、「保留中」のまま永遠に固まってしまう。

このフローは `src/packages/oauth.go`（スキーマと検証）と `src/svc/systemcontroller/controller_oauth.go`（実行）にある。`POST /packages/oauth/start` が start の手順を走らせて `{flow_id, approve_url, user_code, interval_ms}` を返し、`POST /packages/oauth/poll` が poll の手順を一回走らせて `pending`、トークン付きの `approved`、または `expired` を返す。どちらも管理者が必要である。サーバーはフローを引き換えられるまでしか保持しない —— トークンはブラウザへ渡され、ブラウザは他の回答と同じようにそれを質問の回答として送るので、サーバー側に写しを持っておくことは、それが漏れうる場所を一つ増やすだけである。

検証は二つの半分から成り、それらを混同するのはバグである。`ValidateOAuthSpec` はフローの*形*（必須のフィールド、解析可能な時間、URL のホストにテンプレートが無いこと）を検査するもので、パッケージがインストールされるときに `Compile` が走らせるのはこれである。`ValidateOAuthFlow` はそれに加えて下記のアドレスの方針を含み、フローがまさに*実行*されようとするときにだけ走る。インストールは、そのフローが走ったずっと後に、`Compile` からは見えない `OAuthAllowPrivate` の設定を持つホストの上で起きる —— なのでコンパイル時にアドレスの方針を適用すると、たった今フローが成功したばかりのインストールを拒否してしまう。

**アドレスの番人は荷を負っている。** パッケージは任意の URL を名指しし、それへ接続するのは*コントローラ*なので、番人が無ければパッケージはそれをホスト自身のネットワークへ向けられてしまう。`packages.CheckOAuthAddr` は HTTP のクライアントの `DialContext` の中で（そしてすべてのリダイレクトで）走り、ループバック、プライベート、リンクローカル、マルチキャスト、未指定、CGNAT のアドレスを拒む。URL は `https` でなければならない。解析の時点ではなく接続の時点で検査することが、これを DNS リバインディングに耐えるものにしている。`ServerConfig.OAuthAllowPrivate` はこれを緩めるもので、テストがフローを 127.0.0.1 の `httptest` のサーバーへ向けられるようにするためだけに存在する。

#### 任意の質問

どの質問も `optional: true` を設定できる。それ以外のすべての質問は空でない値で答えられなければならず、そのままではパッケージの作者に、アプリケーションが本当に無くても済ませられる設定 —— SMTP のリレー、API キー —— を表す手立てが無い。仮のプレースホルダの既定値をひねり出して、オペレーターがそれを上書きしてくれることを願うほかない。

任意の質問は回答のマップから欠けていてもよいし、空文字列で答えられてもよい。`Compile` はそれを `ErrMissingResponse` と `ErrEmptyResponse` の両方から免除し、その `@variable@` の箇所には**空文字列**を差し込む。空の回答は `OutputType.Output` も飛ばす。型付きの質問に対してまさにそれを拒否するのがその仕事だからである —— 空文字列は有効なポートではない。したがって `optional` は `type` と組み合わさる: 答えられた任意のポートは今もポートとして検証され、空のものは何も無いところへコンパイルされる。

正しさのために二つの細部が効いてくる。`Compile` は渡された回答を歩いて置換するので、マップから丸ごと欠けている質問には二度目のパスが与えられ、その目印が空文字列で埋められる。それが無ければリテラルの `@smtp_host@` がコンテナの環境まで生き延びてしまう。そして `autoGenerateResponses` は型による分岐の前に任意の質問を飛ばす。値を生成してしまってはその質問の意味が失われるからである —— 空のままにしたい任意の secret が、そうしないとランダムな 256 ビットの文字列として届き、アプリケーションはそれで律儀に認証を試みることになる。空の任意の質問は、`default` を宣言していればそれに、そうでなければ空文字列にフォールバックする。

`optional` は真偽値には意味が無い。真偽値はチェックボックスであり、常にその二つの値のどちらかに解決されるからである。

#### 条件付きの質問（`show_if`）

質問は `show_if: <boolean_question>` を持つことができ、同じパッケージの中の真偽値の質問を名指しする。インストールのダイアログは、そのチェックボックスにチェックが入るまでその質問を隠しておくので、パッケージは上級者向けの一群 —— SMTP のリレー、API キー —— を、あらゆる欄をいきなりオペレーターに突きつける代わりに、一つのスイッチの後ろへ畳んでおける。

これは UI 上の助言以上のものである。コンパイラがそれを尊重する。制御している真偽値が偽に解決される間、条件付きの質問は**空文字列**へコンパイルされ、「答えられていて空でない」という要求から免除される —— ちょうどそれが `optional` であって空のまま残されたかのように —— *まだマウントされたままの欄が何を送っていようと関係なく*。`questionHidden`（`src/packages/questions.go`）は制御の値を送られた回答から読み、オペレーターが一度も触っていない場合は真偽値の宣言された `default` にフォールバックし、チェックの入っていないボックスは `"false"`、`"0"`、あるいは何も来ないという形で届きうるので、寛容に解析する。`Compile` は隠された質問について空文字列を強制し `Output()` を飛ばすので、オペレーターに見えてすらいない欄について古い値が型の検証を落とすことはありえない。回答のマップから丸ごと欠けている質問も、その `@marker@` の箇所は空文字列で埋められる。真偽値が真のときは、任意でない条件付きの質問は通常どおり必須である。

`ValidateShowIf` は、存在しない質問を参照する `show_if`（`ErrShowIfUnknown`）、`boolean` の型でない質問を参照するもの（`ErrShowIfNotBool`）、その質問自身（`ErrShowIfSelf`）、そしてそれ自体が条件付きである別の質問（`ErrShowIfChain` —— 連鎖は禁止）を拒否する。条件付きの質問が筋が通るのは、その可視性を制御しているものが素のチェックボックスであるときだけである。

### コンパイル

コンパイルはすべての回答を検証し、型ごとの検証を適用し、すべてのテンプレート変数を置換し、コンテナイメージの URL を正規化し、解決済みの `Package` 構造体を作る。VM のパッケージについては、メモリの文字列がバイト数へ解析され、CPU の既定値が適用される。更新後のコマンドは前後の空白が取り除かれる。検証のエラーは集められてまとめて返される。

**systemd のユニットに届く値は、制御文字を含んでいてはならない。** ユニットファイルは行が単位であり、その引用は行をまたがない。どんな引用符で囲まれていようと、指示は最初の生の改行で終わる。したがって、改行を含む値は自分の行を壊すだけでは済まない —— 改行より後はすべて同じ `[Service]` セクションの新しい指示として解析され、`somevalue\nExecStartPre=/bin/sh -c '…'` という環境の値は生成されるユニットへ `ExecStartPre` を一つ追加する。これは単に悪い出力を生むのではなく、権限の境界をまたぐ。パッケージの作者は既にイメージとコマンドを支配しているが、それは*コンテナの中*で何が動くかに対する権限である。一方 systemd の指示は、podman が呼ばれるより前に、**ホスト上で root として**動く。

`packages.ValidateNoControlChars` はすべての C0 制御文字と DEL を拒否する。**タブだけが唯一の例外である** —— これは正当な空白であり、systemd のトークナイザはこれを区切りとして扱い、引用は本当にこれを含みうる。

この検査は**二度走り、どちらのパスも荷を負っている**:

- `InputPackage.Validate()` は `environment`、`command`、`entrypoint` における作者のリテラルを受け持つ。これは `Compile` の*先頭*で走るので、置換前のテキストしか見ない。
- `Compile` の最後における**コンパイル済みの**パッケージ全体の走査が、置換後のすべてを受け持つ。environment の値、command、entrypoint、ボリュームのマウント先、`post_update` である。効いてくるのはこちらのパスである。YAML の中で裸の `@marker@` である値は、それ自体は制御文字を含まないので `Validate()` を通る。改行は*回答*とともにやってくる。`type:` を宣言しない質問は他の何にも検証されないので、実際に呼び出し側の選んだバイト列を持ってユニットファイルへ届くのは、この回答の経路である。

`systemd.quoteCommandArg` は最後の砦として同じ文字を取り除く。ユニットの生成にはエラーの戻り値が無く、バイト列が `/etc/systemd/system` へ書かれる直前の最後の地点だからである。これはエスケープするのではなく**捨てる**。systemd は引用の中の C 風のエスケープを確かに解決するが、そのバイトをそもそも届ける正当な理由が無いときに、セキュリティの境界をパーサーの細部の上に載せても得るものは何も無い。

これまで動いていたものが拒否されることは無い。複数行の値は既に壊れたユニットを生んでいた。変わったのは、誰も点検しないユニットを黙って生成する代わりに、コンパイル時に大きな音を立てて失敗するようになったことである。

### 更新後のコマンド

`post_update` のフィールドは、reconcile 中にシステムコントローラがイメージ SHA の変更を検出したあと、動作中のコンテナの中で実行されるシェルコマンドの文字列の一覧である。これにより移行の作業を自動化できる（PostgreSQL のコンテナが更新されたあとの `pg_upgrade` など）。

- **コンテナ専用** -- `post_update` は VM のパッケージについては検証中に拒否される（`ErrPostUpdateVMNotSupported`）。
- **テンプレートの置換** -- 各コマンドは質問の回答からの `@variable@` の置換に対応する。environment と network のフィールドと同じである。
- **空白の除去** -- 各コマンドはコンパイル中に前後の空白が取り除かれる。空、あるいは空白だけのコマンドは検証中に拒否される。
- **実行の引き金** -- コマンドが実行されるのは、`ReconcileConfig.VersionChanged` が真であり、**かつ**そのパッケージの systemd のユニットの内容が以前にインストールされたユニットと異なるときだけである。どちらかが偽なら、コマンドは走らない。
- **実行の順序** -- コマンドは、バージョン変更によるすべての再起動が完了したあと（NC のユニットが先、次に依存関係、次にサービス、そして更新後のコマンド）、順に走る。パッケージの中では、コマンドは一覧の順に走る。
- **実行の方法** -- 各コマンドは `podman exec <container-name> sh -c '<command>'` によって 5 分のタイムアウト付きで走る。`ReconcileConfig` の `PostUpdateExec` の関数が実行の仕組みを提供する。nil にすると更新後の実行は無効になる。
- **非致命的** -- コマンドの失敗はログに出るが、reconcile を止めることも、以後のコマンドの実行を妨げることもない。

パッケージ YAML の例:

```yaml
image: postgres:16
post_update:
  - "pg_upgrade --check"
  - "pg_upgrade"
  - "vacuumdb --all --analyze-in-stages"
```

### ファイルテンプレート

テンプレートはパッケージ YAML の中の名前付きのオブジェクトであり、三つのフィールドを持つ: `volume`（対象のボリュームの名前）、`path`（ボリューム内のファイルのパス）、`content`（Go の text/template の文字列）である。

テンプレートのデータの文脈は四つの名前空間を提供する:

- `.Responses.key` -- 質問の回答の値（質問の名前を鍵とする）。
- `.Package.Name`、`.Package.Version`、`.Package.Repo`、`.Package.Image`、`.Package.Description` -- パッケージのメタデータ。
- `.System.Hostname`、`.System.ExternalIP`、`.System.InternalIP` -- システムレベルの情報。
- `.Dep.KEY.Host` と `.Dep.KEY.Ports` -- インストール済みの依存関係の実行時の座標。鍵は親の YAML が `dependencies:` の下で宣言するのと同じ dep の鍵である。`Host` は podman のコンテナ名（共有ネットワーク上の podman の DNS で解決できる）。`Ports` は `map[string]string` であり、コンテナのポートの数値（例: `"5432"`）と、その dep の network の項目で宣言された意味のある名前（小文字化したもの。例: `"sql"`）の両方を鍵とする。名前付きのポートには `{{index .Dep.db.Ports "sql"}}` でアクセスする。dep の無いパッケージではこのマップは nil である。存在しない dep への `{{.Dep.db.Host}}` は（他のあらゆる欠けたマップの鍵と同じく）`<no value>` を描き、nil の `Ports` への `index` は意図的にエラーになるので、誤設定のテンプレートは大きな音を立てて失敗する。

`volume` と `path` のフィールドは `@variable@` の置換に対応する（environment、network、ボリュームのフィールドが使うのと同じ仕組みである）。`content` のフィールドは Go の `text/template` の構文を使い、`{{.Responses.key}}`、`{{.Package.Name}}`、`{{.Dep.KEY.Host}}` などを用いる。`@dep_*@` の目印の形は `content` の中では尊重され**ない** —— 代わりに Go のテンプレートの `.Dep` の名前空間を使うこと。`@dep_*@` は `environment:` の値と dep の `responses:` のブロックの中では今も正しい形である。

テンプレートはボリュームの初期投入（アーカイブ、git の clone）**および依存関係のインストールの後**に適用されるので、親の content が描かれる時点では `.Dep` は埋まっている。reconcile の際、テンプレートは描き直されるが既存のファイルが上書きされることは決してなく、アーカイブのアップロードや以前の実行によるデータは保たれる。dep のマップは永続化された依存関係のレコードから作り直されるので、reconcile が実際に欠けているテンプレートを書くときにも `.Dep` は解決される。

検証が強制するのは: テンプレートの名前がボリュームの命名の規約（ドット、ダッシュ、アンダースコアを含む英数字）に従うこと、パスが相対でありディレクトリの遡上を含まないこと、ボリュームが定義済みのパッケージのボリュームを参照していること（ボリュームのフィールドがテンプレート変数を含む場合を除く）、そして content が有効な Go の `text/template` として解析できること、である。

### イメージの正規化

コンテナイメージの参照はコンパイル中に正規化される:
- 単一の名前（`nginx`）は `docker.io/library/nginx:latest` になる。
- 二つの要素（`user/app`）は `docker.io/user/app:latest` になる。
- 完全な参照はそのまま保たれる。タグが無ければ `:latest` が付け足される。

### 回答の永続化

回答はバージョンごとに `responses/<repo>/<pkg>/<version>.json` へ保存される。`last` の写しが `responses/last/<repo>/<pkg>.json` へ保存され、アップグレードや、アンインストール済みのボリュームからの再インストールの際に再利用される。last の回答はインストールの成功後に消される。

last の回答を管理する API のエンドポイントは二つある:

- `POST /packages/last-responses`（管理者が必要）-- パッケージ（リポジトリと名前で指定）についてキャッシュされた last の回答を取得する。
- `POST /packages/clear-last-responses`（管理者が必要）-- キャッシュされた last の回答のファイルを削除する。

### インストールの質問の UI

ユーザーがパッケージをインストールするとき、質問のダイアログは既存の回答（現在のインストールからのもの）を読み込み、それが無ければキャッシュされた last の回答（以前のアンインストールからのもの）を読み込む。現在の回答は last の回答に優先する。

**キャッシュされた回答**は、抑えた背景色の読み取り専用の整形された枠として表示され、保存された値を示す（パスワードは `********` として表示される）。隠しのフォームの入力が送信のためにその値を保つ。キャッシュされた各欄には、ツールチップ（「Clear to enter a new value」）付きの消去ボタン（X のアイコン）があり、押すと読み取り専用の表示が編集可能な入力へ置き換わる。消去ボタンはゴースト風の見た目で、ホバーすると赤くなる。

**既定値**は、キャッシュされた値が無いとき二通りに示される。入力欄のプレースホルダのテキストとして（例: 「Default: 8080」）と、入力欄の下の抑えた色の補助テキストとして（値は等幅で表示）である。既定値が定義されていないときは型ごとのプレースホルダが示される。ポートには「Auto-assigned if empty」、ホスト名には「Auto-generated if empty」、時間の長さには「e.g. 30s, 5m, 2h, 1d」である。

サーバーからの**検証のエラー**は、入力欄の下に赤いテキストとして欄ごとに表示され、入力欄には赤い枠が付く。

**大きさとページ分割。** ダイアログはビューポートの高さ（余白を除く）を上限とし、フレックスの列として配置されるので、ヘッダーとフッターは留まったまま質問の領域がスクロールする —— 基底の `DialogContent` の `overflow-hidden` は、そうしないと質問の多いパッケージのあふれた部分を到達不能にしてしまう。質問は**1 ページ 5 問**でページ分割され、Previous/Next の操作が付き、最後のページではそれが Install のボタンに道を譲る。すべてのページはマウントされたまま（非表示のものは `display:none`）なので、制御されていないフォームの入力は打ち込まれた値を保ち、送信もされる。ページをアンマウントすると、その上の回答が黙って落ちてしまう。欄のエラーはそれを持つページへ飛ぶので、検証のエラーがページャの後ろに隠れることはない。ページャは既存の `datatable.next`/`previous` の文字列と数値のページカウンタを再利用するので、翻訳の鍵を増やさない。

`show_if` で宣言された**条件付きの質問**は、それを制御するチェックボックスにチェックが入るまで隠される（[条件付きの質問](#条件付きの質問show_if)を参照）。

**OAuth の質問**は、質問ごとの単一の状態 —— `idle`、`starting`、`waiting`、`connected`、`error` —— から描かれる。その状態はキャッシュされた回答から初期化されるのであって、「どこかにトークンが存在するか」からではない。以前のインストールでキャッシュされたトークンは、かつては何も起きていないうちからその欄を接続済みに見せ、再接続の失敗を通してもそう見せ続け、赤いエラーの上に緑の Connected のバッジを載せていた。今やトークンはただ一つの判断（Connect か Reconnect か）のために読まれるだけであり、それ以外では隠しの入力が送るものにすぎない。再接続の失敗はオペレーターの手元に既に持っていたトークンを残すが、失敗した試みが成功したと主張するものは何も無く、進行中の再接続が接続済みに見えることもなく、トークンを伴わない承認は、空の資格情報をインストールしてしまう黙った成功ではなくエラーになる。
### パッケージ情報のダイアログ

パッケージ情報のダイアログは note をラベル付きの一覧として表示する。note はその型に基づいて描かれる。URL の note は新しいタブで開くクリック可能なハイパーリンク（`target="_blank"`）、メールの note はユーザーのメールクライアントを開く `mailto:` のリンク、電話の note は `tel:` のリンクである。型の無い note はリンクの無い素のコードブロックとして描かれる。

### パッケージのマニフェスト API

`POST /packages/manifest`（認証が必要）はパッケージの生の YAML の定義を返す。リポジトリ、名前、バージョンを受け取る。ファイルの内容を `Content-Type: text/x-yaml; charset=utf-8` で返す。パッケージのファイルが存在しない場合は 404 を返す。

### パッケージの操作のドロップダウン

パッケージの一覧の UI では、各パッケージの行に `...` のドロップダウンメニューがある（平坦な表示でも、リポジトリごとにまとめた表示でも）。このドロップダウンには以下が含まれる:

- **Info**（インストール済みのパッケージのみ）-- 質問、回答、コンパイルされた note を示すパッケージ情報のダイアログを開く。
- **Manifest** -- コピーボタン付きで生の YAML のパッケージの定義を表示するダイアログを開く。
- **Version/Repository** -- バージョンとリポジトリ名を示す無効化された項目として表示される。
- **Uninstall**（インストール済みのパッケージのみ）-- アンインストールの確認のダイアログを起動する。

### 注目のパッケージ

各リポジトリはパッケージ名の JSON の配列を含む `featured.json` のファイルを持てる。これは `LoadFeatured` によって読み込まれ、`RepoPackageGroup` の中でパッケージの一覧と一緒に返される。平坦なパッケージの一覧の API は各項目に `featured` の真偽値を設定する。まとめられたパッケージの一覧の API は、検索の絞り込みでパッケージの一覧が減っても各グループの `Featured` の配列を保つ。

- `GET /packages`（認証が必要）-- 検索、並べ替え、ページネーション、および任意の `featured_only` と `installed_only` の絞り込みでパッケージを一覧する。
- `GET /packages/featured`（認証が必要）-- すべてのリポジトリにまたがって注目のパッケージを一覧する。
- `GET /packages/by-repo`（認証が必要）-- リポジトリごとにまとめてパッケージを一覧する。`search` と `featured_only` のクエリパラメータを受け取る。

#### 注目のパッケージの絞り込み

平坦なパッケージの一覧の API（`GET /packages`）とまとめられたパッケージの一覧の API（`GET /packages/by-repo`）は `featured_only` のクエリパラメータを受け取る。`"true"` に設定すると、注目と印の付いたパッケージだけが返される。この絞り込みは `installed_only` と交差する —— 両方を同時に有効にできる。UI では「Featured only」のチェックボックスがこの絞り込みを切り替える。注目の絞り込みの既定の状態は `true` である（初回の訪問では注目のパッケージだけが表示される）。絞り込みの設定（`pkg_group_by_repo`、`pkg_installed_only`、`pkg_featured_only`）は `localStorage` に永続化される。

### インストール済みのパッケージの絞り込み

平坦なパッケージの一覧の API（`GET /packages`）は `installed_only` のクエリパラメータを受け取る。`"true"` に設定すると、インストール済みのパッケージだけが返される。絞り込みは検索、並べ替え、ページネーションの前にサーバー側で適用されるので、ページ数とオフセットが正しくなる。UI では「Installed only」のチェックボックスがこの絞り込みを切り替え、ページネーションを最初のページへ戻す。

### パッケージのインストールとアンインストール

#### インストール API

`POST /packages/install`（管理者が必要）はパッケージをインストールする。リポジトリ、名前、バージョン、回答、および任意のフラグを受け取る:

- `reuse_volumes` -- 以前のアンインストール済みのバージョンのボリュームを再利用する。
- `import_from_version` -- 特定の以前のバージョンからボリュームを取り込む。
- `skip_response_reuse` -- 以前のインストールから回答を自動で埋めない。

インストールは、リポジトリのパッケージのファイルからインストール先のディレクトリへハードリンクを作り、回答を永続化し、クォータと任意の UID/GID を伴うボリュームを作り、アーカイブと git からボリュームを初期投入し（コンテナのランタイム専用）、ファイルテンプレートを適用し、systemd のユニットファイルを生成し、ネットワークの状態のファイルを書き、systemd のユニットをインストールして起動し、成功時に last の回答を消す。last の回答は、アンインストール時の復旧のためインストールの前に保存される。VM のパッケージでは、ユニットの生成の前に VM のディスクイメージがダウンロードされて raw 形式へ変換され（リモートの URL の場合）、ボリュームの初期投入（アーカイブ、git の clone）は飛ばされる。

#### アンインストール API

`POST /packages/uninstall`（管理者が必要）はパッケージをアンインストールする。リポジトリ、名前、バージョン、および任意のフラグを受け取る:

- `purge_volumes` -- 関連するすべてのボリュームを直ちに削除する。

purge しない場合、ボリュームは `installed/` の接頭辞から `uninstalled/` の接頭辞へ移される。ネットワークの状態のファイルは削除され、systemd のユニットは停止・無効化・アンインストールされる。

**依存関係の連鎖。** 親のパッケージをアンインストールすると、それが所有するすべての依存関係が再帰的にアンインストールされる。この連鎖は親について永続化された依存関係のレコード（`LoadDependencies`）を読み、各子を深さ優先で歩き、どの階層でも同じ参照を繰り返すので、入れ子になった孫の依存関係（`parent--dep--child--dep--grandchild`）も取り除かれる。各依存関係について、連鎖はその DNS のレコードを登録解除し、その systemd のユニット（サービス + NC + ソケット）をアンインストールし、そのネットワークの状態のファイルを削除し、`inst.Uninstall` を呼んでインストールのレコードを落とし、そして（`purge_volumes` が設定されていれば）そのボリュームを purge するか、`uninstalled/` の接頭辞へ移す。この連鎖は `uninstallDependencies`（`src/svc/systemcontroller/controller_install_dependencies.go`）に実装されており、親自身のアンインストールが完了したあとに走る。参照カウントは無い。各依存関係はちょうど一つの親に属する（そのインストールのレコードは `installed/<repo>/<parent--dep--key>/` にある）ので、二つの親の下にインストールされた共有の依存関係は独立した二つのレコードを持ち、片方の親をアンインストールしても自分の写しが取り除かれるだけである。

#### インストール済みのパッケージの情報

`POST /packages/installed/info`（認証が必要）は、インストール済みのパッケージについて質問、回答、コンパイルされた note、note の型を返す。

**管理者でない者は note だけを得て、それ以外は得ない。** この経路が `requireAuth` のままなのは、ダッシュボードがすべてのアカウントに対してインストール済みの各サービスの note を描くからである —— それが note の目的である。しかし `type: secret` の質問には生成された資格情報が、`type: oauth` の質問には提供元のトークンが答えとして入っているので、ログインできる誰にでも回答のマップを丸ごと返すのは、すべてのパッケージの資格情報を手渡すことになる。質問も差し控えられる。質問の `query` 自体は無害だが、伏せられた回答のマップと組にすると、何が隠されているのかを宣伝するだけであり、質問を描く唯一の画面は管理者専用のインストールのダイアログだからである。マップを落とすだけでは足りない —— note はその同じ回答からコンパイルされるので、`redactSecretsInNotes` が note の引用した secret や oauth の回答をマスクする。値で照合するので、それらを一切引用しない note は完全に手つかずのまま残る。六文字未満の回答は放っておかれる。二文字の secret は誰かが選んだ資格情報ではないし、その出現をすべてマスクすれば無関係な note のテキストをずたずたにしてしまう。

#### パッケージのバージョン

`POST /packages/versions`（認証が必要）は、名前で指定したパッケージの利用可能なバージョンを一覧する。

#### パッケージの質問

質問を取得するエンドポイントは二つある:

- `POST /packages/questions`（管理者が必要）-- パッケージ名で質問を取得する（最新のバージョン）。
- `POST /packages/questions/identity`（管理者が必要）-- リポジトリ、名前、バージョンで質問を取得する。

### タイムゾーンの扱い

UI はよく使われる IANA のタイムゾーン名の静的な写しを保持し、ブラウザの `Intl` の API を使ってクライアント側で UTC のオフセットを計算する `getTimezoneOffsetMinutes()` のユーティリティを備える。サーバーは status の ping の応答を通じて、ローカルのシステムの UTC のオフセットを分単位で公開する。

### インストールのプレビュー

- `POST /packages/install-preview`（認証が必要）-- パッケージをインストールしたら何が作られるかをプレビューする。リポジトリ、名前、バージョンを受け取る。リポジトリ、名前、バージョン、説明、イメージ、ボリューム、ポート、アップグレードの情報、ランタイムの種別、そしてそのパッケージが質問を持つかどうかを返す。VM のパッケージでは、プレビューには VM の設定（イメージの URL、人が読めるメモリ、CPU の数）も含まれる。

### パッケージの子

- `POST /packages/children`（認証が必要）-- 指定したリポジトリとパッケージ名について、子のパッケージ名を一覧する。

### アンインストール済みのボリュームの一覧

- `POST /packages/uninstalled-volumes`（認証が必要）-- 以前のアンインストールから残っているボリュームがパッケージにあるかを確認する。アンインストール済みのボリュームが存在するか、アンインストール済みのバージョンの一覧、インストール済みのバージョンの一覧を返す。

### インストール済みのパッケージの管理

- `GET /packages/installed`（認証が必要）-- インストール済みのすべてのパッケージを検索、並べ替え、ページネーション付きで一覧する。
- `POST /packages/responses`（管理者が必要）-- リポジトリ、名前、バージョンで指定したインストール済みのパッケージについて、保存された回答を取得する。
- `POST /packages/purge-volumes`（管理者が必要）-- インストール済みのパッケージのボリュームを恒久的に削除する。

### パッケージの有効化／無効化

- `POST /packages/disable`（管理者が必要）-- パッケージを無効にする。無効のフラグを立て、関連するすべての systemd のサービスを停止する。
- `POST /packages/enable`（管理者が必要）-- 無効にされたパッケージを再び有効にする。無効のフラグを消し、関連するすべての systemd のサービスを起動する。

`Installer` のインターフェイスは、中核の `Install`、`Uninstall`、`ListInstalled`、`GetResponses` のメソッドに加えて `SetDisabled`、`IsDisabled`、`IsPackageChanged` に対応する。

### アンインストール済みのボリュームの管理

- `POST /packages/purge-uninstalled-volumes`（管理者が必要）-- パッケージのアンインストール済みのボリュームをすべて恒久的に削除する。

## ストレージ

ストレージはクォータの強制を伴う btrfs のサブボリュームを使う。

### 関心の分離: ボリューム対オブジェクトストレージ

**ストレージ層はボリュームを管理する。オブジェクトストレージは gfeh が提供する。ストレージ層はオブジェクトストレージには一切関与しない —— 責任を負うのは gfeh である。**

`src/storage` は btrfs のサブボリュームを作り、サイズを変え、名前を変え、スナップショットを取り、削除し、ディスクの使用量を報告する。その職掌はそれで全部である。オブジェクト、バケット、キー、ファイルハンドル、コンテンツ識別子（CID）、ACL、共有、プロトコルのビューが何であるかを、決して知ってはならない。ストレージ層にとってサブボリュームとは、クォータの付いた不透明なバイトの置き場である。

gfeh（`gitea.com/town-os/gfeh`。`town-os-system--gfeh` として出荷される Rust のシステムサービス）は、その線より上のすべてを所有する。オブジェクトの名前空間、ファイルごとのメタデータと権限、階層的なユーザー／ACL のデータベース、共有、ファイルごとの HTTP での公開、外部サービスへのフェデレーション、そしてすべてのプロトコルのビュー（S3、IPFS、Google Drive、素の HTTP。SMB/CIFS は gfehd に存在するが [Town OS はそれを提供しない](#smb-のビューは無い)）である。gfeh がストレージ層を使うのは、自分の区画が置かれるサブボリュームを用意しサイズを変えるためだけであり、そのあとはバインドマウントされた部分木に対して自前で直接 I/O を行う。

どちらかの側を変えるときに尊重すべき帰結:

- `src/storage` や `/storage/*` の API に、オブジェクト、blob、キー／値、ファイル単位のエンドポイントを追加**しないこと**。個々のファイルを指す必要のある機能は gfeh に属する。既存の `upload-archive`/`download-archive` のエンドポイントはボリュームの初期投入のための tar の輸送手段であってオブジェクトの API ではなく、その方向に育ててはならない。
- `storage.Storage` や `storage.Controller` にユーザー、権限、プロトコルを教え**ないこと**。クォータが、ストレージ層の強制する唯一の方針である。
- gfeh の区画は予約された `gfeh/` のサブボリュームの接頭辞の下にある。それらは `/storage/*` の HTTP API を通してではなく、`storage.Storage` の `CreateFilesystem`/`ModifyFilesystem` を**プロセス内で**呼んで用意される。`createFilesystem` は送られてきた名前をすべて無条件に `user/<name>` へ書き換える（`controller_storage.go`）ので、その経路は他のどの接頭辞の下にもボリュームを作れない。したがって区画の用意には専用の `/gfeh/partitions/*` のハンドラが要る。これは同時に、予約された接頭辞の強制、クォータの方針、監査のログを、gfeh の側で重複させるのではなく一か所にまとめておくことにもなる。

- **gfeh は書かれた契約に依存しており、ここでの変更はそれを壊しうる。** gfeh のリポジトリの `TOWNOS_CONTRACT.md` は、gfeh が Town OS に頼っているすべての経路、挙動、不変条件を挙げている —— `user/` への書き換え、予約された接頭辞の規則、`/gfeh/partitions/*` のステータスコード、区別のつかない認証の失敗、そして空の `Account.Networks` のフェイルクローズドな意味 —— そして、それが検証された Town OS のリビジョンを固定している。gfeh は、root、systemd、podman、btrfs 無しでテストを走らせられるよう、その契約をエミュレートしている。

  **`src/storage`、`src/account`、システムコントローラの経路を変えるときは、gfeh のチェックアウトで `make check-townos-sync` を走らせ直すこと。** ずれたエミュレータは、gfeh に緑のテストスイートと壊れた配備を与える。エミュレータと契約の文書は一緒に揃えること。決して片方だけを直してはならない。

その統合の Town OS 側 —— 区画の経路、ネットワークごとのデーモン、admin のソケット、そして名前が DNS と ingress へ届く道筋 —— は[オブジェクトストレージ（gfeh）](#オブジェクトストレージgfeh)にある。

### ファイルシステムの操作

`Storage` のインターフェイスが提供するもの:

- **CreateFilesystem** -- 任意のクォータを伴う新しい btrfs のサブボリュームを作る。
- **ModifyFilesystem** -- ボリュームの名前とクォータのいずれか、または両方を変える。
- **RemoveFilesystem** -- ボリュームを削除する。
- **ListFilesystems** -- 接頭辞と状態（`user`、`installed`、`uninstalled`）による絞り込み、並べ替え、ページネーション、検索を伴ってボリュームを一覧する。btrfs のマウントが見つからないときは（エラーではなく）空の一覧を返す。
- **RenameFilesystem** -- ボリュームの名前を変える。
- **SnapshotFilesystem** -- btrfs のスナップショットを作る。
- **DiskUsage** -- ディスクの使用量の統計を報告する。

クォータは btrfs の qgroup の水準で強制される。クォータ 0 は無制限を意味する。

### ストレージ API

- `POST /storage/create`（認証が必要）-- 名前と任意のクォータで新しいユーザーのファイルシステムを作る。
- `POST /storage`（認証が必要）-- 接頭辞と状態による絞り込み、並べ替え、ページネーション、検索を伴ってファイルシステムを一覧する。
- `POST /storage/modify`（認証が必要）-- ボリュームの名前とクォータのいずれか、または両方を変更する。名前の変更が許されるのはユーザーのファイルシステムだけであり、パッケージのボリュームの名前は変えられない。
- `POST /storage/remove`（認証が必要）-- ユーザーのファイルシステムを削除する。
- `POST /storage/package-volumes`（認証が必要）-- パッケージごとにまとめてパッケージのボリュームを一覧する。アンインストール済みのボリュームを任意で含められる。
- `POST /storage/remove-package-volume`（管理者が必要）-- 内部名で特定のパッケージのボリュームを削除する。
- `POST /storage/remove-package-volume-group`（管理者が必要）-- ストレージの木の葉でないノードの削除ボタンの背後にある連鎖削除。`repo` と `name` は必須であり、`version` が空のときはそのパッケージのインストール済みのすべてのバージョンを対象とする。**対象のパッケージの依存関係の木にあるすべての systemd のユニットが、いずれかのサブボリュームを削除する前に停止される**ので、まだボリュームを開いたままの podman のコンテナが btrfs の削除と競合することはない。`include_uninstalled` は加えて、対応する `uninstalled/` の部分木も掃く（ボリュームの一覧を動かすのと同じ「Show uninstalled」の切り替えに配線されている）。
- `POST /storage/upload-archive`（管理者が必要）-- アーカイブをアップロードしてボリュームへ展開する。
- `POST /storage/download-archive`（管理者が必要）-- ボリュームを圧縮アーカイブとしてダウンロードする。

### ボリュームの名前空間

- **ユーザーのボリューム** -- ディスク上では `user/<name>`。`user/` の接頭辞は作成、削除、変更、一覧の各ハンドラによって透過的に前置され、API の応答では取り除かれるので、API の利用者には裸の名前だけが見える。`user` のルートのサブボリュームは起動時に reconcile が作る。
- **インストール済みのパッケージのボリューム** -- `installed/<repo>/<name>/<version>/<volname>`。
- **アンインストール済みのパッケージのボリューム** -- `uninstalled/<repo>/<name>/<version>/<volname>`。
- **アーカイブの保管** -- `archives/` の接頭辞（システムが管理する）。
- **VM のイメージ** -- `vm-images` のサブボリューム（システムが管理する）。キャッシュされた raw の VM のディスクイメージを保管する。
- **オブジェクトストレージの区画** -- `gfeh/<network>`。Town OS のネットワークごとに一つで、uid/gid 2000 が所有する。予約されている: `/storage/create` はこれを作れない（すべての名前を `user/<name>` へ書き換えるため）ので、[`/gfeh/partitions/*`](#プロトコル-1-区画のプロビジョニングgfehpartitions) を通じて用意される。

接頭辞のルートの名前（`installed`、`uninstalled`、`archives`、`pages`、`vm-images`、`user`、`gfeh`）はすべて予約されており、ユーザーが直接作成、変更、削除することはできない。アーカイブのアップロードとダウンロードは、内部の接頭辞を持たないサブボリューム名を `user/` を前置して解決する。

**接頭辞は、その後ろの名前がそこから登り出られない限り、境界ではない。** `filepath.Join` は `..` を畳むので、`user/` を前置するハンドラへ送られた `../gfeh/home` は `user/../gfeh/home` になり、別のネットワークのオブジェクトストレージの区画を指してしまう —— しかもそれは予約された名前の検査もすり抜ける。その検査は、遡上がまだ持っていない先頭の接頭辞で照合するからである。そこで `storage.ValidateFilesystemName`（先頭のスラッシュ無し、null バイト無し、空や `.`/`..` の要素無し、そして限定された文字集合）が `ModifyFilesystem` の**両方**の名前に適用される —— 名前の変更先だけを検証していたために、呼び出し側は他人のサブボリュームを自分の名前空間へ移せてしまっていた —— そして何も検証しておらず、しかも破壊的である `RemoveFilesystem` にも適用される。`/storage/*` のハンドラは送られてきた名前を `user/` を前置する**前に**検証する。それが、予約された名前の検査を読んだとおりの意味にしているものである。これらの経路は `requireAdmin` ではなく `requireAuth` なので、この機械の普通のアカウントなら誰でもここに到達できた。

**一覧**の接頭辞は意図的に対象外である。`nest/` は呼び出し側が `nest` の下のすべてを求めるやり方であり、それをファイルシステムのパスへ結合するものは何も無く（ストレージ層は自分のベースから一覧し、それを文字列の絞り込みとして使う）、`user/` は無条件に前置されるので、遡上する接頭辞は何かに到達するのではなく何にも一致しない。

### アーカイブの形式の検出

アーカイブの圧縮形式は、アップロードのストリームの先頭のマジックバイトを調べて検出される。最初の 6 バイトがバッファ付きのリーダーで先読みされ、既知の署名と照合される:

- **gzip** -- `0x1f 0x8b`
- **bzip2** -- `0x42 0x5a 0x68`（`BZh`）
- **xz** -- `0xfd 0x37 0x7a 0x58 0x5a 0x00`（`\xfd7zXZ\x00`）

認識できない署名は直ちに拒否される。ファイル名の拡張子も独立に検証され、形式が確認される。

### アーカイブのストリームの検証

形式の検出のあと、伸長されたストリームは `io.TeeReader` を使って tar のアーカイブとして検証される。tee の一方は Go の `archive/tar` のリーダーへ流れて tar のヘッダを検証し、もう一方は本物の `tar -xf` の展開のプロセスへ流れる。検証が不正な tar のストリームを検出した場合、展開は中断される。伸長は利用できる場合は並列の実装を使う。gzip には `pigz`、bzip2 には `lbzip2`、xz には `xz` である。

### アーカイブのアップロード

`POST /storage/upload-archive`（管理者が必要）はマルチパートのフォームを受け取る:

- `subvolume`（必須）-- 対象のサブボリュームのパス。
- `archive`（必須）-- アーカイブのファイル。対応する形式: `.tar`、`.tar.gz`/`.tgz`、`.tar.bz2`/`.tbz2`、`.tar.xz`/`.txz`。
- `subpath`（任意）-- ボリューム内の展開先の相対パス。必要に応じて作られる。
- `stop_service`（任意）-- 展開の前に停止し、完了後に再起動する systemd のユニット名。

アーカイブは一時ファイルを介さず直接ストリームされる。パスの遡上は展開後に検証される（シンボリックリンクの解決）。アップロードの最大サイズの既定は 1 GB（`max_archive_size` の設定）である。展開のタイムアウトの既定は 600 秒（`archive_unpack_timeout` の設定）である。

### アーカイブのダウンロード

`POST /storage/download-archive`（管理者が必要）は JSON の本体を受け取る:

- `subvolume`（必須）-- 元のサブボリュームのパス。
- `paths`（任意）-- サブボリューム内の含めたい特定のパスの配列。
- `stop_service`（任意）-- アーカイブ化の間に停止し、そのあと再起動する systemd のユニット名。
- `format`（任意）-- 圧縮の形式: `tar.gz`（既定）、`tar.bz2`、`tar.xz`。
- `filename`（任意）-- ダウンロードされるファイルの基となるカスタムの名前。サーバーはこの値を無害化し（パスの区切りと制御文字を取り除く）、二重にならないよう既存のアーカイブの拡張子を取り除き、選ばれた形式に応じた拡張子を付ける。渡されないとき、あるいは無害化の結果が空文字列になるときは `download` を既定とする。

要求された形式でストリームされたアーカイブを返す。圧縮にはそれぞれ `pigz`、`lbzip2`、`xz` を使う。Content-Type と Content-Disposition のファイル名のヘッダは、選ばれた形式とカスタムのファイル名に合わせて設定される。`paths` が渡された場合は、一致するパスだけが含まれる。

### コンテナイメージからの自動アーカイブ

パッケージの定義は、コンテナイメージを参照する `archives` のセクションを含められる。インストールと reconcile の際、空のボリュームは、イメージを pull し、一時的なコンテナを作り、指定されたディレクトリをボリュームへ複写することで埋められる。

### git によるボリュームの初期投入

ボリュームはリポジトリの URL を持つ `git` のフィールドを指定できる。インストールと reconcile の際、空のボリュームはそのリポジトリを clone して初期投入される（5 分のタイムアウト）。URL はテンプレート変数を参照できるので、ユーザーは質問の回答でリポジトリを上書きできる。既存のデータが上書きされることは決してない。clone の失敗はログに出て飛ばされる（非致命的）。

### git のソースの再ビルド

`POST /packages/rebuild-git`（管理者が必要）は、インストール済みのパッケージについて git で初期投入されたボリュームを更新する。各 git のボリュームについて go-git 経由で最新の変更を pull し、そのうえで依存するサービスを再起動する。パッケージのリポジトリ、名前、バージョンが必要である。テンプレート変数は再ビルドの前に、保存された回答に対して評価し直される。

### VM のイメージの管理

VM のパッケージは raw 形式のディスクイメージを必要とする。リモートのイメージはダウンロードされ `qemu-img convert -O raw` で変換される。変換された `.raw` のファイルは `vm-images` のサブボリュームにキャッシュされる。以後のインストールはキャッシュされたイメージを再利用する。ローカルのイメージの参照は `vm-images` のサブボリュームから直接解決される。

- `GET /vm-images`（認証が必要）-- キャッシュされた VM のディスクイメージを一覧する。各イメージについて名前とファイルサイズを返す。
- `POST /vm-images/upload`（管理者が必要）-- URL から VM のイメージをダウンロードし raw 形式へ変換する。URL と任意の名前を受け取る。名前は既定で URL のファイル名に `.raw` の拡張子を付けたものになる。ダウンロードには 30 分のタイムアウトがある。変換されたイメージは `vm-images` のサブボリュームに保管される。
- `POST /vm-images/delete`（管理者が必要）-- キャッシュされた VM のイメージを名前で削除する。

### 表示名の刈り込み

インストール済みおよびアンインストール済みのパッケージのボリュームについての API の応答は、パスの先頭のリポジトリの区間を取り除く（例: `default/nginx/2.0/data` は `nginx/2.0/data` になる）。ディスク上の完全なパスは、それを必要とする操作（例えばアーカイブの操作中の停止／起動のための systemd のサービス名の導出）のために `internal_name` のフィールドに保たれる。

### ストレージの UI

ストレージの管理の画面は二つの区画から成る:

**User Filesystems** -- ページ分割され、並べ替えでき、検索できるデータテーブル。各行に Modify（名前とクォータ）と Delete のボタンがある。作成のダイアログはクォータの欄をシステムの `default_quota` の設定から先に埋めておく。

**Package Volumes** -- パッケージごとに整理された階層的な木。各パッケージは折り畳める木の見出しであり、ボリュームの総数、バージョンの数、クォータの合計、インストールの状態のバッジを示す。パッケージが複数のバージョンを持つ場合は、バージョンごとのクォータと状態を伴うバージョンの小見出しが示される。「Show uninstalled volumes」の切り替えが有効なときはアンインストール済みのボリュームも含まれる。

葉となる各ボリュームの行はクォータと状態を表示し、三つの操作を提供する:

- **Download**（アイコンのボタン）-- 任意のファイル名の欄（ダウンロードされるファイルの基となる名前。アーカイブの拡張子は自動で付く）、圧縮形式の選択（gzip、bzip2、xz）、任意のカンマ区切りのパスの絞り込み、ダウンロード中に依存するサービスを停止するチェックボックスを備えたダイアログを開く。ストリームでの保存に File System Access API を使い、blob によるダウンロードへフォールバックする。
- **Upload**（アイコンのボタン）-- アーカイブのファイル（`.tar`、`.tar.gz`、`.tgz`、`.tar.bz2`、`.tbz2`、`.tar.xz`、`.txz`）を選ぶダイアログを開く。展開先の任意のサブパスと、アップロード中に依存するサービスを停止するチェックボックスを備える。
- **Modify**（ボタン）-- ボリュームの名前、状態、関連するサービス名を示し、クォータを変える欄を備えたダイアログを開く。パッケージのボリュームでは名前の欄は編集できない。
## Pages

Pages は静的サイトのホスティングの機能であり、三つの内容の出所の種別に対応する。アーカイブのアップロード、コンテナイメージ、git のリポジトリである。ユーザーはドメインまたはサブドメインを割り当て、システムは Caddy のコンテナを通じてその内容を提供する。更新は再ビルドまたは再アップロードによって手動で起動する。

### データの模型

各 page のサイトが持つもの: 一意な名前（主キー）、出所の種別（`archive`、`container_image`、`git`。既定は `archive`）、リポジトリの URL（git では必須）、ブランチ（既定は `main`）、コンテナイメージの参照（container_image では必須）、イメージのディレクトリ（container_image では必須）、ドメイン（既定は page の名前）、状態（`pending`、`active`、`error`）、**ネットワーク**、作成／更新の時刻である。page は SQLite のテーブルに保存される。

`Network` はその page を公開するネットワークであり、パッケージのインストールのネットワークとまったく同じである。page のホスト名、リーフの SAN、DANE の TLSA の所有者、ingress の vhost がすべてその下で名付けられる TLD を選び、誰がその page を解決できるかを決める。空 —— ゼロ値であり DB の既定値でもある —— は既定／home のネットワークを意味し、パッケージにおける `Installer.LoadNetwork` と同じ約束事である。[page もネットワークにスコープされる](#page-もネットワークにスコープされる)を参照。作成時に受け取られ、部分更新のフィールドの一つでもある。

page の内容は `pages/` の接頭辞の下の btrfs のサブボリュームに保管される。各 page は `pages/{name}` にサブボリュームを、`pages-webroot/{name}` に `/data/pages/{name}` を指すシンボリックリンクを得る。`pages` の接頭辞は予約されており、一般のストレージの API では名前の変更も削除もできない。

### Pages API

変更のエンドポイントはすべて管理者の認証を必要とする。一覧のエンドポイントは通常の認証を必要とする。

- `POST /pages/create`（管理者が必要）-- 新しい page を作る。名前、出所の種別、リポジトリの URL、ブランチ、ドメイン、コンテナイメージ、イメージのディレクトリを受け取る。出所の種別は既定で `archive` である。検証は出所の種別によって異なる。git はリポジトリの URL を必要とし、コンテナイメージはイメージとイメージのディレクトリの両方を必要とする。btrfs のサブボリュームと webroot のシンボリックリンクを作る。git とコンテナイメージの page は非同期に用意される（clone またはイメージの展開）。状態は成功時に `pending` から `active` へ、失敗時に `error` へ遷移する。アーカイブの page は `/pages/upload` で内容がアップロードされるまで `pending` の状態のままである。ドメインは渡されなければ page の名前を既定とする。
- `POST /pages/upload`（管理者が必要）-- アーカイブ種別の page の内容をアップロードする。`name` と `archive` のファイルを含むマルチパートのフォームを受け取る。出所の種別が `archive` の page でのみ有効であり、他の種別には 400 を返す。ストレージのアーカイブのアップロードと同じマジックバイトによる形式の検出、拡張子の検証、ストリームの検証を使う。page の btrfs のサブボリュームへ直接展開する。成功時に状態を `active` に、失敗時に `error` に設定する。
- `POST /pages/update`（管理者が必要）-- page のリポジトリの URL、ブランチ、ドメイン、出所の種別、コンテナイメージ、イメージのディレクトリの部分更新。渡されたフィールドだけが変わる。
- `POST /pages/remove`（管理者が必要）-- データベースから page を削除し、webroot のシンボリックリンクを取り除き、btrfs のサブボリュームを削除する。
- `POST /pages/rebuild`（管理者が必要）-- 挙動は出所の種別によって異なる。git の page は最新の変更を pull する（`.git` が欠けていれば新たに clone する）。コンテナイメージの page は podman 経由でイメージから展開し直す。アーカイブの page は 400 を返す（代わりに `/pages/upload` で再アップロードする）。
- `GET /pages`（認証が必要）-- すべての page を並べ替え、検索、ページネーション付きで一覧する。名前、リポジトリの URL、ブランチ、ドメイン、出所の種別、状態、時刻で並べ替えできる。

### Pages の UI

page の管理の画面は、名前、ドメイン、出所の種別、リポジトリの URL、ブランチ、状態の列を持つ、ページ分割され並べ替えでき検索できるデータテーブルを表示する。出所の種別はバッジとして示される。状態は色分けされたバッジとして示される（active には既定、error には赤、pending には回転する読み込みのアイコンと「Provisioning...」のテキストを伴う二次色）。

作成のダイアログは上部に出所の種別のドロップダウンを持つ（Archive Upload / Container Image / Git Repository。既定は Archive Upload）。欄は選ばれた出所の種別に応じて動的に変わる。git はリポジトリの URL とブランチを示し、コンテナイメージはイメージの参照とイメージのディレクトリを示し、アーカイブは任意のファイルのアップロードの入力を示す。git とコンテナイメージの page では、フォームの送信が用意を起動する。すべての入力は無効になり、送信ボタンは「Provisioning...」のテキストとともにスピナーを示し、ダイアログは閉じられなくなる。UI は最長 60 秒まで、2 秒ごとに page の状態をポーリングする。ファイルが選ばれたアーカイブの page では、アップロードは作成のあと同期的に行われる。

行ごとの操作は出所の種別によって異なる。アーカイブの page は Upload のボタンを示し、git とコンテナイメージの page は Rebuild のボタンを（確認付きで）示す。すべての page は Edit と Delete の操作を持つ。編集のダイアログはその page の出所の種別に応じた欄を示す。

## オブジェクトストレージ（gfeh）

gfeh は[関心の分離](#関心の分離-ボリューム対オブジェクトストレージ)で述べた分割のオブジェクトストレージ側の半分である。`src/storage` は btrfs のサブボリュームとクォータを所有し、gfeh はオブジェクト、ファイルごとの権限、ユーザー／ACL の森、共有、そしてすべてのプロトコルのビューを所有する。この節はその境界の Town OS 側 —— デーモンがどう配備されるか、そしてそれを越えるすべてのプロトコル —— である。

`gfehd` は crates.io に公開されている Rust のバイナリであり、ここでは `quay.io/town/gfeh`（`Containerfile.gfeh`）として梱包される。gfeh 自身のリポジトリはイメージを出荷していないからである。これは単一のマルチテナントのデーモンではなく、**区画ごとに一プロセス**である。

### 配備の形: ネットワークごとに一区画

**区画**とは、一つの btrfs のサブボリューム、一つの `gfehd` のプロセス、一つの admin のソケット、そして**それ自身のユーザーの集合**である。Town OS のネットワークごとにちょうど一つあるので、オブジェクトストレージの名前空間は、DNS と WireGuard を区切るのと同じ境界で区切られる。`office` の区画における主体、権限付与、公開は、`home` では何の意味も持たない。

| もの | 場所 |
|---|---|
| 区画のデータ | `<btrfsBase>/gfeh/<network>` → コンテナの `/data/<network>` |
| 設定 | `<btrfsBase>/gfeh-control/<network>/gfehd.yaml` → `/etc/gfeh/gfehd.yaml` |
| admin のソケット | `<btrfsBase>/gfeh-control/<network>/run/admin.sock` → `/run/gfeh/admin.sock` |
| ユニット | `town-os-system--gfeh-<network>.service` |

パスの補助関数は `src/gfeh/layout.go` にある —— `PartitionVolume`、`ConfigPath`、`SocketPath`、`ServiceKey`、`NetworkFromKey` —— これらの文字列が組み立てられるのはそこだけである。

ソケットが btrfs の上にあるのは、それが gfehd のコンテナと systemcontroller のコンテナの両方から見える唯一のファイルシステムだからである。これは `ingressctl` が自分の gRPC のソケットに使っているのと同じ手である。gfehd は **uid/gid 2000**（`gfeh.UID`/`gfeh.GID`）として動き、バインドマウントはホストの所有権をそのまま通すので、区画のサブボリュームは作成時にその uid へ chown される —— そのために `storage.Filesystem` は任意の `UID`/`GID` を持ち、`storage.Controller` は `Chown` を持つ。非再帰であるのは `HostVolumeMount` の chown と同じ理由による。デーモンは自分の子を自分の uid で作るので、既に正しく所有されており、ずれることは決してない。

**ポート。** 四つの HTTP のビューは**どの区画でも固定の同じコンテナのポート**にバインドする —— s3 は 9000、http は 9001、drive は 9002、ipfs は 9003 —— そして**ホストのポートは公開しない**。それが安全なのは、まさに何も公開しないからである。各コンテナは自分のネットワーク名前空間を持ち、ingress はパッケージに到達するのとまったく同じようにコンテナ名で到達する。二つの区画が両方とも 9000 で S3 を提供していても衝突しえない。`make test-full` を同時に走らせている場合も含めてである。

**どの区画もホストのポートを一切公開しない。** それを必要とする唯一のビュー —— HTTP でもなく ingress の後ろに座ることもできない SMB —— は[提供されない](#smb-のビューは無い)からである。`DefaultSMBPortBase`（`4450`）と `GFEH_SMB_PORT_BASE` は使われないまま残っているので、いつかそのビューが戻ってきてもハーネスの設定は無害なままである。

### プロトコル 1: 区画のプロビジョニング（`/gfeh/partitions/*`）

この四つの経路が存在するのは、`createFilesystem` が送られてきた名前をすべて無条件に `user/<name>` へ書き換えるので、`/storage/create` では `gfeh/` の接頭辞の下にボリュームを作れ**ない**からである。これらは `TOWNOS_CONTRACT.md` で宣言されており、gfeh の Rust のクライアントはこの形をそのまま解析するので、**ここでの変更はリファクタではなく契約の変更である**。ずれを捕まえるのは gfeh のチェックアウトの `make check-townos-sync` であり、こちら側では `controller_gfeh_partitions_test.go` が通信の形を固定している。

| 経路 | 認証 | 要求 | 応答 |
|---|---|---|---|
| `POST /gfeh/partitions/create` | 管理者 | `{name, quota}`。名前は接頭辞**無し** | `Filesystem` `{name:"gfeh/<n>", quota}` |
| `POST /gfeh/partitions/modify` | 管理者 | `{name, quota}` | `Filesystem` |
| `POST /gfeh/partitions/remove` | 管理者 | `{name}` | 200、空 |
| `POST /gfeh/partitions` | 認証 | 本体無し | `Filesystem` の**素の JSON の配列** |

二つの細部が荷を負っている:

- **一覧は `PageResult` ではなく裸の配列を返す。** Town OS の他のすべての一覧のエンドポイントはページ分割するが、これはできない。gfeh の `list_partitions()` が `Vec<Filesystem>` を直接デシリアライズするので、ページ分割の封筒は Rust 側で復号に失敗するからである。
- **接頭辞は非対称である。** 要求は裸の名前を運び、応答は `gfeh/<name>` を運ぶ。この接頭辞は Town OS の名前空間の産物であって区画の同一性の一部ではない。gfeh の `Partition::from_volume` が戻ってきたときにそれを取り除く。

gfeh のクライアントが分岐するステータスコード: **409** は既に存在する（その用意は「作るか、さもなくばサイズを変える」であり、この状態でその二つを区別する —— そうしないと、最初の一回を除くすべての起動で区画が存在するデーモンは、一度しか起動できないことになる）、**404** は存在しない、**400** は不正な名前、**403** は管理者でない。パスの区切りを含む名前はこの境界で拒否される。gfehd も自分の側で拒否するからである。合法な区画名が何かについて食い違えば、`../user/something` がオブジェクトストレージのルートの外のボリュームを指せてしまう。

ハンドラは `/storage/*` ではなくプロセス内で `storage.Storage` を呼ぶので、予約された接頭辞の強制、クォータの方針、監査のログは一か所にとどまる。これらの経路は `grantRoutes` に**入っていない** —— 権限の木の根を用意することは権限付与で買えるものではないので、権限付与を持つアカウントは、どのハンドラが走るより前に全体の許可リストによって拒まれる。

### プロトコル 2: admin のソケット（`/v1/*`）

各デーモンの管理の面は、その **Unix のソケットの上でだけ**の JSON over HTTP であり、決してポートではない。トークンも認証も無い。ソケットのファイルシステムの権限がアクセス制御であり、そこへ到達できるということは既にこの機械の root であるということだからである。Go 側は `src/gfeh/client.go`（`UnixClient`）であり、`DialContext` をソケットへ固定し、`http://gfeh` という偽の権威を使う。

| 呼び出し | メソッドとパス | 目的 |
|---|---|---|
| `Health` | `GET /v1/health` | 生存確認。準備完了の探査でもある |
| `Names` | `GET /v1/names` | この区画が公開してほしい名前 |
| `ListPrincipals` / `CreatePrincipal` / `DeletePrincipal` | `GET`/`POST` `/v1/principals`、`DELETE /v1/principals/<name>` | 区画のユーザーの森 |
| `ListGrants` / `CreateGrant` / `RevokeGrant` | `GET /v1/grants?principal=`、`POST /v1/grants`、`DELETE /v1/grants/<id>` | ACL |
| `ListExposures` / `WithdrawExposure` | `GET /v1/exposures`、`DELETE /v1/exposures/<token>` | 公開された `/f/<token>` のリンク |

gfehd は内部のエラーを HTTP のステータス（404/409/400）へ対応付け、`StatusError.Unwrap` がそれを Go の番人の値へ戻すので `errors.Is` が働く。

ユーザーの追加は `POST /v1/principals {name, parent, ceiling}` である —— **パスワードは無い**。だから UI は一度もそれを尋ねない。ceiling は gfeh の射影の規則に従う。Town OS の管理者には `all`、それ以外には読み／書きである。権限付与は gfehd によってその主体の ceiling へ切り詰められるので、UI は送ったものではなく*返ってきた*権限を表示する。管理者は、権限付与が狭められたことを見られなければならない。

### プロトコル 3: 名前 —— gfeh が答え、Town OS が組み立てる

**gfeh が DNS のレコードや ingress の経路を登録することは決してない。** `RebuildDNS` は `TeardownTLD` を呼び、`RebuildIngress` は導出した全集合とともに `SetRoutes` を呼ぶ —— どちらも他所の状態を破壊する —— ので、gfeh が直接登録したものは次の reconcile までしか生き延びない。代わりに `GET /v1/names` がビューとポートを伴う**ラベル**（`s3.<partition>`）を返し、Town OS がゾーンを組み立てる。したがって名前は一度押し込まれるのではなく、再構築のたびに*尋ねられる*。

`gfehFQDN(label, tld)`（`gfeh_tls.go`）はラベルをそのネットワークの TLD の下で修飾するものであり、A レコード、リーフの SAN、DANE の TLSA の所有者、ingress の vhost がすべて一致していなければならない唯一の文字列である —— `packageFQDN` と `pageFQDN` が保つために存在するのと同じ不変条件である。これは**常に**修飾する。`isPublicFQDN` を参照しない。gfeh のラベルはどれも既にドットを含んでおり（`s3.gfeh`）、その述語はそういう名前をすべて公開の FQDN と読むので、そうするとすべての名前が修飾されないまま残り、誰も所有していないドメインについて ACME の証明書を要求してしまうからである。

**ここはまた、ラベルが通信上の文字列であることをやめて vhost、DNS のレコード、ファイルシステムのパスになる関門でもある**ので、`gfeh.ValidateLabel` はそこで、そしてそこだけで適用される。ingress の vhost は引用無しに `https://<hostname> {` として書かれるので、改行と波括弧を持つラベルはそのブロックを閉じて別のものを開いてしまう —— しかも Caddy は不正な vhost 一つを拒否するのではなく、設定全体を拒み、この機械のすべての名前を落とす。検証を通らないラベルは空文字列を返し、どの呼び出し側も既に空の FQDN を捨てるので、不正な形の名前は壊れたレコードや経路や証明書やディレクトリではなく、何も生まない。長さ（`gfeh.NameMaxLen`）はラベル単独ではなく**組み立てられた**名前について検査される。上限に収まるラベルでも、長い TLD の下で修飾すると超えうるし、DNS が運ばない名前は証明書や vhost も主張すべきでないからである。

公開はパッケージや page とまったく同じである:

- **デュアルホームの DNS** —— 既定でないネットワークの区画は、`RebuildDNS` と `RebuildNetworkDNS` の畳み込みを通じて、この機械のオーバーレイの IP を指すスコープ付きの A レコード（そのネットワークの WireGuard の peer に提供される）*と*、LAN の IP を指すグローバルな A レコードを得る。DANE の TLSA は両方の半分に固定される。
- **TLS** —— 名前ごとにローカルの CA のリーフ。peer が生の WireGuard のアドレスで接続できるよう、そのネットワークにおけるこの機械のオーバーレイの IP を SAN として運ぶ。
- **Ingress** —— HTTP のビューごとに一つの vhost。バックエンドは共有の `town-os-ingress` の podman のネットワーク上の `<container>:<port>` である。`dedupeIngressRoutes` が経路の集合を先勝ちで守る。Caddy は重複した vhost 一つで設定全体を拒否するからである。

`IsHTTPView` がその最後の手順の関門であり、**未知の**ビューは HTTP でないものとして扱われる。HTTP を話さないものへの vhost は TLS のハンドシェイクを受け入れてから失敗するので、経路が無いことよりも悪い。（HTTP でないビューは DNS のレコードを生み ingress の経路は生まないだろう。今日、提供される四つのビューはすべて HTTP である。）

### 区画のインデックス

gfeh が提供するどのビューも**プロトコル**に答えるのであって、ブラウザに答えるものは一つも無い。HTTP のビューは経路をちょうど一つ、`/f/{token}` しか持たないのでそのルートは 404 であり、S3 は操作として解析できないものすべてに XML のエラーを返し、Drive と IPFS はそれぞれ自分の API に答える。だから、新しい名前に対して誰もがする唯一のこと —— それを開くこと —— が、オブジェクトストレージは壊れていると告げていた。実際には、そもそも見に行く場所が無かったのである。

各区画は **`gfeh.<tld>`** に静的なインデックスを公開する —— `gfeh.IndexLabel` であり、これは `"gfeh"` という文字列を二度目に書いたものではなく `VolumePrefix` である。インデックスは、それが索引するビューのラベルの親に着地しなければならないからである。新しく覚える名前は無い。ビューは既に `s3.gfeh`、`http.gfeh`、`drive.gfeh`、`ipfs.gfeh` である。

- **これは `collectGfehSites` によって普通の `GfehSite` として寄与される**。それこそが要点である。A と AAAA のレコード、スコープ付きのオーバーレイのレコード、DANE のピン、リーフの SAN、ingress の経路を、ビューについてその六つすべてを導くのと同じコードから継承するので、vhost と証明書が別々の文字列から組み立てられることはありえない。これが加えられるのは、その区画に ingress が前に立つビューが少なくとも一つあるときだけである。閲覧できるものを何も提供していない区画のインデックスは、見るものが何も無いと告げるページを描くためだけの名前と証明書と経路になってしまう。
- **これは gfehd ではなく pages のコンテナが提供する。** 静的な HTML に自前のサーバーは要らないし、Caddy の `respond` の本体としてインラインで出せば、生成されたマークアップが設定ファイルの中に入ってしまい、そこでエスケープを一つ間違えれば Caddy はすべてを拒否する。
- **内容は自分自身の `gfeh-index/` のルートの下にある**。`gfeh-control/` がそうであるのと同じ理由で `gfeh/` の兄弟である。`pages/` の下にあるものはすべて page であり、行に所有され、pages の reconcile に掃かれる。二つが共有する唯一のものは webroot である。それがコンテナの提供元だからである。`ViewIndex` は意図的に `HTTPViews` に**入っていない**ので `IsHTTPView` はそれを受け付けない —— その述語は「これは gfehd が報告した、ingress が前に立てるビューか」に答えるものであり、インデックスは gfehd に報告されるものでも、gfehd が提供するものでもない。
- **`pruneStalePageSymlinks` は `gfehIndexHostnames` を畳み込む。** インデックスは page ではないので、これが無いと最初の `reconcilePages` がすべてのインデックスのリンクを削除してしまう —— そしてオブジェクトストレージがあって page が無い機械は、毎回そのもっとも激しい場合に当たる。有効な集合は**ネットワークの集合だけ**から導かれ、決してデーモンに尋ねることはないので、単に起動が遅いだけの区画が自分のインデックスを刈られることはありえない。削除してよいものは、Town OS が所有する状態から決定できなければならない。
- **インデックスは `ReconcileGfeh` からではなく `RebuildIngress` から `reconcileGfehIndexes` によって描かれる。** その配置は荷を負っている。ingress の再構築は起動時、毎時の reconcile、パッケージと page の CRUD、そして決定的に `publishGfehNames` で走る —— それが、冷たい起動においてどのデーモンでも応答している最初のパスである。gfehd は `/status/ping` をポーリングするが、それはハンドラの差し替えまで 503 だからである。gfeh の reconcile から書かれたインデックスは、デーモンが何を提供しているか言えるようになる前に書かれることになり、次の一時間まで古いまま座り続ける。

インデックスが運ぶのは**ビューだけ**であり、それらは既に DNS にある。主体、権限付与、クォータは運ばず、公開されたリンクも運ばない —— これはオブジェクトストレージとは何であるかを説明するために運用者が配って回るページであり、そこに載る `/f/<token>` は、そこへ公開してくれと誰も頼んでいない持参人の資格情報である。公開されたリンクには、下にある自分自身のページがある。

### 公開ファイルのインデックス

`http.gfeh.<tld>` は、その存在意義のすべてが人にファイルを渡すことにある唯一の名前でありながら、404 を返す唯一の名前でもあった。gfehd は `/f/<token>` に答え、自分自身のルートを含めて他の何にも答えないからである。そのルートは今、**公開ファイルのインデックス**を提供する —— 有効な公開のすべてを、名前を付け、それが提供されるトークンへリンクして並べたものである。

これは機械の上で**二つのバックエンド**を持つ唯一の名前であり、ingress の `Route` の `PathBackend` はそのために存在する（[Ingress](#ingress) を見よ）。

- **`/` は pages のコンテナが扱う。** `gfeh-index/http.gfeh.<tld>/` から `gfeh.RenderPublicIndex` を描き出し、区画のインデックスとまったく同じディレクトリの検査、変更時のみの書き込み、シンボリックリンク、刈り取りを通る（`writeGfehIndexFile`）。書き手が二つあれば、インデックスがどこに住むのか、そして刈り取りがどの名前を消してよいのかについて、意見が二つあることになる。
- **他のすべての経路は gfehd のままである。** 一致するのはルートだけであり、それ以上ではない。前方一致にすれば gfehd が後から生やすかもしれない経路を覆い隠すことになり、それはこのページが直す失敗をそのまま裏返したものである。ルートは、gfehd が 404 を返すことによって自分は提供しないと言った、ただ一つの経路である。
- **経路ごとのバックエンドがプログラムされるのは、バイト列が存在する名前についてだけである。** `reconcileGfehPublicIndexes` は自分が書いたものを返し、`gfehPublicIndexBackends` はそれらだけを経路に載せる。webroot の項目が無い名前について `/` を pages のコンテナへ送れば、gfehd の 404 が Caddy の 404 に置き換わるだけであり、同じ失敗にロゴが違うだけのものになる。
- **`gfehIndexHostnames` はこの名前も覆う。** さもなければ pages の刈り取りが最初のパスでその webroot の項目を削除し、次に ingress が再構築されるまでインデックスは 404 になる。
- **到達できないデーモンは自分のページを保つ。** `ListExposures` が失敗したとき、前回の描き出しは「まだ何も公開されていない」へ書き換えられるのではなく、そのまま残される。短い停止は誰かがすべてのファイルを取り下げた証拠ではないし、そう扱う reconcile は再起動のたびにインデックスを空にし、一つ後のパスで埋め直すことになる。

**これは持参人の資格情報を意図的に列挙する。** `/f/<token>` の URL は capability であり —— それを持っていることが認可のすべてである —— ファイルを公開することはもはや「リンクを送った相手が到達できる」ではなく「その名前を解決できる者すべてに一覧される」を意味する。それがこのページという取引である。公開されたファイルの一覧は持つ値打ちがあり、リンクを省いた一覧は誰も開けないファイル名の並びにすぎない。そこから従うのは、**ファイルを再び私的にする唯一の手立ては取り下げである**ということである。無効にされた公開は行を寄与しない。行は、どこにも解決しないリンクを宣伝することになるからである。

### プロトコル 4: UI のプロキシ（`/gfeh/*`）

admin のソケットは認証されておらず、ネットワークからも到達できないので、Town OS がそれをプロキシする。これらは意図的に**四つの契約の経路とは別**にしてあるので、`check-townos-sync` は契約が宣言するものとちょうど一致し続ける。

| 経路 | 認証 |
|---|---|
| `GET /gfeh` | 認証 —— ネットワーク、TLD、クォータ、ユニットの状態、`/v1/names` の出力を伴う区画 |
| `GET /gfeh/principals?network=` | 認証 |
| `POST /gfeh/principals/add` / `remove` | `requireObjectStorage`（管理者、または `gfeh` の権限付与） |
| `GET /gfeh/grants?network=&principal=` | 認証 |
| `POST /gfeh/grants/add` / `revoke` | `requireObjectStorage` |
| `GET /gfeh/exposures?network=` | 認証 |
| `POST /gfeh/exposures/withdraw` | `requireObjectStorage` |

四つの `GET` は監査から除外されており、五つの変更操作は監査の鍵を持つ。区画が何も設定されていないとき、`GET /gfeh` はエラーになるのではなくオブジェクトストレージが設定されていないと報告する。

**これらのすべて —— 読み取りも含めて —— は `requireNetworkScope` によって呼び出し側のネットワークに閉じ込められる。** 「どのネットワークか」は、ハンドラだけが解析した本体やクエリの中にあるからである。スコープの付いたアカウントが別のネットワークの主体や公開されたリンクを一覧できることは、まさにこのスコープが防ぐために存在する漏れであり、読み取りは `requireAuth` なので上流の何もそれを止めなかったはずである。`GET /gfeh` はネットワークを名指ししない（それらを列挙する）ので、代わりに行を絞り込む —— 同じ `Restricted()` の述語による。素のアカウントをその空のスコープに対して絞り込めば、誰かを閉じ込めるどころか、すべての区画をすべての普通のアカウントから見えなくしてしまうからである。

**`gfehClientFor` の中の順序は荷を負っている: 形、次に権限、次に存在である。** 空のネットワークは誰に対しても 400 であり（打ち間違いは権限の問題ではない）、スコープ外のネットワークはいかなる区画の参照よりも*前に* 403 であり、そのあとで初めて、無い登録簿が 503 を、未知のネットワークが 404 を受け取る。参照が先だと、尋ねる筋合いの無かった呼び出し側が、その区画が存在するか、そのデーモンが上がっているかを知ってしまい、しかもそれを別種の*成功した*拒否として受け取ることになる —— そしてスコープの付いたアカウントが自分のスコープの外へ手を伸ばしたことを、何も記録しなかった。

### サービスアカウントは存在しない

以前のリリースは `gfeh` という専用の管理者アカウントを作り、そのパスワードを `gfeh_service_password` の設定に保管して、デーモンがコントロールプレーンへ認証できるようにしていた。**それはもう無い。** Town OS はデーモンが起動する前に各区画のサブボリュームとクォータを自分で用意し、主体は admin のソケット越しに作るので、その資格情報は何も買っていなかった —— その一方で、*誰も作っていない有効な管理者アカウント*という代償を払っていた。それはすべての機械のユーザー一覧に座り、すべてをアンインストールできるだけの権限を持ち、「この機械には管理者がいるか」というあらゆる問いを「*人間の*管理者が」という意味にしてしまっていた。

`hasEnabledAdmin`（`src/svc/systemcontroller/admin_presence.go`）が今ではその素直な問いであり、`/status/ping` の設定済みのフラグと `POST /account/create` のブートストラップの分岐が共有するので、その二つが食い違うことはありえない —— 一方が「設定済み」と言い、もう一方がそう言わない機械は、誰も入れない機械である。

`account.PurgeLegacyServiceAccounts` は、アップグレード後の最初の起動でその行と保管されたパスワードを削除し、何かを取り除いたかどうかを報告するので、機械は毎回の起動でログを出すのではなく一度だけそう言う。これは意図的に生の SQL である。`Manager` に `Delete` は無く、アカウントを削除する能力を、片付けの副作用として導入すべきではない。

`gfehd.yaml` に残るのは `credentials:` と `drive.tokens:` である —— **エンドユーザーが gfeh のビューへ認証するためのもの**であり、決して Town OS のログインではない。`town_os:` のブロックは設定のスキーマに今も存在する（gfehd の YAML はそのまま鏡写しにしてある）が、Town OS はそこにアカウントを描き出さない。

### SMB のビューは無い

SMB は**提供されない**。それは ingress の後ろに座れない唯一のビューであり、自前の資格情報 —— NT ハッシュ（`MD4(UTF16LE(password))`）—— を必要とする唯一のビューでもある。それは保管されたパスワードのハッシュから導けないので、共有を欲しがるユーザーは全員が二つ目のパスワードを持たなければならなかった。Town OS のアカウントはそれを持たないので、gfehd が認証できる相手は誰もいない —— そして LAN 上の認証されない共有は、取るべきフォールバックではない。

その帰結: どの区画も `smb:` のブロックを宣言せず、そのためのホストのポートも割り当てられず（`SMBPortBase` はハーネスの `GFEH_SMB_PORT_BASE` の配線を残すためだけに保たれている）、`Account.SMBNTHash` と `src/account/smb_credential.go` は無くなり、`smb_nt_hash` の列は `migrateLegacyAccountColumns` によって破棄される —— NT ハッシュはソルト無し、ワークファクター無しで、いまだ NTLM を話すものにとってはパスワードそのものと等価なので、誰も提供しないビューのためにそれを保存したままにしておくのは、両方の意味で最悪である。他の四つのビューには影響しない。

### 設定ファイル

`src/gfeh/config.go` は gfehd の YAML を**そのまま**鏡写しにする。gfehd の設定の構造体はどれも `#[serde(deny_unknown_fields)]` なので、余計なキーは無視されるのではなく、起動時の致命的な失敗になる。トップレベル: `data_dir`、`partition`、`network`（**ポインタ**である。無いことは既定の区画を意味し、空文字列はそれとは異なる不正な要求である）、`admin_socket`、五つの任意のビューのブロック、`credentials`、`town_os` である。Town OS は五つのうち四つのビューを描き出し、`smb:` のブロックも `town_os:` のアカウントも描き出さない。デーモンは uid 2000 として動きこれを読まなければならないので、`<btrfsBase>/gfeh-control/<network>/` の下に `0640` で、gfeh の gid がグループとして読める形で書かれる。

### 起動と reconcile

`ReconcileGfeh` は起動時、**ingress と pages の後**、そして **`Reconcile` の前**に走る —— その頃には TLS の CA とストレージが存在しており、名前はさらに下の `RebuildDNS`/`RebuildIngress` の呼び出しから利用できなければならない。これは **`ReconcileNetworks` の後に二度目も**走る。これは冪等であり（変わっていない区画は跳ね上げられずそのまま放っておかれる）、その reconcile が生み出したネットワークを拾う。`/networks/create`、`/networks/remove`、`/networks/enable`、`/networks/disable` からも呼ばれるので、実行時に追加されたネットワークも区画を得る。全体を通じて非致命的である。

ネットワークごとに、サブボリューム（UID/GID 付き）を用意し、設定を描き出し、**描き出された内容が変わったときにのみ**ユニットをインストールして再起動する（reconcile が既に使っている `ReadUnit` の差分の定石である）。`pruneGfehPartitions` は、もう存在しないネットワークのユニットを取り除く。

**区画ごとの待ちは無くなっており、その不在が荷を負っている。** `reconcileGfehPartition` はユニットを起動してそこで止まる。デーモンが応答しているかどうかは別に、`GfehReadyNetworks` と名前の収集器によって尋ねられる。どちらも、黙っている区画を失敗としてではなく「何も寄与しないもの」として既に扱っている。かつてこの待ちはループの中に、区画ごとに一度あった —— しかも何もしない区画についても、である。`ensureFirstUserPrincipal` は home 以外のどのネットワークでも最初の行で戻るからだ。期限のある context の上では、それは単に遅いというだけでは済まなかった。決して応答しない最初のデーモンが残りの予算を丸ごと `WaitForReady` で使い切るので、それより後のすべての区画は期限切れの context で `Start` を試み、`pruneGfehPartitions` はまったく走らなかった。死んだデーモンが一つで、オブジェクトストレージの残りを道連れにした。しかも、ネットワーク名がたまたま並んだ順に。

唯一残る待ちは reconcile の一番最後の `seatGfehFounder` である。これは **home** の区画だけを待ち、`gfehFounderWaitBudget`（10 秒。テストのために設定ごとに上書き可能）を上限とし、そのうえでこの機械の最初のアカウントを着席させる。最後にあるので、それが長引いても、既に終わっている仕事を遅らせることしかできない。まだ冷えた状態から起動しているデーモンは次のパスで着席させられ、起動は `ReconcileNetworks` の直後にそれを走らせる。同じ理由で、`GfehReadyNetworks` は各健康の探査に、呼び出し側に残っているものから引くのではなく `context.WithoutCancel` によって自分の予算を与える —— そうしないと、使い切られた期限がすべての区画を一度に死んで見せてしまう。キャンセルは今も尊重される。それはシャットダウンだからである。

**オブジェクトストレージに入／切の設定は無い。** ファイルを保管することこそこの機械の目的なので、それは DNS や ingress が動くのと同じように動く —— 有効にすべき機能としてではなく、Town OS が何であるかの一部として。スイッチが買うのは、誰かが自分のファイルの行き先を調べている間にそれが切の位置で見つかる機会だけだった。デーモンを落としたい管理者は、他のシステムサービスと同じようにサービスのパネルからそれを止める。アップグレードされた機械の設定のテーブルに残った古い `object_storage_enabled` の行は、何にも読まれない。

残っている抜け道は方針ではなく*ビルド*についてのものである。これは ingress を関門とし（`INGRESS_IMAGE` が空だと四つの HTTP のビューはどこからも到達できないので、区画を起動しても何も提供しない名前を公開することになる）、`GFEH_IMAGE` が明示的に空ならオブジェクトストレージを丸ごと飛ばす（dev モード）—— `UI_IMAGE` と `INGRESS_IMAGE` が使うのと同じ `LookupEnv` の約束事である。`Getenv` だと空の値が「既定を使う」を意味してしまい、切るスイッチが無くなるからである。

**最初のアカウントは home の区画に着席させられる。** `ensureFirstUserPrincipal` は、この機械のもっとも早く作られたアカウント（`CreatedAt` による。同点はユーザー名で決めるので、マップの反復順によって創設者が reconcile のたびに変わることはない）の名前を持つ主体を、`gfeh.CeilingForAccount(admin)` とともに作る。森が空の区画は誰にも仕えない。オペレーターが Users のタブを開き、何も見つからず、自分のアカウントがそこに無いことを自力で理解しなければならなくなる。**home だけである** —— どの機械にもその区画はあるが、後から追加されたネットワークは、そこに権限付与を与えられた誰かのものであり、そこに創設者を着席させれば、他人が作った名前空間をその人に手渡すことになる。gfehd のおかげで冪等である。既に存在する主体には 409 を返すからである。

**名前はハンドラの差し替えの後に公開される。** `publishGfehNames` は背景で走る。gfehd は `/status/ping` をポーリングするが、それは**完全なルーターが上がるまで 503 を返す**（[起動ステータス](#起動ステータスと更新)）ので、区画は起動が実質的に終わるまで起動を完了できない。これをその場で待てば、待っている当の起動をデッドロックさせる。時間内にどの区画も準備完了にならなければ、名前は単に次の reconcile で公開される。

区画は `collectSystemServices()` に登録されるので、`POST /system-services/refresh` はそれらを pull し直して再起動する —— ingress を黙って古いままにしていたのは、この登録の漏れだった。

### バージョンの結合

**Town OS は現在の gfehd をビルドし、バージョンのつまみはあえて置いていない。** `Containerfile.gfeh` が実行するのは裸の `cargo install gfehd` である —— `--version` も `--locked` も無い —— ので、イメージはビルド時に crates.io にあるものを、現在の依存に対して解決した上で運ぶ。

かつてはつまみがあり（`GFEH_VERSION`、それに `GFEH_LATEST` という脱出口）、それは無害どころか実害だった。Makefile は毎回のビルドでバージョンを `--build-arg` として渡しており、`--build-arg` は **`ARG` の既定値に勝つ**。したがって出荷されていたのは Makefile の数字であり、Containerfile の数字は飾りだった。そして両者はずれた —— Containerfile は `0.1.2` と書き、それより古いものが Town OS の下で動けない理由を長々と説明していたのに、Makefile は黙って `0.1.1` をビルドしていた。現在の公開版を取ることは、下限を構成上満たすことであり、答えの二つ目の住処を無くすことでもある。

どちらの失敗もスイートは捕まえられないが、その理由は異なる。**ユニット**のスイートは偽の gfehd で代用しており、そもそもデーモンを動かさない。**統合と UI** のスイートは本物を動かす —— `make/test.sh` がビルドしたイメージを両方のコンテナへ読み込む。パーティションが起動し、その管理ソケットに応答し、自らの上限を課すことを示せる代用物など無いからである。しかし単に*古い*だけのデーモンは、やはり問題無くビルドされ、push され、インストールされ、起動する。それは実機でオブジェクトストレージが決して起動しないという形でしか現れない。記録のために書いておくと、それこそが下限の存在した理由である。0.1.1 は SMB のユーザーを持つパーティションの設定を解析できず（gfehd の設定の構造体はどれも `#[serde(deny_unknown_fields)]` であり、その `SmbConfig` に `users` のフィールドが無い）、起動した瞬間に認証しに行くが、起動の最中のそれは ping 以外のすべてに 403 を返す `:5309` のスタブである。

**どちらのビルドも、それぞれ異なる手段で層のキャッシュを破る。** `cargo install gfehd` は毎回のビルドでバイト単位に同一の `RUN` の行なので、その層は恒久的なキャッシュのヒットになる —— さもなければ、「今日 crates.io にあるもの」が契約のすべてであるビルドが、最初のビルドの crate を永遠に、静かに、毎回きれいなログとともに提供し続けることになる。より安いキャッシュの鍵は無い。crate がいつ変わったかを知るとは crates.io に尋ねることであり、それこそがこのビルドの目的だからである。

- `release-gfeh` は **`--no-cache`** を渡す。出荷される物において、これより弱いものは許容できない。
- `gfeh-local` は日単位の粒度の **`GFEH_CACHE_DATE`** の build-arg を渡す。この代用物はすべての統合と dev の実行の前提条件なので、ここで `--no-cache` を使えば実行のたびに Rust の依存の木を再コンパイルすることになる。しかし純粋なキャッシュのヒットは、そのマシンで最初にビルドされた時点の gfehd に凍結してしまう —— そして統合と UI のスイートは、それに対して本物のパーティションを起動する。日単位はその折衷である。当日のうちはキャッシュのヒット、日付が変われば再ビルドとなる。

いずれの場合も cargo のレジストリのボリュームは残るので、依存の木は再コンパイルされるが再ダウンロードはされない。これは一般的なビルドの規則（[CLAUDE.md](CLAUDE.md)）の具体例である。リポジトリのソースからビルドされるローカルのイメージは、ソースの変更がそのキャッシュを破るので乖離しえない。内容をビルド時に取得するイメージには、明示的なキャッシュの破棄が必要である。

### UI

`/dashboard/objects`（ナビゲーションは `nav.objects`、「Object Storage」）。上部にネットワークの選択があり、その下に `?tab=` の下位タブがあって、`ui/src/routes/objects/` の下にそれぞれ一ファイルずつある: **Overview**（区画ごとの状態、クォータ、そして公開された名前と、それぞれが ingress を通って到達されるのか直接接続されるのか）、**Users**（主体と ceiling。追加は Town OS のアカウントを射影する）、**Grants**、**Links**（公開。取り消し付き）である。読み取りは `requireAuth` なので、このタブは管理者専用ではない。変更の操作は管理者か `gfeh` の権限付与を必要とし、いずれにせよ呼び出し側のネットワークの上でのみである。

その画面の二つの細部は、使えない数字やトークンを見て読者が行動してしまうのを止めるために存在する:

- **Overview の Port の列は HTTP のビューでは空である。** gfehd が HTTP のビューについて報告するポートは、ingress がプロキシする*コンテナ側のバックエンドのポート*であり、読者のいるどこからも到達できない —— 「Ingress (HTTPS)」の横に `9000` と印字すれば、`s3.gfeh.home:9000` へ接続してみて、この機能は壊れていると結論することを誘う。SMB は自分の数字を保つ。それは本物のホストのポートになるはずのものだからである。
- **Links のタブは完全な URL を、サーバー側で組み立てて描く。** `GfehExposureView.URL` は `gfehPublishedLinkBase` —— `https://<http-view-fqdn>/f/` —— から作られる。それは ingress の vhost とリーフの SAN を名付けるのと同じ収集器から来るので、公開されたリンクは構成上、ingress が経路を持ち証明書が覆う名前になる。ブラウザで組み立てないのは、UI がサーバーの既に持っている四つのことを知らなければならなくなるからである。提供する名前が区画のものでも機械のものでもなく *http のビューの*ものであること、それがグローバルなものではなくその区画自身のネットワークの TLD の下で修飾されること、経路が `/f/<token>` であること、そして報告されたポートは決して現れてはならないこと、である。その区画が HTTP のビューを何も提供していないときこのフィールドは空である —— 正直な答えである。そのときそのトークンを提供しているものは何も無いからである。そして無効な公開は、クリックできる 404 ではなく素のテキストとして描かれる。このタブにある有効なリンクはどれも http のビューのルートで公開の一覧にも載る（[公開ファイルのインデックス](#公開ファイルのインデックス)）ので、一覧から外すのは取り下げであって、単に共有をやめることではない —— そしてタブは表の上でそう告げる（`objects.links_public_notice`）。これらのリンクを見せる画面はここだけであり、送った相手だけのものだと信じたまま次の一つを公開してしまう前にそれを言えるのも、ここだけだからである。この注意書きが出るのは、リンクが**有効であり、かつ提供されている**ときだけであり、列と同じ `URL` のフィールドで判定される。HTTP のビューを持たない区画にはインデックスが座るルートが無いので、そこでの警告は起きていない露出について述べることになる。

**この画面が、オブジェクトストレージを管理する唯一の場所である。** サービスの画面にオブジェクトストレージの区画は無い。区画は**そのものがシステムサービス**であり —— それぞれ一つの `town-os-system--gfeh-<network>` のユニットである —— 既にその画面の System Services のテーブルの一行、`Object Storage (<network>)` として、他のすべてのシステムサービスと同じ状態のバッジ、同じ start/stop/restart/logs の操作とともに存在している。その横のパネルはその行を繰り返し、しかもそれとは独立にポーリングしていたので、一つのユニットが二つの水準に二つの操作を持ち、食い違いうる状態になっていた。さらにそのパネルは無条件に描かれるのに、テーブルは自分のポーリングが返ってくることを関門にしていたので、最初の描画ではオブジェクトストレージだけが画面の一番上に来て、その一瞬あとにシステムサービスがその上へ落ちてきていた。サービスの画面での `?expand=objects` は、その行がある System Services を開く。

## サービス

### サービスのユニットの絞り込み

systemd のユニットの問い合わせは dbus の水準で `town-os-package--*` のパターンに絞られており、システム上のすべてのユニットではなく Town OS のパッケージのユニットだけを取得する。システムサービスのユニット（`town-os-system--*`）は `IsSystemServiceUnit()` によって別に識別される。結果の集合はさらに、ネットワークコントローラ（`-network.service`）、uPnP の補助（`-upnp.service`）、ポートフォワード（`-fwd-`）を除く。ネットワークコントローラのユニットは失敗の検出のために内部では保たれるが、ユーザーに見える一覧からは除かれる。

### サービスの説明の付加

パッケージの説明は、パッケージごとに個別の YAML を読むのではなく、リポジトリごとに一回の `LoadPackages` の呼び出しでまとめて読み込まれる。説明は、各パッケージの同一性から期待されるユニット名を組み立ててサービスのユニットに対応付けられる。

### サービスのユニットの生成

systemd のサービスのユニットは、パッケージのランタイムの種別に応じて異なる形で生成される。

**コンテナのパッケージ**は、起動に `podman run`、停止に `podman stop` を使う podman ベースのユニットを生成する。ポートの対応付け（`-p`）、環境変数（`-e`）、ボリュームのマウント（`-v`）を含む。

**VM のパッケージ**は `qemu-system-x86_64` を使う QEMU ベースのユニットを、以下とともに生成する:

- `-m {MB}` -- メガバイト単位のメモリ（コンパイルされたバイトの値から変換される）。
- `-smp {cpus}` -- 仮想 CPU の数。
- `-nographic` -- ヘッドレスでの動作（画面出力なし）。
- `-enable-kvm` -- KVM によるハードウェアの高速化。
- `-drive file={image},format=raw,if=virtio` -- virtio のブロックデバイスとしての raw のディスクイメージ。
- ポートの対応付けごとの `hostfwd=tcp::{external}-:{internal}` を伴う `-netdev user,id=net0` -- QEMU のユーザーモードのネットワークと、ホストからゲストへのポートフォワード。
- `-device virtio-net-pci,netdev=net0` -- 準仮想化されたネットワークのデバイス。

VM のユニットは、起動前と停止後のフックで `firewall-cmd` を通じてファイアウォールのポートも管理し、ポートの衝突を避けるためソケットのユニットと協調する。

### サービスのユニット API

- `GET /systemd/units`（localhost または認証）-- すべてのパッケージのサービスのユニットを平坦に一覧する。パッケージの識別子、パッケージの説明、ネットワークコントローラの失敗のフラグを付加したユニットの状態を返す。
- `GET /systemd/units-tree`（localhost または認証）-- 同じデータを依存関係の木にまとめたもの。根のパッケージが上に、その親の下に dep が入れ子に、再帰的に並ぶ（形は `/storage/package-volumes` を鏡写しにしている）。各ノードは `repo`/`name`/`version`（生の実効名。`--dep--` を含みうる）を、人に見せる `package_identifier` と並べて運び、加えて平坦なエンドポイントと同じ状態のフィールドも運ぶので、クライアントは行を豊かにするために二度目の取得をする必要がない。**検索とページネーションは根のノードにのみ適用される** —— 依存関係の子孫はページの数に数えられないので、木はページの境界であっても常に部分木を丸ごと携えて届く。
- `POST /systemd/status`（管理者が必要）-- サービスのユニットの状態を変える。ユニット名と動作（start、stop、restart、enable、disable）を受け取る。
- `POST /systemd/status/tree`（管理者が必要）-- 根のパッケージの依存関係の木の全体に対して動作を適用する。`repo`、`name`（生の実効名なので、インストールの API からの値をそのまま戻せる）、`version`、`action` を受け取る。許されるのは `start`、`stop`、`restart` だけであり、`enable`/`disable` は拒否される。またシステムコントローラ自身のユニットを停止することは拒まれる。**走査の順序は動作に依存する**。ユニットは葉から先に集められ（start と restart にとって自然な順序である）、stop ではその順序が反転されるので、根がその子孫より先に落ちる。

### サービスの管理の UI

サービスの画面は、インストール済みのパッケージの systemd のユニットのページ分割されたデータテーブルを表示する。各行はパッケージの識別子、説明、active の状態、sub の状態、そして操作のドロップダウンを表示する。

#### サービスの操作

各サービスの操作のドロップダウンが提供するもの:

- **Start** -- サービスを起動する（確認付き）。
- **Stop** -- サービスを停止する（確認付き。システムコントローラ自身については無効）。
- **Restart** -- サービスを再起動する（確認付き）。
- **Service Logs** -- このサービスのユニットのジャーナルのビューアを開く。
- **Network Logs** -- このサービスのネットワークコントローラのユニット（`-network.service` の接尾辞を持つユニット名）のジャーナルのビューアを開く。

### 高度なログ

サービスのテーブルの下の「Advanced Logs」のボタンは、以下を備えたモーダルを開く:

- **Controller Logs** -- `town-os-systemcontroller.service` のログを見る。
- **System Logs** -- システム全体（すべてのユニット）のログを見る。
- **Journal Errors** -- 優先度 3（エラー以上。`journalctl -p 3` に相当）に絞ったシステムのログを見る。
- **カスタムのサービス名** -- 任意の systemd のユニットのログを見るためのテキスト入力。

### ジャーナルのビューア

ジャーナルのビューアのダイアログが提供するもの:

- 文脈に応じてユニット名、「System Logs」、「Journal Errors」を示す動的なタイトル。
- ユニットの active の状態と sub の状態を示す状態のバッジ（特定のユニットを見ているとき）。
- デバウンス付きの絞り込み（300 ミリ秒）を伴うリアルタイムの検索。
- 日付と時刻による時間範囲の絞り込み。
- 自動スクロールを伴う継続的なログの追尾のための follow モードの切り替え（検索や時間の絞り込みが有効なときは自動的に無効になる）。
- 最初の末尾へのスクロール: ビューアが開いたとき、項目の読み込みが終わった時点で一度ログの入れ物が末尾までスクロールされる。末尾へのスクロールの効果は `journalEntries.length > 0` を関門としているので、項目が届く前の空の初回描画で消費されてしまうことはない。加えて、確定と描画の間に展開された木が育つ場合に備え、末尾の `requestAnimationFrame` がレイアウトの落ち着いたあと scrollTop を留め直す。
- 項目を分単位でまとめる木のビューの切り替え。木のビューが既定であり、各分のグループは**既定で展開されている**。展開の状態のマップは明示的な折り畳みだけを保持する。未定義の項目は展開として扱われるので、初めての切り替えは展開ではなく折り畳みになる。
- 表示されているすべてのログの項目のクリップボードへのコピー。
- ログの出力における ANSI のカラーコードの描画。
- 構造化されたフィールド（`name=value` の対）の強調。

### ログ API

ログのデータを提供するエンドポイント:

- `GET /systemd/logs`（localhost または管理者）-- 過去のジャーナルの項目を Server-Sent Events で流す。`unit` のクエリパラメータがサービスを選ぶ。空または `__system__` はシステム全体のログを返す。
- `GET /systemd/logs/tail`（localhost または管理者）-- ジャーナルの項目の JSON の一ページを返す。対応するパラメータ: `unit`、`lines`（既定は 100）、`before`/`after`（カーソルによるページネーション）、`grep`（大文字小文字を区別しない検索）、`since`/`until`（Unix の時刻）、`priority`（syslog の重大度による絞り込み。0 は絞り込み無し）。
- `GET /systemd/logs/tree` と `GET /systemd/logs/tree/tail`（localhost または管理者）-- 木にスコープされた対応物。`unit` の代わりに `repo`、`name`、`version`（すべて必須）を取り、そのパッケージの依存関係の木にある**すべての** systemd のユニットを覆うので、親のログとその dep のログが一つのビューで交ざる。再生とページ送りの意味論は、それ以外は `/systemd/logs` と `/systemd/logs/tail` に一致する。

## アカウントの管理

### アカウントの模型

各アカウントが持つもの: ユーザー名（主キー）、パスワードのハッシュ（JSON には決して出さない）、メール、電話、実名、管理者のフラグ、無効のフラグ、**権限付与の集合**、ネットワークのスコープ、作成／更新の時刻である。アカウントは SQLite のテーブルに保存される。

アカウントの「種別」は**無い**。アカウントは管理者である（すべてのネットワークで、すべての権限付与を持つ）か、そうでないかであり、管理者でないものは切り替えられた権限付与を持つ。`Account.Restricted()` —— 管理者でなく、少なくとも一つの権限付与を持つもの —— は導出されるのであって、保存されることは決してない。

**サービスアカウントは存在しない。** 以前のリリースはオブジェクトストレージのデーモンに専用の管理者アカウントを与えていた。それはもう無く、`account.PurgeLegacyServiceAccounts` がアップグレード後の最初の起動でそれを（および保管されたパスワードを）削除する。[サービスアカウントは存在しない](#サービスアカウントは存在しない)を参照。

### 検証の規則

- **パスワード** -- 最低 8 文字であり、印字可能な ASCII（`0x21`--`0x7E`。空白は不可）のみ。上位ビットの立ったバイトと制御バイトは、bcrypt に至る経路のすべての層 —— HTTP の Basic 認証、JSON、URL のエンコード、DB の latin1 の列 —— がそれらを同一に往復させることを信じるのではなく、作成の時点で拒否される（`ErrPasswordInvalidChars`）。
- **メール** -- 標準的なメールの形式（`user@domain.tld`）。
- **電話** -- 任意の整形（`+`、空白、ダッシュ、括弧）を含む数字。
- **連絡先の情報** -- メール、電話、実名はすべて必須である（空でないこと）。
- **権限付与** -- すべての名前が `account.AllGrants` に無ければならず（`ErrInvalidGrant`）、管理者は明示的には一つも持てず（`ErrGrantsAdmin` —— 既にすべてを持っているので、保存された部分集合は食い違うことしかできない）、権限付与を持つアカウントは少なくとも一つのネットワークにスコープされていなければならない（`ErrGrantsNoNetworks`）。
- **ネットワークのスコープ** -- すべての項目が有効なネットワーク名でなければならない（`ErrInvalidNetworkName`）。空の一覧が「任意のネットワーク」と読まれることは決してない。

### 権限付与

**権限付与**とは、管理者でないアカウントが持てる名前の付いた能力である。二つある:

| 権限付与 | 定数 | 買えるもの |
|---|---|---|
| `wireguard` | `account.GrantWireGuard` | そのアカウントのネットワークにおける WireGuard の peer の登録と更新 |
| `gfeh` | `account.GrantGfeh` | その同じネットワークが所有するオブジェクトストレージの管理 —— 主体、その権限付与、公開されたリンク |

`account.AllGrants` が登録簿である。そこに無い権限付与は保存できない。それが、API の要求の打ち間違いが、黙って何にも一致しない権限になってしまうのを止めている。能力の追加はそこへの一項目と、`grantRoutes` へのその経路だけである —— 新しい列も、新しい移行も、新しい `UpdateFields` のポインタも要らない。UI はチェックボックスを鏡写しの `ui/src/lib/grants.js` から描くので、新しい権限付与に新しいマークアップも要らない。

この二つは**独立している**。`wireguard` を持ってもオブジェクトストレージでは何も買えず、`gfeh` を持っても peer の登録は買えない。アカウントは両方を持ってもよい。`Account.HasGrant` は「この呼び出し側はそもそもこれをしてよいか」に答え、`Account.MayAdministerNetwork` は「どのネットワークで」に答える —— 互いの代わりになることは決してない。

#### 強制は三層であり、その組み合わせこそが要点である

1. **`grantAllowlist`** は*全体に効く*フェイルクローズドなミドルウェアである。明日追加された経路は、誰かが `grantRoutes`（`src/svc/systemcontroller/controller_auth.go`）に `"METHOD PATH"` を鍵として載せるまで、制限されたアカウントには既定で拒否される。有効なトークンを持たない要求、管理者からの要求、権限付与を持たない普通のアカウントからの要求は、その経路自身の認証へまっすぐ通される —— 権限付与とは、それを行使するために存在するアカウントにとっての*上乗せの*権限であり、これはそれらだけを閉じ込める。
2. **その経路自身のミドルウェア** —— `requirePeerEnroll`（`wireguard` の権限付与）と `requireObjectStorage`（`gfeh` の権限付与）である。どちらも `requireGrant` から作られており、管理者はすべての権限付与を持つので通す。読み取りは `requireAuth` のままである。
3. **`requireNetworkScope`**。ハンドラの中にある。ネットワークは要求の本体やクエリの中にあり、それを解析したのはハンドラだけだからである。これは**閉じ込める**のであって与えはしないし、`Restricted()` のアカウントだけを閉じ込める —— 普通のアカウントは権限付与を持たず、したがってスコープも持たず、空のスコープはすべてのネットワークを拒否するので、それを素のアカウントに適用すれば、あえて `requireAuth` にしてある経路のすべての読み取りが 403 になってしまう。

`grantRoutes` が、権限付与で買えるものの全部である:

```
wireguard: GET  /networks/peers   POST /networks/peers/add   POST /networks/peers/refresh
gfeh:      GET  /gfeh             GET  /gfeh/principals      POST /gfeh/principals/add
           POST /gfeh/principals/remove                      GET  /gfeh/grants
           POST /gfeh/grants/add  POST /gfeh/grants/revoke   GET  /gfeh/exposures
           POST /gfeh/exposures/withdraw
```

加えて `grantCommonRoutes` がある。これはどの権限付与を持つかに関わらず、権限付与を持つ者なら誰でも到達できる: `POST /account/authenticate`、`GET /account/me`、`GET /networks`、`GET /dns/services`、`GET /tls/ca.crt`、`GET /status/ping` である。これらが無ければ権限付与は使えない —— まずサインインしなければ権限付与を行使できない —— ので、すべての権限付与へ重複させるのではなく共通にしてある。

`GET /status/ping` がその一覧にあるのには二つ目の理由がある。それは**公開**であり、認証のミドルウェアをまったく付けずに登録されているので、匿名の他人でも 200 を得る。許可リストは全体に効くフェイルクローズドなものなので、これを省くと、有効なトークンがその 200 を 403 に変えてしまっていた —— 認証したほうが、何も提示しないより厳密に不利になっていたのである。これはダッシュボードの 60 秒ごとのセッションの心拍でもあり、その状態表示の全体の出所でもあるので、`gfeh` を持つアカウントはすべての `/gfeh` の経路へ到達できても、使えるページを得られなかった。`wireguard` も併せて与えても助けにならなかった。ping はどちらの権限付与にも紐付いていないからである。

意図的に**無い**ものにも注意すること。`/gfeh/partitions/*` は `requireAdmin` のままである（区画を用意することは権限の木の根を作り btrfs のサブボリュームを確保することであり、`TOWNOS_CONTRACT.md` はそれを管理者のために予約しており、gfeh のクライアントは 403 で分岐する）。また `GET /networks/peers/connected` は、すべてのネットワークにまたがってすべてのアカウントの peer と観測された送信元アドレスを集約する。

作成後は変えられない `Admin` とは違い、権限付与は変更できる。そして `account.Manager.CreateGranted` は `Create` とは別のメソッドなので、不変条件（権限付与を持つ者は決して管理者ではなく、常に空でないスコープを持つ）は、広げられた位置引数の並びから組み立てられるのではなく、作成の時点で一か所で強制される。

#### 古い列からの移行

以前のリリースは能力ごとに真偽値の列を持っていた。`legacyGrantColumns`（`src/account/sqlite.go`）がそれぞれを、それがなる権限付与へ対応付け、`migrateLegacyAccountColumns` がそれを引き継いで列を落とす:

| 古い列 | なるもの |
|---|---|
| `wireguard` | `wireguard` |
| `object_storage` | `gfeh` |
| `network_only`（両方を一つのフラグへ畳んでいた、途中段階のスキーマ） | 両方 |

**一つの列に、一つの権限付与。** peer を登録できたアカウントは今も登録でき、できなかったアカウントが黙ってそれを得ることもない —— アップグレードの最中に権限を広げるのは取り返しのつかない方向である。そのアカウントはパスワードを保ったままで、画面には権限が増えたと書かれるものが何も無いからだ。`smb_nt_hash` はきっぱりと落とされる（[SMB のビューは無い](#smb-のビューは無い)を参照）。

### すべてのアカウントは home ネットワークに属する

`Manager.Create` —— **最初の**アカウントとすべての普通のアカウントが通る経路 —— は `networks: ["home"]` を書く。`CreateGranted` はそれを混ぜ込まない。そこでは管理者が選んだスコープがちょうどそのアカウントの到達できるネットワークであり、`home` を畳み込めば `office` にスコープされた入口を広げてしまう。

これが安全なのは、権限付与を持たないアカウントにとってスコープは**閉じ込めではなく所属**だからである。`Restricted()` は偽なので、上のどの層もそれを参照しない。そして、そこに無いネットワークを名指しすることはありえない —— [home ネットワークは常に存在する](#home-ネットワークは常に存在する)を参照。

### アカウント API

- `POST /account/create` -- 新しいアカウントを作る。ブートストラップのモード（有効な管理者のアカウントが存在しない）では認証なしのアクセスが許される。そうでなければ管理者の認証が必要である。空でない `grants` の配列は、渡された `networks` とともに `CreateGranted` へ回される。そうでなければアカウントは `Create` を通じて作られ、home ネットワークに加わる。ユーザー名の重複のエラーは、ユーザーの列挙を防ぐため一般的な失敗のメッセージを返す。
- `POST /account` -- ユーザー名でアカウントを取得する（認証が必要）。
- `GET /account` -- すべてのアカウントをページネーションと検索付きで一覧する（認証が必要）。
- `POST /account/update` -- アカウントのフィールドを更新する（認証が必要）。更新されるユーザー名は**本体**から来るので、他人のアカウントの編集は管理者専用である。その検査が無ければ、認証されたどのアカウントも `{"username":"admin","fields":{"password":"..."}}` を POST してこの機械を乗っ取れてしまう —— コントローラはホストの podman のソケットを動かすので、それは root である。普通のアカウントも自分の連絡先の詳細とパスワードは編集できるので、この経路はきっぱり `requireAdmin` にはしていない。管理者の状態はアカウントの作成後には変えられない。権限付与とネットワークのスコープは変えられるが、**管理者だけが、自分自身のアカウントについてであっても**である —— そうでなければ普通のユーザーが自分に `gfeh` を与えて区画へ入り込んだり、`wireguard` を与えてオーバーレイに peer を登録したりできてしまう。nil の `networks` は保存されたスコープに手を触れず、nil でないものはそれを丸ごと置き換える。`validateGrantResult` は更新の*後*の行の状態を検査するので、管理者への権限付与、権限付与を持つ者の昇格、権限付与の下からのスコープの消去が、すべて捕まる。
- `POST /account/disable` -- アカウントを無効にし、認証をできなくする（管理者が必要）。そのアカウントの生きているセッションも取り消す。無効化を効かせているのはそれではない —— `SessionManager.Validate` は無効なアカウントのトークンをそれ自身で拒むので、この保証は取り消しが成功したことに依存しない —— それは、あとでアカウントが再び有効にされたときに、無効化の前に発行されたトークンが再び働くのを止めるためのものである。誰かのアクセスを取り消したあとの「有効化」で管理者が意図しているのは、そういうことではない。
- `POST /account/enable` -- 無効にされたアカウントを再び有効にする（管理者が必要）。

### アカウントの管理の UI

ユーザーの管理の画面（`/dashboard/users`）は、アカウントのページ分割され並べ替えでき検索できるデータテーブルを表示する。各行はユーザー名、メール、電話、実名、管理者／ユーザーの役割のバッジ、有効／無効の状態を示す。行ごとの操作には Edit のボタン（パスワード、メール、電話、実名、**権限付与のチェックボックス**、ネットワークのスコープの選択を更新するダイアログを開く）と、確認付きの有効／無効の切り替えがある。専用のユーザー作成のページ（`/dashboard/users/create`）へのリンクがあり、そこには同じ操作を備えた登録のフォームがある。どちらのフォームもチェックボックスを `ui/src/lib/grants.js` から描き、ネットワークが一つも選ばれていない状態での権限付与を拒否する。

### セッションの管理

セッションは JWT のトークン（HS256）を使い、セッション ID（UUID）、ユーザー名、発行の時刻のクレームを持つ。署名鍵は一時的である。サービスの起動のたびに `crypto/rand` によって 32 バイトのランダム値が生成され、ディスクへ永続化されることは決してない。起動時に `InitSessionManager` が走ると、既存のセッションはすべて消される（`DELETE FROM sessions`）。新しい鍵では以前のトークンは無効だからである。`TOWN_OS_SIGNING_KEY` の環境変数で生成された鍵を上書きできる。セッションは最後の使用から 7 日後に失効する。背景の掃除の作業が定期的に失効したセッションを取り除く。

**無効なアカウントのトークンは届いた時点で死んでいる。** `Validate` が `Disabled` を検査して拒む。サインインの後のすべての要求は、その関数だけから認可されるからである。この検査が無ければ、アカウントの無効化はそれが*再び*ログインすることを止めるだけで、既に持っているトークンはセッションの寿命いっぱい有効なままであり、しかも自分が使われることで自分を延長していた。

**セッションのマネージャが無いことは、サービスが無いことであって、開かれたサービスではない。** この機械のすべての認可の判断は、かつて一つの nil から導かれていた。`requireAuth`、`requireAdmin`、`requireGrant`、`revokeSession`、`requireNetworkScope`、`callerIsAdmin` はいずれも `GetSessionManager() == nil` を「認証は設定されていないので通せ」と読んでいた。それは*認証すべき相手が誰もいない*ことと*全員が認可されている*ことを同じ状態にしていた —— 認可の面の全体が、設定されていないフィールド一つ分の距離で、ホストの podman のソケットを root として動かすコントローラの上で、`POST /account/create` と `POST /packages/install` を匿名の呼び出し側へ提供するところにあり、しかも型システムにそう書かれているものは何も無く、そうなってもエラーは出なかった。

その条件は今や **`ServerConfig.AuthDisabled` であり、推論されるのではなく明言される**。認証が有効なのにセッションのマネージャが無いのは設定の誤りであり、`NewHandler` はハンドラではなく `ErrAuthNotConfigured` を返す —— 要求ごとにではなく構築の時点で拒むのである。起動してから認証の要る経路すべてで 500 を返す機械は分かりにくい障害だが、起動しない機械は、まだ直せるうちに、何が悪いかをジャーナルに一度だけ言うからである。ミドルウェアも同じ状態を拒むので、他の経路で組み立てられたハンドラの集合も閉じている。

`InitTestServer` は、設定がセッションのマネージャを備え付けないとき —— そしてそのときにのみ —— `AuthDisabled` を設定する。それが、マネージャを一度も作らない約 230 のテストの呼び出し箇所を手つかずで動かし続けるものであり、その一方でマネージャを*作る*テストは認証の強制を保つ。そこで認証を無効にすれば、スイートのすべての認可のアサーションが同語反復になってしまう。

`callerIsAdmin` は、答えが移動したのではなく変わった唯一の場所である。識別できない呼び出し側に対して、かつては真を返していたが、今は**偽**を返す。そこへ到達するすべての経路は `requireAuth` か `requireAdmin` の後ろにあり、どちらも今やその状態をきっぱり拒むので、実際には到達不能である —— しかし、その事実を根拠に伏せ字の補助関数が気前よく振る舞うのは、場所が違う。

`SessionManager` のインターフェイスが提供するもの: `Create`、`Validate`、`Revoke`、`RevokeAllForUser`、`Cleanup`、`List`、`GetUsername`、`HasActiveAdminSessions`、`StartCleanup`。

セッションの API のエンドポイント:

- `POST /account/authenticate` -- ユーザー名／パスワードによるログイン（公開）。JWT のトークンとアカウントのオブジェクトを返す。認証の失敗（パスワード違い、存在しないユーザー、無効なアカウント）はすべて、ユーザーの列挙を防ぐため同じ一般的な「invalid credentials」のエラーを返す。
- `GET /account/sessions` -- 認証されたユーザーのセッションを一覧する（認証が必要）。
- `GET /account/me` -- 認証されたユーザーのユーザー名を取得する（認証が必要）。
- `POST /account/session/revoke` -- ID で特定のセッションを取り消す（認証が必要）。

### 監査のログ

すべての管理の操作は監査のログに記録される。各項目が持つもの: 自動採番の ID、アカウント（ユーザー名）、操作の説明、要求のパス、無害化された詳細（資格情報はマスクされる）、成功のフラグ、エラーのメッセージ、作成の時刻である。

**無害化するものは削除ではなくマスクする。** 資格情報の値を `[REDACTED]` に置き換え、鍵は残す。監査を読む者は、そのフィールドが存在して差し控えられたことを見られるべきであり、それを一度も運ばなかった要求と区別できない状態に置かれるべきではない。`auditRedactedKeys` に対して、鍵の全体と、最後のアンダースコアより後の接尾辞の両方を、大文字小文字を区別せずに照合するので、無害な名前まで飲み込んでしまう部分文字列の規則を使わずに `smtp_password` が捕まる。またマップだけでなく配列にも再帰する。パッケージのインストールの `responses` のマップは**不透明**として扱われ、丸ごとマスクされる。その鍵はパッケージの作者のものなので照合する語彙が無く、その値はまさに、このログが写しになってはならない、生成された `type: secret` と `type: oauth` の回答だからである。裸の `key` は意図的に一覧に**入っていない** —— 入れると接尾辞の規則が `public_key` を捕まえてしまい、それは `POST /networks/peers/add` が運ぶものであり、WireGuard の公開鍵は構成上公開でありながら、どの端末が登録されたかを示す唯一のフィールドだからである。

追跡される操作には以下が含まれる: ファイルシステムの作成／変更／削除、リポジトリの追加／削除／移動／更新、パッケージのインストール／アンインストール、ボリュームの purge、パッケージの無効化／有効化、ユニットの状態の設定、アカウントの作成／更新／無効化、認証、セッションの取り消し、設定の更新、アップグレードの却下、アーカイブのアップロード／ダウンロード、page の作成／更新／削除／再ビルド、VM のイメージのアップロード／削除。

読み取り専用のエンドポイントは監査のログから明示的に除外される。除外されるパスには、ルートのパス（`/`）、すべての GET の一覧／問い合わせのエンドポイント、情報のエンドポイント（`/packages/installed/info`）、回答の取得（`/packages/last-responses`、`/packages/responses`）、インストールのプレビュー（`/packages/install-preview`）、バージョン／質問の参照、タイムゾーンの一覧、page の一覧のエンドポイント、status の ping、システムサービスの一覧（`/system-services`）、監査のログの問い合わせ、設定の読み取り、ログの配信のエンドポイントが含まれる。

- `POST /audit/log`（localhost または管理者）-- カーソルまたはオフセットによるページネーション、アカウントによる絞り込み、並べ替え、検索を伴って監査のログを問い合わせる。

### 設定の管理

キーと値の設定は SQLite に保存される。既定の設定には `default_quota`（50 GB）、`max_archive_size`（1 GB）、`archive_unpack_timeout`（600 秒）、`locale`（en-US）、`dns_tld`（home）、`dns_resolution_mode`（auto）、`dns_local_forwarders`（false）、`peer_ttl`（7200 秒）、`gfeh_partition_quota`（0）が含まれる。`proton_image` は `proton` のタグの付いたビルドでのみ登録される。表の全体は[設定](#設定)を参照。

- `GET /settings` -- すべての設定を取得する（管理者が必要）。
- `POST /settings/get` -- 鍵で特定の設定を取得する（管理者が必要）。
- `POST /settings/set` -- 設定の値を設定する（管理者が必要。監査のログに記録される）。バイトの値の設定（`default_quota`、`max_archive_size`）は人が読める文字列（「500GB」、「10MB」など）を受け取り、解析して数値のバイト数として保存する。

**アカウントのマネージャはどれもメソッドごとに context を取り、`dbTimeout` は全体の物語ではなく天井である。** かつては問い合わせごとに自分でルートの context を開いていた（`account.dbCtx`。今は無い）ので、呼び出し側のキャンセルはマネージャの境界で止まっていた。放棄された HTTP の要求は働き続け、グレースフルシャットダウンは問い合わせを中断できなかった。これがここで他所より効いてくるのは、`OpenDB` が `SetMaxOpenConns(1)` を設定するからである —— SQLite は書き手を一つしか許さないので、すべての問い合わせは単一の接続の後ろで直列化され、遅い問い合わせ一つが他のすべての呼び出し側を、中断できない 30 秒の待ちの後ろに留め置く。

`account.queryCtx` は代わりに呼び出し側から派生する。より短い期限を持つ呼び出し側はそれを保ち、期限の無い呼び出し側もなお永遠にぶら下がることはできず、キャンセルされた呼び出し側は問い合わせを止める —— 自分の時計を使い切らせるのではなく。nil の context は panic ではなく `context.Background()` として読まれる。マネージャは、呼び出し側が忘れた引数のために機械を落とす層としては間違っているし、ハンドラを直接組み立てるテストはサーバーの context を nil のままにするからである。

ハンドラは `c.Request().Context()` を渡す。背景の goroutine はサーバースコープの context を渡し、決して要求のものは渡さない。その操作は、それを引き起こした要求より長く生きなければならないからである。

**`getLocale()` が唯一の意図的な例外**であり、context を取るのではなくサーバーの context を使う。これは約 55 箇所から呼ばれ、そのほとんどはエラーのメッセージを作るためであり、そもそも要求の context は境界として間違っている。それが既にキャンセルされている唯一の場合は、クライアントが切ったときであり、そのときそのメッセージはどのみち届かないからである。

六つすべてが変換された —— `SettingsManager`、`AuditManager`、`PagesManager`、`NetworkManager`、`SessionManager`、`Manager` —— `OpenDB` も併せて、そして `dbCtx` は無くなった。

二つのメソッドは呼び出し側のではなく**サーバーの** context を取り、どちらも意図的である。`AuditManager.LogEntry` は、ハンドラが何をしたかを記録するために、ハンドラが戻った*後*に `auditMiddleware` から呼ばれる。要求の context を渡せば、要求の途中で切ったクライアントが、その要求を記録している書き込みをキャンセルできてしまうので、もっとも記録されない操作が、まさに誰かが途中で切断したものになってしまう。`NetworkManager.ReapExpiredPeers` は背景の peer の掃き取りであり、その部分的な完了は、生きている WireGuard のデバイスが今も運んでいる peer を残す。

`Manager.Authenticate` は二つの問い合わせを縛る context を取るが、その間の argon2id のハッシュは縛ら**ない**。argon2 にはキャンセルが無く、同時のハッシュをそれぞれ 64 MiB で 64 個までに抑えているのは `loginGate` である。

### 設定の UI

システム設定の画面は、システム全体のすべての設定について、管理者が構成できる操作を提供する。各設定は枠付きの区画に、見出しと、現在の値を人が読める形で示す説明、そして数値の入力、単位の選択、保存のボタンを持つフォームとともに表示される。

- **Default Volume Quota** -- GB、MB、バイトで設定できる。ゼロに設定されているときは「0 (no quota)」と表示する。
- **Max Archive Size** -- GB、MB、バイトで設定できる。アーカイブのアップロードで許される最大のファイルサイズを制御する。
- **Archive Unpack Timeout** -- 秒、分、時間で設定できる。アップロードされたアーカイブの展開に許される最大の時間を制御する。
- **Language** -- 母語の文字の名前でよく使われる言語を示すドロップダウン。展開できる区画が拡張ロケールを現す。未填充のロケールはアスタリスク付きで示され、無効になっている。
- **Proton Image** -- Proton のランナーのコンテナイメージの参照（`quay.io/town/proton:latest` など）を編集できるテキスト入力。
- **Local DNS Forwarders** -- `dns_local_forwarders` に裏打ちされたスイッチ。その下には、rolodex が*実際に*転送している宛先が、設定から推測するのではなく `GET /dns/status` から読んで表示される。発見が使えるものを何も見つけなかった場合、そのパネルは公開のフォワーダーがまだ使われていると言う。それが、スイッチが入に見えるのに何も変わっていない唯一の場合である。[ローカルのフォワーダー](#ローカルのフォワーダー)を参照。

現在の値は、表示のためにもっとも適切な単位へ分解される（例えば 1073741824 バイトは「1 GB」、120 秒は「2 minutes」と表示される）。入力の検証は負の値と数値でない値を拒否する。

## パッケージのアップグレード

### アップグレードの検出

アップグレードのシステムは、インストール済みのパッケージのバージョンを、設定されたリポジトリで利用できる最新のバージョンと比較する。より新しいバージョンが存在するとき、あるいはローカルの変更が検出されたとき、パッケージはアップグレードの印を付けられる。

- `GET /packages/upgrades`（認証が必要）-- 利用できるアップグレードを一覧する。各項目にはリポジトリ、名前、インストール済みのバージョン、最新のバージョン、変更のフラグが含まれる。
- `POST /packages/upgrades/dismiss`（管理者が必要）-- 現在のアップグレードを却下済みと印を付ける。現在のアップグレードの集合の SHA256 のハッシュを計算し、`dismissed_upgrades_hash` の設定として保存する。

status の ping の応答には `upgrades_available`（数）と `upgrades_dismissed`（真偽値。ハッシュが一致すれば真）が含まれる。

## ネットワーク

### UPnP のポートの対応付け

`upnp.Manager` のインターフェイスは、UPnP/IGD を通じてローカルのネットワークのゲートウェイ上の TCP のポートフォワードを管理する `AddPortMapping` と `RemovePortMapping` を提供する。実装は SSDP を通じてインターネットゲートウェイデバイスを発見し、WANIPConnection2 の SOAP のメソッドを使う。ローカルの IP は外部のアドレス（8.8.8.8:80 の UDP）へ接続して検出する。

### ネットワークコントローラ

ネットワークコントローラは、パッケージごとのポートフォワードと UPnP の対応付けを管理する。ネットワークの要件を持つ各パッケージは、外部／内部の対応付け、UPnP のフラグ、フォワードのフラグを伴うポートを指定する JSON の状態ファイルを持つ。

- **socat による転送**（`forward=true` のとき）-- `socat TCP-LISTEN:{externalPort},fork,reuseaddr TCP:127.0.0.1:{internalPort}` を走らせて通信を転送する。
- **UPnP の対応付け**（`upnp=true` のとき）-- ゲートウェイ上でポートを対応付ける。`forward=true` のときは外部から外部へ対応付け（socat が listen する）、`forward=false` のときは外部から内部へ対応付ける（podman のブリッジがそれを処理する）。
- **調整** -- fsnotify で状態ファイルを監視し、必要に応じて転送器と対応付けを停止／起動する。
- **更新** -- UPnP の対応付けは 1800 秒の TTL で 10 分ごとに更新される。
- **停止** -- context のキャンセル時に、すべての UPnP の対応付けを取り除き、すべての socat のプロセスを kill する。

### 依存関係の共有ネットワーク

パッケージの依存関係は親のパッケージの podman のネットワークを共有する。これにより、同じ依存関係の木にあるコンテナは、ホストのポートフォワードを介するのではなく、コンテナ名で直接（共有ネットワーク上の podman の組み込みの DNS を通じて）通信できる。

- **冪等なネットワークの作成** -- すべてのサービスのユニットは、ネットワークコントローラ（NC）が存在するかどうかに関わらず `ExecStartPre=-/usr/bin/podman network create {network}` を含む。これは起動順序の安全網である。NC がまだネットワークを作っていない場合（イメージが未ビルド、systemd の競合など）でも、サービスは起動できる。NC もネットワークを作る —— 先に着いたほうが勝ち、もう一方は何もしない。
- **ネットワークの所有権** -- 親のパッケージが podman のネットワーク（`town-os-net--{repo}-{name}-{version}`）を所有する。NC は `ExecStartPre` でネットワークを作り、`ExecStopPost` でそれを取り除く（`podman network rm -f`）。
- **依存関係は親のネットワークに加わる** -- 依存関係のサービスのユニットは、自分のものを作るのではなく `--net {parent-network}` を使う。（親より先に起動する場合に備えて）`ExecStartPre` で冪等にネットワークを作るが、決してそれを取り除かない。
- **ポートを持たない単独のパッケージ**は元のパターンに従う。`ExecStartPre` で `podman network rm -f` のあと `podman network create`、`ExecStopPost` で `podman network rm -f` である。NC も親の NC も持たない単独のパッケージだけが、`create` の前に `rm -f` を行う。
- **依存関係を持つ親**は、`ExecStartPre` の `create` の前に `rm -f` を**しない**。依存関係が既にそのネットワークの上で動いている可能性があるからである（`Before=` の順序付けにより、それらが先に起動する）。

### 依存関係の systemd の順序付け

依存関係の systemd のユニットは、親との相対で正しい起動／停止の順序を保証する順序付けの指示を持つ:

- **依存関係のユニット**: `PartOf={parent-service}`（親を止めると dep へ連鎖する）と `Before={parent-service}`（dep は親より先に起動し、親より後に止まる）。
- **親のユニット**: `Wants={dep1} {dep2} ...` と `After={dep1} {dep2} ...`（親は dep を欲し、起動の前にそれらを待つ）。
- **ネットワークコントローラ**: NC のための既存の `Wants=` は、依存関係の `Wants=` の対象と統合される。

これは `PackageUnitConfig` のフィールドによって構成される: `ParentNetwork`、`ParentUnitName`（依存関係用）、`DependencyUnitNames`（親用）である。reconcile は依存関係のレコードと `ParentName()` からこれらを計算する。

### 依存関係の環境変数

親のパッケージは、共有ネットワーク上で依存関係へ到達するための環境変数を受け取る:

- `TOWNOS_DEP_{KEY}_HOST` -- 依存関係の podman のコンテナ名（共有ネットワーク上の podman の DNS で解決できる）。
- `TOWNOS_DEP_{KEY}_PORT_{containerPort}` -- コンテナ側のポート番号（親と dep は同じネットワーク上にあるので、ホストのポートの対応付けは要らない）。
- `TOWNOS_DEP_{KEY}_PORT_{NAME}` -- dep が `network.external` / `network.internal` で意味のあるポート名を宣言していたとき、数値の形に加えて出力される（下記の**名前付きのポート**を参照）。名前は大文字化されるので、dep での `sql` は親では `TOWNOS_DEP_DB_PORT_SQL` になる。数値の形と名前の形は共存し、常に同じ値を運ぶ。

### 依存関係のテンプレート変数

上記の実行時の環境変数に加えて、依存関係のホストとポートの値は、パッケージのコンパイル中に `@variable@` のテンプレートの目印としても使える。これにより親のパッケージはコンパイル時に自分の `environment` のフィールドの値から依存関係を参照でき、また**兄弟の依存関係**が `dependencies.<key>.responses` のブロックの中で互いを参照することもできる。

- `@dep_KEY_host@` -- 依存関係の podman のコンテナ名に解決される（共有ネットワーク上の podman の DNS で解決できる）。
- `@dep_KEY_port_N@` -- その依存関係の数値のコンテナのポート N に解決される。
- `@dep_KEY_port_NAME@` -- dep が意味のある名前 `NAME` を付けたコンテナのポートに解決される（下記の**名前付きのポート**を参照）。テンプレートの中では小文字であり、環境変数の接尾辞とは大文字小文字を区別せずに一致する。同じポートについて `@dep_KEY_port_N@` と共存する。

テンプレートの鍵は、`TOWNOS_DEP_*` の実行時の環境変数の名前から `TOWNOS_` の接頭辞を取り除き、残りを小文字化して導かれる。例えば `TOWNOS_DEP_DB_HOST` はテンプレートの鍵 `dep_db_host` になり、`TOWNOS_DEP_DB_PORT_5432` は `dep_db_port_5432` になる。

`@dep_*@` の形が尊重されるのは、`@variable@` の置換が既に走る場所だけである —— `environment` の値と dep の `responses` である。ファイルテンプレートの `content` の中では、代わりに Go のテンプレートの `.Dep` の名前空間を使うこと（上記の**ファイルテンプレート**を参照）。`{{.Dep.KEY.Host}}` と `{{index .Dep.KEY.Ports "sql"}}` が同じ値を運ぶ。`.Dep` は同じ `TOWNOS_DEP_*` の計算から埋められ、すべてのポートを、その数値の鍵（`"5432"`）と、宣言されていた場合は小文字化された意味のある名前（`"sql"`）の両方の下に現す。

**親**の側では、これらの変数は依存関係のインストールの後、その依存関係のコンテナ名とポートが分かった時点で解決される。それらはユニットの生成の際に親の環境の値へ適用される。reconcile も依存関係の環境変数を作り直すので、再起動やバージョンの変更をまたいでも systemd のユニットは正しいままである。

**依存関係**の側（`dependencies.<key>.responses` の下で宣言され、別の兄弟の鍵を参照する回答）では、解決は `installDependencies` の中でトポロジカルソートによって行われる:

- `src/svc/systemcontroller/controller_install_dependencies.go` の `orderDependencies` が、各兄弟の dep の `Responses` を `@dep_KEY_host@` / `@dep_KEY_port_N@` の目印について解析し、DAG を作る。参照を持たない兄弟の dep が先に走り、参照する兄弟は、それが名指しした兄弟の後に走る。等しく準備のできた dep どうしの同点は、決定性のためアルファベット順で決める（Go のマップの反復は乱雑なので、再現性のために並べ替えは必須である）。
- 兄弟の dep のあいだの循環は致命的なエラーであり、どの dep も用意される前にインストールを中止する。
- その順序の各 dep について、`depIP.CompileWithContext` が走る**前**に `applyDepTemplates` がその dep の `Responses` に対して呼ばれ、`@dep_OTHER_*@` の目印が、既にインストールされた兄弟から溜まったコンテナ名／ポートの値で置換される。このコンパイル前の置換が無いと、dep の YAML の型付きの質問（`type: port` や、`Output` が `strconv.ParseUint` を走らせる任意の型）が、リテラルのプレースホルダを `ErrInvalidResponseType` で拒否し、インストールの途中で中止して、半分だけインストールされた親をディスクに残してしまう。
- 自己参照（`dep X が @dep_X_host@ を参照する`）は循環としてではなく無視される。宣言された兄弟の鍵でない名前への参照は、外部のテンプレート変数として扱われ、順序付けでは無視される。
- インストールのハンドラは SSE でエラーを流し、HTTP のハンドラからは `nil` を返すので、監査のログはインストールが実際に完了したかどうかに関わらず常に `success=true` を記録する。つまり部分的なインストールの失敗（半分だけインストールされた dep の木、`installed/<repo>/<parent>/<version>/` の下に孤児として残った btrfs のボリューム）は、SSE のストリームと systemd のユニットの一覧でしか見えず、`/audit/log` では見えない。

例: 依存関係の鍵 `db`（ポート 5432 を公開する Postgres のコンテナ）を持つパッケージは、`127.0.0.1` をハードコードする代わりに、環境の区画で `@dep_db_host@` と `@dep_db_port_5432@` を使える:

```yaml
environment:
  DB_HOST: "@dep_db_host@"
  DB_PORT: "@dep_db_port_5432@"
```

兄弟から兄弟への参照の例（jitsi の形）: `jitsi` は `prosody`、`jicofo`、`jvb` に依存する。`jicofo` と `jvb` はそれぞれ prosody のコンテナ名と内部の XMPP のポートを必要とするので、親の YAML は参照する各 dep の `responses` のブロックを通じてそれらを通す。`orderDependencies` はまず `prosody` を、続いて `jicofo` と `jvb` を（この二つはアルファベット順で）インストールし、それぞれプレースホルダは prosody の具体的なコンテナ名とポート 5222 に置換されている:

```yaml
dependencies:
  prosody:
    package: prosody
  jicofo:
    package: jicofo
    responses:
      xmpphost: "@dep_prosody_host@"
      xmppport: "@dep_prosody_port_5222@"
  jvb:
    package: jvb
    responses:
      xmpphost: "@dep_prosody_host@"
      xmppport: "@dep_prosody_port_5222@"
```
### 依存関係の共有ボリューム

同じ依存関係の木にあるパッケージは、双方の同意によって btrfs のサブボリュームを共有できる。dep の作者はボリュームに `shareable: true` の印を付ける。そのうえで親の作者は、`expose:` のブロック（dep のボリュームを親のコンテナへマウントする）か、別の dep の上の `consume:` のブロック（ある兄弟のボリュームを別の兄弟のコンテナへマウントする）のいずれかを宣言する。`shareable: true` の無いボリュームは相互にマウントできない —— インストール／reconcile のパスは、共有可能でないボリュームへの参照をすべて拒否する。

この配線は、既存の `HostVolumeMount` の仕組みの上の薄い層である。インストールの経路が各 `expose`/`consume` の項目を、生産側の dep のディスク上の btrfs のサブボリュームを指す podman の `-v <hostpath>:<containerpath>:<options>` のフラグへ解決する。reconcile は起動のたびに、親の永続化された YAML から同じフラグを組み立て直し、`installUnitIfChanged` の内容の差分が変更を自動的に拾う —— 特別な再起動のフックは無い。

**dep 側の同意。** dep はボリュームごとに `shareable: true` を宣言する:

```yaml
# radarr/1.0.yaml
volumes:
  movies:
    mountpoint: /movies
    quota: "@moviesize@"
    shareable: true     # opt-in: parent or sibling may mount this
  config:
    mountpoint: /config  # not shareable; rejected if any parent tries to expose it
```

**親 → dep（`expose:`）。** 親の `dependencies.<key>.expose:` のマップは、親のコンテナへバインドマウントする dep のボリュームを名指しする。各項目はコンテナのパスと、任意の `readonly` のフラグ（既定は `true`。親は通常 dep の出力を消費するだけだからである）を取る:

```yaml
# plex/1.0.yaml
dependencies:
  radarr:
    package: radarr
    expose:
      movies:                  # volume name in radarr's YAML
        path: /data/movies     # in-container path on Plex
        readonly: true
  sonarr:
    package: sonarr
    expose:
      tv:
        path: /data/tv
        readonly: true
```

**兄弟 → 兄弟（`consume:`）。** `dependencies.<key>.consume:` の一覧は、ある兄弟の dep のボリュームを**この** dep のコンテナへマウントする。各項目は `from:`（同じ親の `dependencies:` のマップにある兄弟の dep の鍵）、`volume:`（その兄弟の YAML でのボリューム名）、`path:`（消費する dep でのコンテナのパス）、そして任意の `readonly`（既定は `false`。兄弟から兄弟への共有は通常書き込みを必要とするからである —— 例えば *arr がダウンロードのクライアントの `/downloads` へ取り込む場合）を取る:

```yaml
# media/1.0.yaml — parent that wires download client + arrs
dependencies:
  qbittorrent:
    package: qbittorrent
  radarr:
    package: radarr
    consume:
      - from: qbittorrent
        volume: downloads
        path: /downloads
  sonarr:
    package: sonarr
    consume:
      - from: qbittorrent
        volume: downloads
        path: /downloads
```

**トポロジカルなインストールの順序。** `consume.from` の参照は、既存の `@dep_KEY_*@` の回答の参照と並んで、`orderDependencies` が組み立てるインストール時の DAG に辺を加える。兄弟の A から消費する dep の B は、B のコンテナが起動するときに A の btrfs のサブボリュームが既に存在するよう、厳密に A の後にインストールされる。consume の辺どうしの循環（A が B から消費し、B が A から消費する）は致命的なエラーであり、どの dep も用意される前にインストールを中止する。自己消費（`from:` がこの dep 自身の鍵と等しい）は検証の時点で拒否される。

**検証。** コンパイル時の検証が拒否するもの: 相対、あるいは遡上を含むマウントのパス、同じ `dependencies:` のマップで宣言されていない鍵への `consume.from` の参照、自己消費、一つの dep の中で重複する consume のパスである。パッケージをまたぐ検証（生産側の対応するボリュームに `shareable: true` があること）は、生産側の YAML が読み込まれるインストール／reconcile の時点で行われる —— 共有可能でないボリュームを expose あるいは consume する親は、`volume %q is not marked shareable on %s` でインストールに失敗する。

**テンプレートによるパスの置換。** `expose.<volname>.path` と `consume[].path` は、通常のボリュームのマウント先とまったく同じように `@question@` の置換に参加する。`consume.from` と `consume.volume`（および `expose` のマップの鍵）は識別子であってデータではないので、置換されない。

**権限についての注意 —— バインドマウントは UID/GID をそのまま通す。** ホスト上の dep の btrfs のサブボリュームは、dep のコンテナがそれを作ったときの uid:gid が所有する。dep が 1000:1000（linuxserver/* の既定）で動き、消費する親や兄弟が別の uid で動くなら、消費側は読み書きで EACCES を受ける。直し方はプラットフォームではなくパッケージ YAML にある。ボリュームを共有するパッケージのあいだで `PUID`/`PGID` の質問の既定値を揃えること。`HostVolumeMount.UID`/`GID` の chown の行は意図的に非再帰であり、dep の作者が書き込み可能なマウントに対して明示的に設定したときにだけ適用される。共有ボリュームの解決器が自動で chown することは決してない。

**テンプレートの名前空間。** dep の共有可能なボリュームは、ファイルテンプレートの `.Dep` の名前空間にも `.Dep.<key>.Volumes.<volname>` として現れる（値は dep のコンテナの中でのそのボリュームのマウント先である）。これは `.Dep.<key>.Ports` と並行である。共有可能でないボリュームは意図的にこのマップから省かれるので、ファイルテンプレートは dep の作者が公開に同意していないデータへ到達できない。

**アンインストールの順序。** 既存の `Before=`/`PartOf=` の指示が既に、親が dep より先に止まること、そして dep がその生産側より先に止まることを保証しているので、親がアンインストールされる（その dep を連鎖的にアンインストールする）とき、生産側のボリュームに手が触れられる前に消費側のコンテナは消えている。新しいアンインストールのロジックは要らない。

**対象外。** dep はちょうど一つの親に属する（既存の不変条件である）。共有ボリュームは dep をマルチテナントにするものではない。逆方向の共有（親のボリューム → dep）は v1 では対応しない。必要になったときのため、スキーマは拡張可能なままにしてある。システムサービス（`town-os-system--*`）はこの機能を得ない —— `GenerateSystemServiceUnit` は `expose`/`consume` を参照しない。

### 名前付きのポート

依存関係のポートの参照は、コンテナのポートの数値の代わりに意味のある名前を使える。dep はその名前を `network.external` / `network.internal` の YAML の鍵として宣言し、親は `@dep_KEY_port_NAME@` を通じて同じポートを参照する。これにより生のポート番号はちょうど一か所（それを所有する dep）にとどまり、親はプロトコルの些事ではなく役割（`sql`、`http`、`admin`）について語れる。

**正典の形。** dep がポート番号を所有する —— 自動生成と上書きの両方が通常どおり働くよう、理想的には `type: port` の質問の既定値として:

```yaml
# dep: named-db/1.0.yaml
environment:
  PGPORT: "@port@"
network:
  internal:
    sql: "@port@"
questions:
  port:
    query: "What port should PostgreSQL listen on?"
    type: port
    default: "5432"
```

```yaml
# parent: named-parent/1.0.yaml
environment:
  DB_HOST: "@dep_db_host@"
  DB_PORT: "@dep_db_port_sql@"   # no "5432" anywhere in the parent
dependencies:
  db:
    package: named-db
```

**マップのスキーマ。** `network.external` や `network.internal` のポートの項目は、以下のいずれかの YAML の鍵を持つ:

- 数値のポートの文字列（旧来の形）: `"5432": "5432"` → ホストのポート 5432 → コンテナのポート 5432。名前は記録されない。
- `PortNameRegexp`（`^[a-zA-Z][a-zA-Z0-9_]*$`）に一致する意味のある名前: `sql: "5432"` → コンテナのポート（値）がホストのポートも兼ね、名前 `sql` が `PackageNetwork.{External,Internal}Names[containerPort]` に保存される。名前は（数値としての解析との曖昧さを避けるため）文字で始まらなければならず、英数字とアンダースコアを含みうる。

両方の形は同じマップの中で共存する。パーサーは鍵で分岐する。一つの名前が二つの異なるコンテナのポートに対応すること、あるいは二つの名前が同じコンテナのポートに対応することは、コンパイル時のエラーである。コンパイルされた `Package` の型は、既存の `PortMap` と並んで二つの任意の `PortNameMap` のフィールドを得る。数値のポートしか気にしない利用者（ユニットの生成、ネットワークの状態の直列化）に変化は見えない。

**環境変数とテンプレートの出力。** コンパイルされた dep の各ポートについて、インストーラは（常に）`TOWNOS_DEP_<KEY>_PORT_<N>=<N>` を出力する。そのポートに名前があれば、加えて同じ値で `TOWNOS_DEP_<KEY>_PORT_<UPPER_NAME>=<N>` も出力する。テンプレートの解決器は `TOWNOS_` の接頭辞を取り除き残りを小文字化するので、`@dep_db_port_5432@` と `@dep_db_port_sql@` はどちらも同じ値に解決される。`controller_install_dependencies.go` の `depKeyRefRegex` は両方の形を受け付け、兄弟の dep のトポロジカルソートは DAG を組み立てるときに名前による参照を認識する。

**後方互換性。** 数値の形を使う既存のパッケージは手つかずで動き続ける —— 移行は強制されない。親は同じファイルの中で、同じ dep への数値の参照と名前の参照を混ぜられる。reconcile は起動中に両方の形を組み立て直すので、生き残っている既存のインストールが退行することは決してない。

**いつ名前を使うか。** 親が dep のポートを参照するときは常に、である。名前は親が引ける唯一の事実であり、数値は dep が所有する。まず内部のポートに名前を使うこと（親と dep のあいだの通信は共有の podman のネットワークの上にある）。外部の名前付きのポートも許されるが、親が通常ホストの割り当てを通じて dep へ接続することはないので、あまり使われない。

## ネットワーク（WireGuard のオーバーレイ）

**ネットワーク**とは、DNS の TLD と対になった名前付きの WireGuard のオーバーレイである。パッケージはネットワークへインストールされ、peer はそこへ加わる。TLD は、誰が何を解決できるかを区切るものである（[ネットワークの TLD、デュアルホーム、スプリットホライズンの解決](#ネットワークの-tldデュアルホームスプリットホライズンの解決)を参照）。

### ネットワークの模型

`account.Network`（`src/account/network.go`）が運ぶもの: `Name`、`TLD`、`Subnet`、`Address`（この機械自身のオーバーレイのアドレス。常に `.1` のホスト）、`PublicKey`、`PrivateKey`（決して直列化されない）、`ListenPort`、`Enabled`、そして時刻である。名前は WireGuard のインターフェイスの接尾辞や systemd のユニット名として再利用されるので、DNS のラベルとして安全である（`^[a-z0-9]([a-z0-9-]*[a-z0-9])?$`、最大 32 文字）。

`Enabled` が制御するのは*輸送*だけである。偽のとき WireGuard のインターフェイスは立ち上げられず、遠隔からのアクセスは断たれるが、ローカルの DNS の解決とコンテナそのものは動き続ける。

### home ネットワークは常に存在する

`DefaultNetworkName` は `home` であり、それは起動時の reconcile ではなく、テーブルと並んで **`account.InitNetworkManager` によって投入される**。したがってデータベースが存在する瞬間からそこにある。コントローラが起動する前にも、すべてのテストサーバーにも、この機械が提供する最初の要求に対しても、である。`account.DefaultNetwork()` が正典の行である。

これが効いてくるのは、下流のすべてがそれを前提に書かれているからである。最初のアカウントはそこへスコープされ（[すべてのアカウントは home ネットワークに属する](#すべてのアカウントは-home-ネットワークに属する)）、既定の TLD はその TLD であり、gfeh はそこに区画を与え創設者をそこに着席させる。まず作られなければならない機械には、それらすべてが偽である窓が生じる —— それこそが、最初の起動でオブジェクトストレージが死んだまま座り、後の再起動がたまたまそのネットワークを既にある状態で見つけるまでそのままになっていた原因である。

これは**取り除けない**（`ErrNetworkProtected`。`POST /networks/remove` はこれを拒む）し、二度目に作ることもできない —— `home` に対する `POST /networks/create` は TLD の衝突の検査から 409 を受ける。

これは **DNS だけ**のものである。`applyNetworkTransport` はこれに WireGuard のインターフェイスも、オーバーレイのサブネットも、peer も与えないので、トンネルで繋がる端末を持つことはありえない。したがって投入される行は**輸送のフィールドを一切持たない** —— 空のサブネット、鍵の対無し、ポート 0 である。それはプレースホルダではなく真実である。導出されたサブネットや鍵は、何にも読まれないフィールドになってしまう。

**これへの peer の登録は拒まれる。意図的に、そして両方の層で拒まれる。** `POST /networks/peers/add` は `home` に対して 400 を返し、`NetworkManager.AddPeer` は誰が呼んでも `ErrNetworkDNSOnly` を返す。これが効いてくるのは、[すべてのアカウントは home ネットワークに属する](#すべてのアカウントは-home-ネットワークに属する)からである。そこでの登録が通ってしまえば、所属しているというだけでトンネルに乗る道を持つことになり、しかも保存された peer は、存在せず今後も存在しないトンネルを表すことになる。peer は本物のオーバーレイの上で動的に作られるので、トンネルが欲しい呼び出し側はそれを名指しする。

この拒絶はかつて**偶然のもの**だった。ネットワークを確かめる場所はどこにも無く、ハンドラはそのまま `netip.ParsePrefix` まで落ちて、投入された行の空の `Subnet` の解析に失敗し、**500** として表に出ていた。それは拒絶ではなく機械が壊れているように読め、呼び出し側に理由を何も伝えず、しかも何かがその行にサブネットを書いた途端に拒まなくなるものだった。この番人は名前で判断し、サーバー側で鍵の対を生成するより前に置かれ、その後ろには、別の理由でサブネットが空になっているネットワークのための「輸送を持たない行」の検査が続く。

**その TLD は `dns_tld` から来ており、コントローラが両者を歩調を合わせて保つ。** 投入の時点ではそれを知りようがない（account のパッケージは設定のマネージャを持たない）ので、行は素の既定値のまま生まれ、`ensureDefaultNetwork` が起動時にそれを reconcile して、両者が食い違うときにだけ書く。`POST /dns/tld` は設定を書くのと同時にそれを指し直す。どちらも `NetworkManager.SetTLD` を通る。それがまさにこのために存在する。これを間違えるのは見た目の問題では済まない。`applyNetworkTransport` は `n.TLD` を `rolodex.EnsureNetworkScope` へ渡し、それが home のスコープがどのゾーンを所有するかを決めるからである。

### アドレスとインターフェイス

- **サブネット** —— `wireguard.SubnetForNetwork(seed, name)` が、機械の同一性の種とネットワーク名から決定的な `/24` を導く。機械の同一性を鍵にすることで、peer を提供する二つの Town OS の機械は別々のサブネットを選ぶので、両方に加わる端末が衝突を見ることは決してない。サブネットは `10.64.0.0/10` から取られる。家庭用のルーターが配る `10.0`/`10.1` の範囲から離れるためである。種は `networkIPAMSeed()` である。systemd の machine-id、無ければホスト名、無ければ定数なので、導出が失敗することは決してない —— そこにインスタンスのソルトが畳み込まれる。
- **インターフェイス名** —— `wireguard.InterfaceName(salt, name)` は、ソルトを付けたネットワーク名の SHA-256 の `"town" + 16 進 4 桁`である。作成の順序をまたいで安定しており、ネットワークがいくつあるかに依存せず、カーネルの 15 文字の制限に収まる。wg-quick はインターフェイスを設定のファイル名から導くので、設定は `<InterfaceName>.conf` として書かれる。`systemcontroller.NetworkInterfaceName(name)` はソルトを適用した形であり、統合テストはこれを使うので、何も作っていないデバイスに対してテストが主張することは決してない。
- **listen するポート** —— `wireguard.ListenPortForName(salt, name)` は、`DefaultListenPortBase`（51820）から、ソルトを付けた名前のハッシュの分だけずらす。別のネットワークが既に握っているポートは前へ探りながら越える。

#### インスタンスのソルト

WireGuard のインターフェイス名、その UDP の listen するポート、そのオーバーレイのサブネットは、どれも**名前空間全体で共有される**ものであり、テストと dev のコンテナはどちらも `--net host` で動く（意図的である —— ブリッジネットワークの DNS はキャプティブなネットワークで壊れる）。ソルトが無いと、`make test-full` の機械と `make dev` の機械は、同じネットワーク名について*同じ*インターフェイス名と listen するポートを導いてしまう。あとから上がったほうは自分のデバイスを作れず、そのオーバーレイはただ死ぬ。同時に動く二つのテストの worktree も同じように衝突する —— 鉄の掟である。

`TOWN_OS_WG_SALT`（`EnvWireGuardSalt`）は一度だけ `wireGuardSalt` へ読み込まれる。ハーネスは `make/lib.sh` の `wireguard_salt` を通じてこれを `<role>-<INSTANCE_ID>` に設定する —— 役割が一つのチェックアウトの中でテストの機械と dev の機械を分け、`INSTANCE_ID` がチェックアウトどうしを分ける。両方の半分が必要である。これは所与の役割とチェックアウトについて安定であり、それは dev にとって重要である。dev のデータベースは実行をまたいで生き延びるので、そうしないと保存されたサブネットが以前のソルトの名前を持つデバイスを指してしまう。**実機は何も設定せず、従来のソルト無しの名前を保つ**。空のソルトはすべての導出を手つかずで返す。

**podman の既定のサブネットの池は `10.64.0.0/10` の外に保たなければならない。** 実行時のイメージが `/etc/containers/containers.conf` に `default_subnet_pools = [{"base" = "172.16.0.0/12", "size" = 24}]` を書くのは、まさに podman の既定値（10.89/16、10.90/15、10.96/11 など）がすべてオーバーレイの範囲の中にあるからである。範囲内の `/24` はオーバーレイの経路と衝突するものとして飛ばされ、負荷がかかると池は「could not find free subnet from subnet pools」で枯渇し、パッケージのコンテナのネットワークが動かなくなる。そのファイルを取り除いたり、池を `10.64.0.0/10` へ広げ直したりしないこと。

`wireguard` のパッケージは**インターフェイスの制御を自分では一切行わない**。鍵の対を生成し wg-quick 風の設定を描き出すだけである。systemcontroller が描き出された設定をホストと共有されたネットワークの状態のディレクトリへ書き、生成された systemd のユニットがカーネルのインターフェイスを上げ下げする。それが、systemcontroller のコンテナをホストのネットワーク名前空間の要件から自由にしているものである。

**`applyNetworkTransport` の中では順序が効く。** rolodex は、インターフェイスが起動しオーバーレイのアドレスが割り当てられた*後*に、UP のリンクの上で、経路に覆われた状態でプログラムされなければならない —— 割り当てられていることと使えることは同じではない。先にプログラムすると、ホストがまだ持っていないアドレスを rolodex にバインドさせることになる。バインドは `EADDRNOTAVAIL` で失敗し、その listener は永久に死ぬ。rolodex は起動の時点で listener を記録し、その死骸がその後のすべての再主張を阻むからである。

### Peer

`account.NetworkPeer` が運ぶもの: `Network`、`PublicKey`、`Name`、`AllowedIP`、`Endpoint`、`Rolodex`、`CreatedBy`、`ExpiresAt`、`CreatedAt` である。

- **`Rolodex`** は、自分のオーバーレイのアドレスで rolodex の DNS のサーバーを動かす peer を示す。すると機械はそのアドレスを TLD ごとのフォワーダーとして登録するので、共有の TLD の下でその peer が権威を持つ名前がオーバーレイをまたいで解決される。電話やノートパソコンはこれを偽のままにする。
- **`CreatedBy`** は所有権の鍵である。`wireguard` の権限付与を持つアカウントは自分が作った peer だけを更新できるので、スコープの付いたアカウントが他のアカウントの peer を生かし続けることはできない。
- **`Endpoint`** は、この機械自身が自分をどう見ているかではなく、**登録するクライアントが接続した宛先のアドレス**（その `peers/add` の要求の `Host` ヘッダ）から導かれる。この機械の公開 IP（ipinfo.io による）や LAN のアドレスは、NAT やポートフォワードやリレーの後ろでは到達できない —— 同じ Wi-Fi にいる電話は公開 IP へ折り返せないし、プライベートな LAN のアドレスへはそもそも経路が無い。すると peer は虚空へハンドシェイクすることになり、ユーザーにはそれが壊れた DNS のように見える。接続された宛先のアドレスは構成上到達可能である。要求がそれを通って届いたのだから。接続可能なアドレスが無い場合（ループバックからの登録）、エンドポイントは、働きようのないものを設定するのではなく**省かれる**。

### peer の登録の TTL と刈り取り

登録は永遠には生きない。`peer_ttl` の設定（秒。既定は `7200`）が、それが有効であり続ける長さである。長く生きるクライアントはそれが経過する前に `POST /networks/peers/refresh` で自分の peer を更新する。放置された端末の peer は自分で失効するので、追加しかしない `peers/add` のエンドポイントが黙って死んだ peer を溜め込みオーバーレイのアドレスを焼き尽くすことはありえない。nil の `ExpiresAt` はその peer が決して失効しないことを意味する —— rolodex のサーバーやオペレーターが追加した端末のような恒久的な peer である。

失効は常に `now + peer_ttl` として**サーバーが計算する**。呼び出し側がそれを選ぶことは決してない。背景の刈り取りの goroutine が `ReapExpiredPeers` を呼び、そのあと影響を受けた各ネットワークの輸送を一度描き直すので、生きている WireGuard のデバイスと rolodex のフォワーダーは刈られた peer を落とす。これはベストエフォートかつ冪等である。永続化された peer の集合が真実の出所であり、描き直しの失敗は次の刻みか起動時の reconcile で修復される。`peerReapInterval` は TTL の四分の一を `[1m, 15m]` へ切り詰めたものなので、期限切れの peer が居残るのは失効から高々 TTL/4 ほどであり、極端に小さい TTL でも極端に大きい TTL でも病的な掃き取りの頻度にはならない。

### 接続中の peer

`GET /networks/peers/connected` は、永続化された行を各トンネルの生きたカーネルの状態と結合する。永続化された側（名前、アカウント、オーバーレイのアドレス、失効）は「誰が入ってよいか」に答え、`wg show <iface> dump` の側（ハンドシェイク、観測されたエンドポイント、転送量）は「いま実際に誰がここにいるか」に答える —— どちらか一方だけでは問いにならない。だから `account.NetworkPeer` を再利用せずに `ConnectedPeerView` が存在する。

解析は純粋な `wireguard.ParseDump` にある。dump の**最初の**行はインターフェイス自身を記述するもので、意図的に飛ばされる。それを peer として扱えば、インターフェイス自身の鍵を持つ幻を作り出してしまう。`wg` の `(none)` と `off` のプレースホルダは、リテラルの文字列としてそのまま通されるのではなく復号される。

**接続しているとは、WireGuard の 180 秒の `REJECT_AFTER_TIME` の窓（`HandshakeStaleAfter`）の中にハンドシェイクがあることである** —— このプロトコルが提供する唯一の生存性である。セッションの取り壊しが無いので、立ち去った peer は、そのハンドシェイクが古くなるまでは、単に暇にしている peer と区別がつかない。*一度も*ハンドシェイクしていない peer は、エポックではなく nil の時刻を保つ。「一度も繋がっていない」と「一日ずっとオフライン」は、その端末についての異なる事実だからである。

systemcontroller は `--net host` で動くので、wg-quick がデバイスを作った名前空間を既に共有している。実行時のイメージは `wg` のバイナリのためだけに `wireguard-tools` を積んでいる（wg-quick は今もホスト上で、生成されたユニットから走る）。インターフェイスが無いことはエラーではない —— 無効なネットワークや、輸送がまだ上がっていないネットワークには生きた peer がいないだけであり、その永続化された行はそれでも描かれなければならない —— そして dump の失敗はパネルを空白にするのではなく永続化された行へ退化する。`home` のネットワークは丸ごと除かれる。輸送を持たないので、これを含めれば、誰がトンネルで繋がっているかについてのパネルに、永久に切断された行を置くことになるからである。

**切断はエンドポイントを増やすのではなく `POST /networks/peers/remove` を再利用する。** WireGuard には kill すべきセッションが無いので、peer を取り除くことが、そこにある唯一の強制的な終了である。

### ネットワーク API

- `GET /networks`（認証が必要）-- すべてのネットワークを、peer の数、導出されたインターフェイス名、動作の状態とともに一覧する。秘密鍵が公開されることは決してない。
- `POST /networks/create`（管理者が必要）-- ネットワークを作る。名前と任意の TLD（既定は名前）を受け取る。サブネットを導き、鍵の対を生成し、listen するポートを割り当て、作られたネットワークを返す。既に取られている名前や TLD は 409 である —— 常に存在する `home` も含めて。
- `POST /networks/remove`（管理者が必要）-- 名前でネットワークを削除する。home ネットワークは取り除けない。
- `POST /networks/enable` / `POST /networks/disable`（管理者が必要）-- オーバーレイのインターフェイスを上げる／下げる。
- `GET /networks/peers?network=<name>`（認証が必要。`requireNetworkScope` によって閉じ込められる）-- あるネットワークに登録されている peer を一覧する。この経路は `wireguard` の権限付与の許可リストにあるのでスコープの付いたアカウントも到達する。そして peer の一覧は端末、それを登録したアカウント、そのオーバーレイのアドレスを名指しする —— 権限付与は呼び出し側自身のネットワークに対する権限であり、読み取りはそれをもっとも忘れやすい場所である。
- `GET /networks/peers/connected`（**管理者が必要**）-- すべての WireGuard のネットワークにまたがるすべての peer を、生きたトンネルの状態と結合したもの。`requireAuth` の兄弟たちより意図的に厳しく、`grantRoutes` にも無い。
- `POST /networks/peers/add`（`requirePeerEnroll`: 管理者または `wireguard` の権限付与。呼び出し側のネットワークに閉じ込められる）-- peer を登録する。`public_key` が空のとき、サーバーは鍵の対を生成し、秘密鍵とすぐ取り込める端末の設定を返す。任意の `endpoint` と `rolodex` のフラグを受け取る。**home ネットワークは 400 である** —— それは DNS だけのものであり、誰が求めても peer を持たない（[home ネットワークは常に存在する](#home-ネットワークは常に存在する)を見よ）。
- `POST /networks/peers/refresh`（`requirePeerEnroll`。しかも呼び出し側が登録した peer についてのみ）-- peer の TTL を `peer_ttl` だけ延ばし、新しい失効を返すので、クライアントは TTL が経過するずっと前に次の心拍の間隔を測れる。
- `POST /networks/peers/remove`（管理者が必要）-- 公開鍵で peer を取り除く。

### ネットワークの UI

`/dashboard/networks` はネットワークを、作成／削除／有効化／無効化の操作とネットワークごとの peer の登録とともに一覧する。二つ目の **Connected Peers** のパネルは、すべての WireGuard のネットワークにまたがるすべての peer を項目化する —— 端末、それを登録したアカウント、そのオーバーレイのアドレス、それが接続してきているエンドポイント、生きたハンドシェイクと転送の状態、そしてその登録の失効 —— 行ごとの Disconnect の操作とともに。

## TLS とローカルの CA

Town OS は自前の X.509 の認証局を動かすので、パッケージと page の通信は名前によって HTTPS で提供される。LAN 上に公開 CA も ACME への依存も無い。

- **CA**（`src/tls/ca.go`）は btrfs の `tls` のサブボリュームの下にある ECDSA P-256 の鍵の対（`ca.crt`、`ca.key`）であり、有効期間は 10 年なので再起動を生き延びる。`EnsureCA` は既存の CA を読み込むか、必要に応じて生成する。証明書は誰でも読め、鍵は所有者のみであり、決して提供されてはならない。CA の失敗は非致命的である —— まったく起動しないのではなく、HTTPS 無しで起動する。
- **リーフ**（`src/tls/leaf.go`）はパッケージごと、page ごとであり、利用者が一つのマウントのパスだけで済むよう、一つのディレクトリに `cert.pem`/`key.pem` として書かれる。`IssueLeaf` は**冪等**である。既存の証明書が要求された SAN の集合をちょうど覆っておりまだ有効なとき、それはディスクに触れずに戻る。それが、reconcile が起動のたびにこれを呼んでも証明書のファイルをかき回さずに済む理由である。ホスト名は DNS の名前でも IP のリテラルでもよい。IP として解析できるものは `IPAddresses` へ、それ以外はすべて `DNSNames` へ入る。
- **`GET /tls/ca.crt`** は**公開**であり（`grantCommonRoutes` にもある）、どのクライアント —— ブラウザ、オーバーレイ越しに加わる電話 —— もルート証明書を取得してこの機械を信頼できる。

パッケージのリーフの SAN の集合は、その A レコード、DANE の TLSA の所有者、ingress の vhost と同じ単一の FQDN から導かれる。[パッケージの FQDN は一つの文字列である](#パッケージの-fqdn-は一つの文字列である--a-レコードリーフの-santlsa-の所有者ingress-の-vhost)を参照。リーフはまた、peer が名前だけでなく生の WireGuard のアドレスでもパッケージへ到達できるよう、インストールのネットワークにおけるこの機械のオーバーレイの IP も運ぶ。

## Ingress

ingress は共有の Host のルーターである。Caddy の子プロセスを監督し、systemcontroller がプログラムする gRPC の管理 API を公開するサイドカーであり、rolodex をプログラムするのと同じやり方である。望ましい経路の集合をメモリに保持し、変更のたびに Caddyfile を描き出し、Caddy を無停止で再読み込みする。

- **`src/ingress`** はコンテナ内のサービスである（`Server`、`renderCaddyfile`、gRPC のクライアント、`town-os-ingress` のバイナリ）。`CGO_ENABLED=0` でビルドされる。
- **`src/ingress/ingressctl`** は systemcontroller 側のライフサイクルのコントローラである。`town-os-system--ingress` のユニットを生成、インストール、再起動し、systemcontroller が接続する gRPC のソケットのパスを公開する。これが別パッケージなのは、まさに CGO 無しの ingress のバイナリが `src/systemd`（sdjournal を通じて cgo を引き込む）を決して import しないようにするためである。

### ルーティング

- **`:443`** —— 経路ごとに一つの `https://<hostname>` の vhost。その経路のファイルに固定されたローカル CA のリーフ、あるいは公開の FQDN については明示的な ACME の発行者で TLS を終端し、共有の `town-os-ingress` の podman のネットワーク上のバックエンドのコンテナへリバースプロキシする。
- **`:80`** —— Host によるルーティング。page（`ServeHttp`）は素の HTTP で直接提供され（静的な内容であり、機微なものは無い）、パッケージは HTTPS のみを保つよう HTTP → HTTPS のリダイレクトを得る。どの経路にも一致しないホストは既定のバックエンド —— Town OS の UI —— へ落ちる。UI がホストの `:80` に居座らなくなった今も、裸の IP でのログイン（`http://<box-ip>/`）が働き続けるようにするためである。
- **まだリーフが発行されていない**経路（ACME でなく、証明書のディレクトリが空）は HTTPS については飛ばされるので、途中まで用意された項目が Caddy に設定全体を拒否させることは決してない。page は `:80` の vhost を得るが、それには証明書が要らない。パッケージがリダイレクトされるのは HTTPS の宛先が実際に存在してからなので、まだ用意されていない証明書へリダイレクトするものは何も無い。

### 一つの vhost に二つのバックエンド: `PathBackend`

経路は普通ひとつのサービスを名指すし、機械の上のほとんどすべての経路がそうである。`Route.path_backends` はその例外である。`(経路, バックエンド)` の対の並びであり、その経路自身のバックエンドを運ぶ素の `handle` の前に、Caddy の `handle` のブロックとして描き出される。これが存在するのは、バックエンドがある名前を所有していても、その下のすべての経路を所有しているとは限らないからである。唯一の呼び出し元は[公開ファイルのインデックス](#公開ファイルのインデックス)であり、`http.gfeh.<tld>` の他のすべての経路が gfehd へ届く一方で、`/` を pages のコンテナから提供する。

- **素の `handle` は常に最後である。** `handle` のブロックは互いに排他で、書かれた順に評価され、素のものはすべてに一致するので、その後に来るものは死んだ設定になる。
- **経路ごとのバックエンドは、並べ替えられずに与えられた順に描き出される。** 並べ替えは Town OS が実際にプログラムする一致子については安全だが（それらは重ならない）、重なる対についてはどちらが勝つかを黙って変えてしまう。
- **経路はホスト名やバックエンドと同じように検証される**（`validPathMatcher`: 絶対、予約されていない URL の文字に `*` を加えたもの、長さに上限）。悪いものは経路もろともではなく**個別に**落とされる。その経路自身のバックエンドが総取りなので、経路ごとのバックエンドを失う代償は、それが主張していた一つの経路だけであり、その名前の背後のサービスは答え続ける。重複する経路も落とされる —— Caddy は同一の一致子を二つ受け入れ、二つ目には決して到達しないからである。
- **経路ごとのバックエンドは自分の `backend_tls` を持たず**、素の HTTP でプロキシされる。そのフラグは経路が*何のため*のサービスかを述べるものであり、このフィールドが到達するために存在する唯一のものは、他のすべての page と同じく `:80` で HTTP を話す。

### 描き出し

出力は**ホスト名で並べ替えられる**ので、描き出されたバイト列は reconcile をまたいで決定的である —— それが、内容の変わっていない再読み込みを監督者が何もしないで済ませられる理由である。全体の設定は `auto_https off`（証明書は Town OS が管理する）、`protocols h1 h2`（ingress は TCP のみを公開するので、UDP 上の H3/QUIC は到達できない）、そして admin のエンドポイントである。Caddy の admin の API は意図的に**有効なまま**にしてある。監督者は新しい経路を `caddy reload` でプログラムし、それがそのエンドポイントと話すので、`admin off` にすると最初の起動より後のすべての経路の更新が壊れる。

その宛先は Caddy の既定に任せず、`admin 127.0.0.1:<ポート>` として**設定に書き出す**。既定値は Caddy 自身の `2019`（`ingress.DefaultAdminPort`、`--caddy-admin-port` / `ingress.WithCaddyAdminPort` で上書きできる）。本番はこの 2019 のままでよい —— ingress のコンテナは自分のネットワーク名前空間を持ち、:443 と :80 とループバックのメトリクスのポートより他は何も公開しないので、admin の API はそのコンテナの内側からしか届かない。**ホストの名前空間で Caddy を走らせるものは、必ずこれを移さなければならない**。そしてテストはすべてそれに当たる。`go test ./src/...` はホストでそのまま走り、統合のコンテナは `--net host` なので、同時に走る二つの `make test-full` は二つの Caddy の子を同じ :2019 に置くことになる —— 二つ目は bind に失敗して終了し、そうなる前に、どちらかの `caddy reload` がもう一方の admin の API に届いて誤った Caddyfile をプログラムしうる（鉄の掟）。宛先を描き出すことは、`caddy reload` をそこへ*向ける*ことでもある。あの命令は、変換しようとしている設定から admin の宛先を読み取るからである。`127.0.0.1` という綴りは意図的で、デュアルスタックの箱では `localhost` が先に `::1` へ解決される一方、Caddy はそのどちらか一方しか bind しない。

ingress は**インターフェイスに依らない**。`-p 443:443` / `-p 80:80` をホストの IP を指定せずに公開し、その Caddyfile は **`bind` の指示を一切運ばない**ので、Caddy はすべてのインターフェイスで listen し、vhost は純粋に SNI/Host で選ぶ。（ホストの IP に固定される唯一の listener は Prometheus のエンドポイントで、これはクライアントのためではなくこの箱自身のためのものである —— [ingress のメトリクス](#ingress-のメトリクス) を参照。）LAN のクライアントとオーバーレイの peer は同じ listener に当たり、SNI で同じ vhost を選び、同じローカル CA のリーフを得て、同じコンテナへプロキシされる。`bind` の指示やネットワークごとの listener を追加しないこと。

本番は 443/80 をバインドする。統合テストは一時的なポート（`host:PORT` として描き出される）を渡すので、`make test-full` が特権ポートで衝突することは決してない。起動は `RebuildIngress` を通じて経路の全集合を宣言的にプログラムする。`RebuildDNS` と同じ押し込みの模型である。パッケージと page の CRUD は同じ gRPC の API を通じて差分の変更をプログラムする。

### バックエンドが落ちているとき: リトライのページ

プログラムされた経路の背後にあるバックエンドの壊れ方は二通りあり、ingress はそのどちらにも同じページで答える。**サービスが使えないことを告げ、五秒ごとに自分で読み込み直す 503**（`unavailableRefreshSeconds`。`Retry-After` に載る値でもある）を、サービスがふたたび答えるまで返し続ける。何かをプログラムし直すことも、経路を引き下げることもない —— その住所はその間ずっと経路のまま保たれるので、バックエンドが listener を取り戻した次の読み込み直しが、それを配り始める。

家庭の箱にとってこれは例外ではなく、ふだんの状態である。パッケージは更新のたびに再起動し、設定の変更はサービスを弾ませ、再起動は二十個のコンテナを何らかの順序で戻してくる。その数秒のあいだ Caddy が言うのは `502 Bad Gateway` であり、それは*戻ってくる途中*ではなく*壊れている*と読める上に、手で再試行する以外に読み手へ残すものが無い。

この二つの失敗の形は Caddy では別々の仕組みであり、片方だけを覆う実装はよくあるほうを取り逃がす。

- **バックエンドは答えた。5xx で。** それは上流の*応答*なので、応答のマッチャと `handle_response` によって `reverse_proxy` のブロックの**内側**で捕まえる —— 描き出しが出すすべてのプロキシが、素の HTTP のものも含めてブロックを開くようになったのはそのためである。
- **バックエンドはそもそも答えなかった** —— コンテナが落ちている、名前が解決しない、接続を拒まれた。`reverse_proxy` が失敗して Caddy は自分の側のエラーを上げ、それを `handle_response` が見ることは決してない。それは**サイトのブロック**の水準にある `handle_errors` である。家庭の箱ではこちらがよくあるほうで、5xx だけを捕まえる実装が黙って取り逃がすのもこちらである。待ち構えている 5xx が、そもそも来ないのだから。

どちらの道も Caddyfile の**スニペット**（`render_unavailable.go`）として描き出され、一つのファイルにつき一度だけ定義され、各サイトのブロックから import される —— だからページが vhost の間でずれることはなく、この箱がいくつ経路を配ろうとファイルは読める大きさのままである。経路の集合が空のときもスニペットは出す。誰も import しないスニペットが指示として解析されることは決してなく、現れたり消えたりする前置きは最初の経路が来た瞬間に Caddy を無駄に弾ませるからである。

- **ページを受け取るのはブラウザだけである。** どちらの道も `method GET HEAD` と `Accept: *text/html*` を門にしている。API のクライアント —— XHR、webhook、`curl`、path backend の背後にいる DoH のリゾルバ —— は、応答の道では上流自身の状態・本体・ヘッダをそのまま写して受け取り、エラーの道では `Retry-After` の付いた素の 503 を受け取る。JSON を解析しているものへ HTML を手渡すことは、壊れたバックエンド一つを、その呼び手の中の二つ目の、より奇妙な失敗に変えてしまう。

  その写しは `copy_response` **だけ**であって、隣に `copy_response_headers` は置かない。この「置かない」ことが構造を支えている —— 応答のハンドラが走るころには `reverse_proxy` が上流のヘッダを既に response writer へ載せ終えているので、もう一度写せばどのヘッダも二度出る。`Content-Length` も含めてであり、それは行儀が悪いのではなく、規格に反したメッセージである。いかにも `copy_response` の連れ合いに見える指示なので、描き出しの試験と統合の表明の両方で留めてある。
- **ページはサービスの名を、描き出された設定から採る。要求から採ることは決してない。** この札は import のときに差し込まれるスニペットの引数であり、`validSiteHost` を既に通った経路のホスト名である。`:80` の受け皿のブロックだけは定数 `Town OS` で札を付ける。そこは経路に合致しなかった*すべての*ホストに答える場所で、その `Host` ヘッダはクライアントが送ってきた何かであり、それを HTML に映し返すことは、どんな名前でも受け取る唯一の vhost での注入になるからである。`safeUnavailableLabel` は、将来の呼び手に対してもそれを本当のままに保つための番である。
- **パッケージの `:80` の転送ブロックはどちらも受け取らない。** そこは何もプロキシしない —— 使えなくなり得るバックエンドが、そこには無い。

#### このページは翻訳されている

ページに載る四つの文はカタログの項目（`src/i18n` の `ingress.unavailable.*`）であって、描き出し側のリテラルではない。Town OS が備えるすべての言語がこのページの描き出しを一つずつ持ち、Caddy は要求ごとに `Accept-Language` から一つを選ぶ。

これは UI が既にしていることと同じで、理由も同じである。**読み手の言語はブラウザの言語であって、この箱の言語ではない。** `I18nContext` はまず保存された選択、次に `navigator.languages` を解決し、どのカタログも覆わない言語を求められたときにだけ、サーバの全体設定 `locale` に戻る。リトライのページは同じ順序を Caddy の中で解決する —— `Accept-Language` に対する `map` を置き、言語カタログごとに一行、そしてこの箱の locale を載せた `default` の一行を置く。携帯がポルトガル語の客人は、ドイツ語に設定された箱でポルトガル語を読む。

- **`map` を使い、ページは一部だけ書く —— 言語ごとの `handle` ブロックではない。** ブロックの形でも動き、実際に測った。`import` は文字の展開なので、展開のたびに選択子まるごとが埋め込まれる。つまり三十の枝は、再読み込みのたびに Caddy へ手渡す JSON の中の三十枚のページを意味していた —— **経路が一本でも 115 KB**、経路が二十本の箱なら、パッケージの導入・ページの編集・毎時の reconcile のたびにおよそ 2.3 MB を押し込むことになる。map なら訳された文は詰まった表に収まり、ページは一度だけ書かれて、そこから `{townos_title}` などを読む。
- **5xx の道は何も描き出さない。`error 503` を上げる** —— そのサイト自身の `handle_errors` へ。それが、ページをプロキシごとではなくサイトのブロックごとに一度だけ展開させているものである。確かめてある。`handle_response` の内側で上げた `error` は外側のエラーハンドラに届き、元の要求もそのままなので、`Accept` の門はそのまま効く。
- **選択子の行は locale ごとではなく、言語ごとに一つである。** 国別の変種（`de-AT`、`en-GB`、`es-MX` …）は基となる言語から導かれ、その国が実際に違う言い方をする文字列だけを上書きする —— この四文はどれもそれに当たらない —— ので、変種ごとに行を置けば同じページを描き出す正規表現が三十本増えるだけになる。`TestUnavailableLocalesCoverEveryCatalog` は期待される顔ぶれを `i18n.PopulatedLocales()` から導き、両方向で落ちるので、カタログに加わった言語がここだけ黙って英語のまま残ることはない。
- **繁体字中国語は総称の `zh` より前で合わせる。** Caddy は map の行を順に試して最初に当たったものを採り、`^zh` は `zh-TW` まで攫っていくので、順序を違えれば台湾・香港・マカオへ簡体字を配ることになる —— それを読めない者の目には、そのまま動いている翻訳に見える。どの型も先頭に錨を打ち、大小文字を問わず、区切りかヘッダの終わりで閉じてあるので、`^ja` がジャワ語に日本語で答えることはない。
- **サービスの名は書式の動詞であって、文字列の連結ではない。** `"%s is unavailable"` は日本語では英語と違う位置に名を置くので、語順はカタログのものである。描き出しは翻訳された文の中へ `{args[0]}` を差し込み、サイトのブロックがそのスニペットを import するとき、Caddy がそこへホスト名を差し込む。
- **箱の locale はユニットのコマンドラインから ingress に届く**（`--locale`、`ingressctl.Config.Locale`。ユニットを生成するときに `locale` 設定から読む）。これは経路の状態ではなく箱ぜんたいの設定なので、gRPC の押し込みではなくユニットに乗る。言語が変われば、他のあらゆるユニットの変更と同じ道筋で ingress に届く —— 次の reconcile が違うユニットを描き出す。カタログの無いコードは拒まれるのではなく無視される。この設定は自由記述の一行であり、打ち間違いの代償は英語のページであるべきで、起動しない ingress であってはならない。
- **翻訳された文字列は、跨ぐ二つの境界の両方に向けてエスケープされる**（`caddyHTMLText`）。それはまず Caddyfile のトークンであり、次に HTML 要素の中身である。HTML のエスケープはトークンを早々に終わらせてしまう引用符も併せて取り除く。バックスラッシュだけは別に処理する —— Caddy はそれをエスケープとして読むが、Go の `html` パッケージがそれに触れる理由は無いからである。

### ingress のメトリクス

ingress は Prometheus のテキスト展示形式を**自分専用の三つ目の listener** で提供する（`ingress.MetricsPath = "/metrics"`、`ingress.DefaultMetricsPort = 9146`）。描き出すのは `src/metrics` で、コントローラのエンドポイントとまったく同じである。すでに持っている二つの listener に相乗りはできない。あの二つはこの箱の正面玄関であり、`/metrics` の vhost は LAN からもオーバーレイからも SNI で到達できてしまう。

**公開は `127.0.0.1` のみ**（`-p 127.0.0.1:9146:9146`）で、ingress がインターフェイスに依らないという規則に対する唯一の意図的な例外である。この収集はこの箱が提供するすべてのホスト名を並べ、そのうちまだ証明書のないものに印を付ける —— 何が公開され、何が中途半端なままかの地図である —— そしてその前には認証が何もない。Prometheus は `--net host` で走るので、podman のネットワークを一跳びもせずにこのループバックの公開に届く。node-exporter やコントローラの対象とまったく同じである。

**収集の対象はどこでも組み立て直されない。** `ingressctl.MetricsAddrFor(port)` が unit の `-p` の指定と `monitoring.Ports.IngressMetrics` の両方を作る。`rolodex.Manager.MetricsAddr()` と同じ、単一の真実の源という配置である。`INGRESS_IMAGE` が空のときこの job は**省かれる**。ingress のない箱は「ingress がない」と報告すべきであって、永久に落ちているように読めるルーターを報告すべきではない。

`ingress` の job が出すもの:

| メトリクス | 型 | 備考 |
|---|---|---|
| `townos_ingress_up` | gauge | 提供中は常に 1、そうでなければ存在しない |
| `townos_ingress_start_time_seconds` | gauge | 稼働時間は `time() - これ`、収集側の時計で |
| `townos_ingress_caddy_up` | gauge | この収集で caddy の子の admin API が応答したとき 1 |
| `townos_ingress_routes{tls}` | gauge | `local`/`acme`/`pending` —— `pending` はプログラム済みだがリーフがまだない |
| `townos_ingress_route_https_ready{hostname}` | gauge | 経路ごとの 1/0。*どの*名前が HTTPS を提供できないかが見える |
| `townos_ingress_path_backends` | gauge | 全経路にわたるパス単位のバックエンド数 |
| `townos_ingress_reloads_total{result}` | counter | `success`/`failure` |
| `townos_ingress_route_changes_total{op}` | counter | `set`/`add`/`remove` |
| `townos_ingress_dropped_total{kind,reason}` | counter | 描き出しが拒んだもの |

加えて **caddy 自身のメトリクス族**を、子のコンテナ内 admin API（`127.0.0.1:<admin のポート>/metrics`。Caddyfile が持つのと同じ宛先から組み立てるので、移した子に対して収集だけが 2019 に取り残されることはない）から取ってきて、そのまま応答の後ろに付ける。名前空間は `caddy_*`/`go_*`/`process_*` なので `townos_ingress_*` と衝突しようがない。admin API は意図的に公開されていないので、素通ししか Prometheus に届く道はなく、しかもそれらはこの箱が答えるすべてのリクエストを実際に提供している当のプロセスを記述している。本体は描き出し直さない。族に解析し直しても情報を失うことしかできない（caddy はヒストグラムを出すが、`src/metrics` は意図的にそれを模型化していない）。

いくつかの選択は偶然ではなく狙いである:

- **`townos_ingress_dropped_total` は `renderCaddyfileTally` が存在する理由そのものである。** ホスト名が壊れている、バックエンドが使えない、パスの照合子が不正 —— そうした経路は描き出し全体を失敗させるのではなく**個別に**捨てられる。つまり gRPC では受け付けられ、一度ログに残り、そして単に提供されない。ログ行はこれには合わない道具である。すでに壊れていると分かるまで、誰も ingress の journal を読まない。
- **パスのバックエンドは vhost ごとではなく経路ごとに一度だけ検証する。** page の経路は HTTPS の用意ができていて、かつ `ServeHttp` でもあるので、同じパス集合から二つのサイトブロックを描き出す。書き手の内側で検証すると、あらゆる拒否が二重に数えられる。
- **counter の系列はすべてゼロで種を蒔く。** `metrics.Render` は標本のない族を省くので、種を蒔いていない `townos_ingress_reloads_total{result="failure"}` は最初の失敗まで存在しない —— 見張っている事象が一度起きてからでないと発火できない警報になる。
- **`townos_ingress_last_reload_success_time_seconds` は再読み込みが成功するまで省かれる**。ゼロとして出さないのは、`time() - 0` が「まだ設定がない」ではなく「56 年前の設定」と読めてしまうからである。
- **収集は一体として失敗しない**し、ルーターを道連れにもしない。届かない caddy の子が犠牲にするのは gauge 一つであって応答ではない。バインドできないメトリクスの listener は記録した上で受け入れる。監視の穴のためにトラフィックの転送を拒むのは誤った取り引きだからである。

これらを描くダッシュボードはまだない —— パネル集合を `RolodexDashboardMetrics()`/`ControllerDashboardMetrics()` に対して宣言している `rolodex` と `systemcontroller` の job とは違う。系列は集められ、問い合わせできる。パネル集合は別の変更である。

## 起動ステータスと更新

`:5309` は起動の作業が何もされる前にバインドされるので、UI は死んだポートをポーリングするのではなく、起動 —— 自己更新も含めて —— が進むのを見守れる。

### 起動のスタブ

`NewBootHandler` は素の `http.ServeMux` であり（意図的である。本物の API の経路を誤って載せてしまうことがありえないようにするため）、ちょうど三つのものを提供する:

- `GET /status/ping` → `{booting, step, done, error, boot_id}`。**起動中は 503** に、終わったら 200 に答えるので、外部の準備完了の探査 —— テストコンテナの `wait_for_url`、オーケストレータの健康の検査 —— がこのスタブを「サービス準備完了」と扱って、半分しか起動していないコントローラを叩き始めることはない。JSON の本体は今も進捗のフィールドを運ぶので、UI は「立ち上がり中」と「完全に落ちている」を区別できる。
- `GET /boot-status` → 進捗のイベントの SSE のストリーム。
- それ以外はすべて → 404 ではなく **403**。その経路は完全なハンドラには存在しており、差し替えまで利用できないだけだからである。

`RootHandler.Swap` は起動の最後に、スタブを完全な Echo のルーターへ原子的に置き換える。listener のソケットは決して閉じられないのでポートの瞬断は無く、既に振り分けられた SSE のハンドラは自分の writer を持っており、差し替えをまたいで流し続ける。

### 進捗の段階

五つの粗い段階であり、意図的に少なく、ユーザーに向いている —— 自己更新を見守る人が知りたいのは、「コントローラ」「DNS」「システムサービス」「自分のパッケージ」のどれが手間取っているかであって、どの内部のコンストラクタが走っているかではない:

`boot_controller` → `boot_dns` → `boot_services` → `restart_packages` → `ready`

鮮度の段階は、インストール済みのパッケージごとに追加のイベントを `restarting_`（`PackageStepPrefix`）を接頭辞として発行する。UI はその接頭辞を取り除き、それぞれを粗い段階と同じ重みの一行として描くので、パッケージの多い機械でも、一本の止まったバーではなく本物の進捗が見える。これらのパッケージごとの名前は、固定の段階に強制される `[a-z0-9_]+` の形にあえて一致しない —— これらは動的な値だからである。

段階のリテラルは、定数として参照されるのではなく `main.go` の `bs.Step("...")` の呼び出しとして重複して書かれている。`TestBootStepsFrontendInSyncWithBackend` がそれらを `main.go` から解析して、フロントエンドの一覧が一致していることを証明するからである。**この二つを同期させておくこと。** ずれればそのテストが大きな音を立てて失敗する。

### 配信の意味論

`BootStatus` は同時使用に対して安全であり、**決して起動を止めない**。`Subscribe` はまず履歴を新しい購読者へ再生し（遅れて来た購読者が何も取りこぼさないように）、バッファは再生の全体に余裕を足した大きさにする。起動が既に終わっていれば、再生の直後にチャネルを閉じるので `for range` の利用者は抜ける。`publish` は非ブロッキングで送る —— バッファが埋まった購読者は落とされて閉じられ、そのクライアントは再接続して履歴の再生を得る。`Done` の後にイベントが続くことはありえない。

### プロセスの同一性と更新

`boot_id` は systemcontroller の起動のたびに作り直されるランダムな UUID であり、スタブと完全なルーターの**両方**の `/status/ping` が報告する（認証されない最小の ping の応答でも運ばれる。再起動をまたぐ短い間、ブラウザはトークンを持たないからである）。更新を求める前にその id を捉えたクライアントは、「古いプロセスがまだ応答している」（同じ id）と「新しいプロセスが上がった」（違う id）を区別できる —— それが無ければこの二つは区別がつかない。どちらも 200 の ping を返し、どちらも起動後は `/boot-status` に 404 を返すからである。これが、UI の Refresh Core Services の流れが自分の後継を見守れるようにしているものである。

`/boot-status` が監査のログから除外されるのも同じ理由である。ハンドラの差し替えをまたいでストリームを開いたままにしている UI は、次の要求を完全なルーターに着地させ、それは 404 を返す。それはストリームの想定された終わりであって、オペレーターの操作ではない —— 監査すれば、更新が成功するたびに失敗した操作の行を綴じ込み、ダッシュボードの赤い失敗の表示を膨らませてしまう。

`POST /system-services/refresh`（管理者）は、すべてのシステムサービスのイメージを依存関係の順に pull する —— まず systemcontroller のイメージ（バージョンの錨である。最後に自分を再起動するとき、新しく pull されたイメージが既にローカルにあるように）、次に rolodex（この機械の DNS。他の pull がレジストリを解決するのにそれを必要とするかもしれない）、そしてそれ以外を並列に（同時実行は最大 3）—— そして、次のプロセスの鮮度の段階がインストール済みのパッケージを再起動するために消費するマーカーを残す。

## DNS の管理（Rolodex）

Town OS には `rolodex-dns` のコンテナが動かす統合されたローカルの DNS のリゾルバが含まれる。rolodex のサーバーはインストール済みのパッケージのゾーンファイルとレコードを管理し、gRPC の Unix のソケットのインターフェイスを通じてローカルの名前解決を提供する。

### Rolodex のマネージャ

rolodex 自体は systemd がインストールして監督する起動サービスである —— systemcontroller はコンテナの水準でそれをインストールも、起動も、停止も、再起動もしない。代わりに `rolodex.Manager` は:

- **`Forwarders` / `ResolutionMode` / `Blocklists`** -- Town OS が所有する設定であり、保存された構成とホストの探索から解決される。マネージャはファイルを書かない。これらは `ProgramRolodex` が動いているサーバーへ押し込むものである。下の「rolodex.yml は起動用の設定にすぎない」の項を参照。
- **`Generation`** -- rolodex が起動時にバインドする gRPC ソケットの同一性（デバイス、inode、mtime）。rolodex が再起動したちょうどそのときに変わるので、プログラムした設定をすべて落とした再起動に気づく手立てはこれである。ソケットが無いときは空で、それは rolodex が動いていないことを意味する。
- **`WaitForDNSReady`** -- `DNSLoopback:{port}` を TCP でポーリングし、接続を受け入れるか 30 秒の期限が過ぎるまで続ける。DNS に依存するあらゆる操作（イメージの pull など）の前に、起動時に呼ばれる。
- **`SystemServices`** -- rolodex のシステムサービスのメタデータ（鍵、表示名、イメージ、ポート、ユニット名）を返すので、状態の応答と UI で他のシステムサービスと並んで現れる。
- **`Status`** -- systemd のユニットの状態を問い合わせ、rolodex が動いているかを報告する。

rolodex のコンテナは `--net host` で動き、DNS を設定されたポート（既定は `53`。テストのために `DNSPort` で上書きできる）で `DNSLoopback`（`127.0.0.2`）にバインドする。イメージのタグはシステムコントローラのリリースのタグから導かれ（`quay.io/town/rolodex:<tag>`）、`ROLODEX_IMAGE` の環境変数で上書きできる。

**解決のモード。** `rolodex.yml` は `Config.ResolutionMode` を通じて `resolution.mode` を明示的に固定し、既定は **`auto`**（`DefaultResolutionMode`）である —— rolodex 自身の段階的なフォールバックの連鎖であり、ルートサーバーからの反復、次に DoH/DoT、次に `forwarders:` の一覧、次に :53 の公開のリゾルバの順で、最後に働いた段にとどまる。モードが rolodex の既定に委ねられず明示的に書かれるのは、上流が既定を変えても Town OS の挙動が動かないようにするためである。転送の統合テストは `ResolutionModeForward` を選び、フォワーダーをローカルのスタブへ向ける。

**素の `recursive` を既定にしてはならない。** それにはフォールバックが*無く*、rolodex の反復のリゾルバ（`src/resolver.rs`）は**ネームサーバーごとに 1500 ミリ秒の期限で、再送しない UDP のデータグラムを一つ**送る。現在の委譲の集合のすべてのサーバーが失敗すると `resolve()` はエラーになり、`iterative_query` は*あらゆる*エラーを SERVFAIL に変える。したがってパケットが一つ落ちれば問い合わせは SERVFAIL になり、外向きの :53 を遮断したり乗っ取ったりするネットワーク（ホテル、キャプティブポータル、一部の ISP）では*すべての*外部の名前が SERVFAIL になる。`auto` はネットワークが許すところでは再帰の持つプライバシーを保ち、許さないところでは失敗するのではなく劣化する。関連: rolodex の委譲のキャッシュと否定のキャッシュは `ce44bb5` で入ったが、それは**どのリリースのタグにも入っていない** —— それを載せたリリースが出るまで、recursive のモードはキャッシュされていない名前と NXDOMAIN のたびにルートから歩き直す（実測: 冷えた公開の名前で 0.6〜1.9 秒、RFC1918 の PTR で 2.7 秒）。

このモードは `dns_resolution_mode` の設定（`auto` | `recursive` | `forward`。`ValidateDNSResolutionMode` が検証するので、解析できない値が `rolodex.yml` へ届いて DNS を壊すことは決してありえない）を通じて、実行時にオペレーターが設定できる。`main.go` は起動時にそれを `rolodex.Config` へ読み込む。`POST /settings/set` を通じた変更は `Controller.RefreshDNSResolutionMode` を走らせ、それが **`SetResolutionMode`** で動いているサーバーへ新しいモードを書き込む —— ファイルもユニットの再起動も DNS の中断もない。rolodex は知らないモードを既定値に落とさず端から拒否するので、値は送る前に検証される。

### 凍りついた設定

`WriteConfig` はかつて、mtime が systemcontroller のバイナリより新しい `rolodex.yml` を飛ばしていた。新しいファイルは手で編集されたものだろう、という理屈である。**どの起動が書くファイルも、それを書いたイメージの中のバイナリより新しい**ので、二度目の起動から先はその番人がコントローラ自身の出力に一致し、`WriteConfig` は永久の何もしない処理になった。`rolodex.yml` はその機械で最初に走ったコントローラが描き出したもののまま、機械の一生にわたって凍りつき、どんなイメージの更新もそれを動かせなかった。`RewriteConfig` は、オペレーターの起こした変更でこれを迂回するためだけに存在していた。

したがって、ある機械の最初の起動より後にテンプレートへ加えられた節は、どれ一つとしてその機械へ届かなかった。これは二度出荷された —— `dnsbl:`（`31f7e80`）、次いで `metrics:`（`9689461`）—— そして二度目こそが、配備済みのある機械に rolodex のメトリクスの listener がまるごと無かった理由である。Prometheus は 15 秒ごとに予定どおり `127.0.0.2:9153` を収集し、そこには何一つバインドされておらず、`rolodex_*` の系列は一度も存在せず、そして DNS のパネルはどれも**エラーではなく空のグラフ**を描いた —— 暇なリゾルバと見分けがつかない。その間ずっと `systemctl --failed` は空で、コンテナはすべて上がっていた。

守られていた実体は何も無かった。ファイルの中のどの値も Town OS の状態から描き出される —— DNS のポート、フォワーダー、解決のモード、二つのブロックリスト、メトリクスのポート —— そしてそのどれかを変える実行時の経路は、いずれにせよ既にファイルを上書きしていた（それが `RewriteConfig` の正体である）。手による編集は、以前は次の設定変更まで生き延びた。今は次の起動まで生き延びる。

**この項は歴史である。** `WriteConfig` も `RewriteConfig` も削除され、Town OS は rolodex の設定ファイルをまったく書かない。かつてそれらが描き出していた設定は、動いているサーバーへ書き込まれるようになり、残ったファイルはインストールイメージのものである。下の「rolodex.yml は起動用の設定にすぎない」の項を参照。この項を残してあるのは、ここに書かれた失敗 —— 黙って何もしない設定の書き込みが、ある機能が一度も動かなかったという形でしか表に出てこない —— が、この部分系が繰り返し生み出すバグの形だからである。

それらの回帰のテストは、守っていたコードとともに無くなった。代わりになるものは次の項に挙げてある。

### rolodex.yml は起動用の設定にすぎない。設定は gRPC で書き込む

`rolodex.yml` を書くのはインストールイメージ（`../install` の `scripts/rolodex-config.sh`。rolodex の `ExecStartPre` として走る）だけであり、ほかに書き手はいない。このファイルが持つのは、動いている rolodex に対して設定できない二つだけである。DNS の **bind の一覧**（UDP と TCP の `127.0.0.2:53`。systemd-resolved が向けられている先であり、リスナーはどんな API 呼び出しよりも先に存在していなければならない）と、**metrics のリスナー**。後者は、その節が存在することを見て rolodex が起動時に一度だけ開く。このファイルはさらに `resolution.mode: auto` と公開フォワーダーを**起動時の既定値**として持つ。ここの `DefaultForwarders` と同じ値なので、最初の書き込みまでの隙間は rolodex の組み込みの選択任せではなく、定まった状態になる。値はすべて定数なので、このファイルは起動のたびにバイト単位で同一である。Town OS は設定ファイルをまったく書かない。

Town OS が持つそれ以外のすべて —— フォワーダーの一覧、`resolution.mode`、そしてブロックリスト —— は `ProgramRolodex` が管理 API を通じて**動いているサーバー**へ書き込む。設定の変更に代償がなくなったのはこれによる。以前は変更を適用するとはファイルを書き直してユニットを再起動することであり、その機械で唯一のリゾルバを再起動するとは、その上のすべてにとっての DNS の停止である。`SetForwarders` は無条件に送る（rolodex 側は単なる格納であり、突き合わせるための取得もない）。`SetResolutionMode` は先に突き合わせる。`auto` へ切り替えると階層の探索がやり直しになるため、定期的に言い直せば落ち着いた階層を捨て続けることになるからである。ブロックリストは `ReconcileBlocklists` を通る。これはもともと読み戻して差分を取る仕組みだった（二つではなく一つである —— RBL の側は退役した）。

**rolodex が再起動するたびに、もう一度走らなければならない。** rolodex はこれらすべてを起動時に設定ファイルから取り込み、gRPC で設定されたものは何も永続化しない。したがって Town OS が命じていない再起動 —— `Restart=always` の下でのクラッシュ、DHCP のリース変更が `town-os-rolodex-config.path` を通じてユニットを揺らすこと、オペレーターの手による再起動 —— は、機械を黙って rolodex の既定値へ戻す。オペレーターが選んだのとは違うやり方で解決し、何も遮らない状態にである。それを表に出すものは何もない。これらの設定はどこからも読み戻されないからである。

再起動の合図は `rolodex.Manager.Generation()`、すなわち rolodex が起動時にバインドする gRPC ソケットの同一性（デバイス、inode、mtime）である。rolodex は起動のたびにそれを unlink して張り直すので、新しい実行は必ず新しい inode を持ち、気づくのに必要なのは `stat` 一回だけである —— D-Bus の往復も、このコードベースがまだ露出していないユニットのプロパティも、専用の RPC も要らない。`reconcileRolodexProgramming` は自前の **15 秒**の刻みで走り、generation が変わらない限り何もしない。分ではなく秒なのは、その隙間が「設定と違うやり方で解決し、違うものを遮っている機械」だからである。generation は一巡がすべて成功したときにだけ記録するので、部分的な失敗は済んだものとして覚えられるのではなく、やり直される。

**上流で修正済み。** rolodex が「:53 に届くか」の探索を起こすのは、かつては**設定ファイルの中で**ブロックリストが有効なときだけだった。そのため一覧がそのファイルに書かれなくなって以降この探索は起動せず、設定されたブロックリストは、まさにその探索が存在する理由である網の上で劣化していた。いまはこれは `DnsblChecker::resolver_availability_loop` であり、無条件に起動され、チェッカーの*実行時の*有効フラグで判断する —— 自動解決の回復ループがすでに持っていたのと同じ形である。gRPC で有効にされた一覧は数秒のうちに探索を得る。

回帰テストは `TestProgramRolodex*` と `TestReconcileProgramming*`（ユニット）、および `TestProgrammedSettingsSurviveARolodexRestart` と `TestGenerationChangesOnlyWhenRolodexRestarts`（統合）である。後者は本物の rolodex に書き込み、再起動し、サーバー自身が忘れたことを確かめ、書き直す。

**イメージとファイルは対であり、その間を取り持つ交渉は何も無い。** `rolodex.yml` は `../install` が書き、その箱がたまたま動かしている rolodex のイメージが解析する。どちらの向きにもバージョンの握手は無く、serde は知らないフィールドも欠けたフィールドもそのまま拒む。イメージの版では必須なのにファイルに無いフィールド、あるいはファイルにあってイメージが知らないフィールドがあれば、起動時の固い `failed to parse config file` になり、`Restart=always` の下ではそれは crash loop、その箱の上のすべてから DNS が消える。これは既に一度、この試験台で起きた。公開されていたイメージが `rbl` → `dnsbl` の改名より前のもので、本物の rolodex を起こすテストはすべて `missing field \`rbl\`` で死に、mock を使うテストはすべて通った。そこから出てくる規則はこうである。install の側の `rolodex-config.sh` と公開される rolodex のイメージは**一緒に**動く。そしてここで `rolodex-dns/go` を上げるのは半分でしかない —— 両方を必要とした SVCB の固定については「この箱が提供する暗号化された伝送路」の項を参照。

**番人は一つ、向きも一つ。** `TestRolodexDohBackendMatchesTheInstallScript` は `../install/scripts/rolodex-config.sh` から `doh.bind` を取り出して `RolodexDohBackend` と突き合わせるので、**こちら側**でインストールの指令書から離れていく編集はここで落ちる。逆向き —— 編集されたのがインストールの指令書のほう —— は、そのあと誰かがこのリポジトリのテストを走らせたときにしか捕まらないし、install のリポジトリが取り出されていない場所ではこのテストは**飛ばされる**。試験用のコンテナがまさにそれである。install の側には検査が無く、二つのリポジトリの間に生成された共有の定数も無い。この番人は、それが覆う向きだけを覆うものとして扱うこと。

### ローカルのフォワーダー

Town OS が既定で書く `forwarders:` の一覧は `DefaultForwarders` —— 公開のリゾルバである。外部の DNS を遮断するネットワーク（ホテル、キャプティブポータル、自社のサーバー以外への外向きの `:53` を落とす ISP）では、まさにそれらのアドレスが落とされているので、`auto` のフォワーダーの段 —— ルートと暗号化された上流が既に失敗した*後*に到達する段であり、まさにこの場合である —— には落ちる先が無い。そういうネットワークが DHCP で配ったリゾルバのほうは、今も答える。

`dns_local_forwarders` の設定（既定は `false`。`ValidateBool` が検証する）は、フォワーダーの一覧を、この機械自身のネットワークの構成が指しているリゾルバで置き換える。これは**解決のモードではない**。ローカルの段が*どの*アドレスを持つかを変えるだけであり、その段がそもそも参照されるかどうかは今もモードが決める —— `auto` では最後の手段、`forward` では唯一の上流、`recursive` では使われない。したがってこれを入にすることが、決してモードを動かしてはならない。

**`auto` はこのスイッチ抜きで、自分から発見する。** この段に到達するのはルートと暗号化された上流の両方が失敗した後だけなので、その目の前にある選択は決して「ルートか、ローカルのリゾルバか」ではない。「ローカルのリゾルバか、SERVFAIL か」である。自社のサーバー以外への `:53` を濾すネットワークで、そこに `DefaultForwarders` を置いたままにしても、プライバシーは何も買えないうえに失敗が確定する —— まさにそれらのアドレスが落とされているのだから、その段まで届いたクエリは二つのタイムアウトを払ったうえで、どのみち死ぬ。唯一答えるリゾルバに機械が目を向けまいとすることで、守られるものは何もない。だから `Manager.forwarders` は、モードが `auto` で明示的な一覧が構成されていないとき、`dns_local_forwarders` が何と言っていようと発見の経路を通る。

**取引が本物であるモードでは、今も切が既定であり、その向きは今も重要である。** ローカルのリゾルバは、その家庭が引くすべての名前を見る。ルートから解決することは、まさにそれを避けるために存在する。それは、オペレーターが承知のうえで行う取引であって、ネットワークが一度おかしくなったときに機械が本人に代わって行うものではない —— そして `forward` では、この段が唯一の上流であって常にすべてのクエリを引き受けるのだから、それこそが実際に行われている取引である。よってこのスイッチは今も `forward` を支配する。`recursive` では、どちらにせよ一覧は使われない。

**`auto` でスイッチを切ることが発見を抑えなくなるのは、退避先となる明示的な一覧が無い場合に限られる。** これは広く言いすぎやすい —— 「auto はスイッチを無視する」は誤りである。`dns_forwarders` が置かれていれば、スイッチは今も決定権を持つ。切ならオペレーターの一覧を保ち、入なら一覧を導出してそれを上書きする。これこそ、スイッチを入れることがこれまで常に意味してきたことである。スイッチが不活性になるのは、「切」を尊重することが、他のすべてが既に失敗したからこそ到達した段に `DefaultForwarders` を書き込むことを意味してしまう、まさにその場合だけである。

その場合には不活性であるほかない。既定が `false` の真偽値であるスイッチは、「意図して切った」を「一度も触っていない」と別に表現できず、両者は同じ保存値だからである。前者を尊重することは後者のために決して発見しないことを意味し、そしてその後者こそ、この機能が存在する理由である機械なのである。経路にローカルのリゾルバを入れたくないオペレーターは、それを直接述べている `recursive` を求めるか、`dns_forwarders` で段の中身を名指しする。回帰テストは `TestAutoDiscoversWithoutTheFlag`、`TestAutoIgnoresTheFlagBeingOffWithNoExplicitList`、`TestAutoStillConsultsTheFlagAgainstAnExplicitList`、`TestAutoLeavesAnExplicitForwarderListAlone`、`TestNonAutoModesDoNotDiscoverWithoutTheFlag` である。

**発見は既定のコード経路に乗ったので、テストにとっての危険物になった。** `auto` は既定のモードなので、ゼロ値の `rolodex.Config` も発見の経路を通り、`ForwarderDiscovery` はその機械の実物の `/run/systemd/resolve/resolv.conf` と `/proc/net/route` にフォールバックする。`Probe` が nil であることは、本物の DNS クエリを意味する。`Manager` を組み立てて `Forwarders` に到達するテストは —— `ProgramRolodex` は呼び出しのたびに到達する —— `ResolvConfPaths`、`RouteTablePath`、`ForwarderProbe` を固定しなければならない。さもなければ、走っているネットワークに黙って依存し、ゲートウェイが解決する機械ならどれでも通ってしまう。`newProgramTestManager` は、呼び出し側が埋めなかった三つを埋める。統合テスト一式が組み立てる `rolodex.Config` については、`pinRolodexDiscovery` が同じことをする。わざと fixture を指させている唯一の場所はローカル転送先の一式で、そこでは発見が見つけるものこそが主題だからだ。モードを `forwarders` の中で読むようにしたことは、それをマネージャのロックの下に置くことでもあった —— `SetResolutionMode` が `cfg.ResolutionMode` をロックなしで書いていたのは、それを読むのが rolodex.yml の生成だけだった頃の話で、設定の書き込みごと、再プログラムの巡回ごとに転送先リストを解決するようになれば、それは競合になる。

発見は `src/rolodex/hostdns.go` にある。`HostResolversFrom` は `hostResolvConfPaths` を順に読む —— `/run/systemd/resolve/resolv.conf` が**先**、次に `/etc/resolv.conf` —— そして使えるアドレスを生む最初のファイルが勝つのであって、単に存在する最初のファイルではない。この順序は荷を負っている。resolved の機械では `/etc/resolv.conf` はスタブ（`127.0.0.53`）であり、ループバックとして捨てられるので、最初の*読める*ファイルで止まる発見は、まさにこの機能が対象とする機械で何も見つけられない。上流のファイルがコンテナの中から到達できるのは、systemcontroller のユニットが `-v /run/systemd:/run/systemd` をバインドマウントするからである。そのマウントを失うと発見は黙って劣化する。ループバック、未指定、マルチキャスト、リンクローカルのアドレスはすべて落とされる —— resolved のスタブや rolodex 自身の `DNSLoopback` の listener へ転送するのは上流ではなく問い合わせのループであり、リンクローカルのアドレスは、`resolv.conf` の行が運ばないゾーンなしでは意味を持たないからである。

**何も見つけられなかった発見は、既に設定されているフォワーダーを保つ。** `Manager.forwarders()` は `Config.Forwarders` へ、次に `DefaultForwarders` へフォールバックするので、スイッチを入にすることでローカルの段が何も指さない状態になることは決してありえない —— それは、置き換えるために入にしたはずの公開の既定値より厳密に悪い。

`main.go` は起動時にこの設定を `rolodex.Config` へ読み込む（保存された値が解析できないときは切として読む —— 安全な向きである）ので、ネットワークを移った機械は、オペレーターが何もせずとも次回の起動で新しいリゾルバを拾う。`POST /settings/set` を通じた変更は `Controller.RefreshDNSLocalForwarders` を走らせる。これは解決のモードとは違い、フラグが変わっていないときにも**短絡しない**。既に入になっている状態では、発見されるアドレスそのものが動いている可能性があり、それが rolodex へ届くのは描き直しによってだからである。`RewriteConfig` はバイト列が実際に変わったかどうかを今も報告するので、同一の描き出しに再起動の費用はかからない。

`GET /dns/status` は **両方**を報告する: `local_forwarders`（オペレーターが求めたもの）と `forwarders`（`rolodex.yml` が実際に持っているもの）である。この二つが食い違うのはちょうど一つの場合 —— 発見が使えるものを何も見つけられず、公開の既定値が保たれた場合 —— であり、それはスイッチが入に見えるのに何も変わっていない唯一の場合なので、フラグだけを見せる UI は、効いていない設定を見せていることになる。設定の画面が実効の一覧を描くのはそのためであり、それが空のときは明示的にそう言う。

**rolodex のイメージはテストと dev ではアーキテクチャ別に pull される** —— make のハーネスはホストのアーキテクチャ別の rc タグ `quay.io/town/rolodex:rc.latest-<arch>`（`<arch>` は生の `uname -m` の形の `x86_64`/`aarch64`）を pull するのであって、アーキテクチャの付かない素の `rc.latest` ではない。Town OS の内部のイメージの pull は既定で rc のチャネルを使うので、ハーネス、dev、実行時のすべてが `rc.latest-<arch>` を追う。rolodex は各ホストからネイティブに push されたアーキテクチャ別のタグを公開する（rolodex-dns のリポジトリの `make push-rc` / `make push-release`）ので、どのアーキテクチャのテストのホストにもマルチアーキテクチャのマニフェストの組み立ては不要である。*素の* `rc.latest`（アーキテクチャの接尾辞なし）は単一アーキテクチャのマニフェストであり、もう一方のアーキテクチャでは `exec format error` でクラッシュループする —— 直接 pull して安全なのは接尾辞の付いた `rc.latest-<arch>` だけである。Makefile は `HOST_ARCH`（`x86_64`/`aarch64` に正規化）を計算し、`ROLODEX_IMAGE_TAG ?= rc.latest-$(HOST_ARCH)` を既定とする。`ROLODEX_IMAGE` はそれから導かれ、環境変数を通じてテスト／dev のコンテナへ注入される。`make ROLODEX_IMAGE_TAG=<tag> ...`（リリース済みの rolodex には `latest-$(HOST_ARCH)` など）や `ROLODEX_IMAGE` の環境変数で上書きする。本番／実行時の挙動も一致する —— `ROLODEX_IMAGE` が設定されていない限り、systemcontroller は自分のリリースのタグからタグを導く（`defaultVersionTag()` を通じて `rc.latest-<arch>` にフォールバックする）。テストと dev のハーネスは常にそれを設定する。dev のコンテナに焼き込まれた rolodex のユニット（`integration/testdata/town-os-system--rolodex.service`）は `@ROLODEX_IMAGE@` のプレースホルダを使い、`integration/testdata/Containerfile.dev` の `ROLODEX_IMAGE` のビルド引数を通じてイメージのビルド時に置換される（引数が空ならビルドは失敗する）ので、焼き込まれたユニットは常にハーネスが読み込むイメージと一致する。

### この機械が**提供する**暗号化トランスポート: DoH は ingress 越し、DoT と DoQ は直結

ここまではすべて*クライアント*としての rolodex である。DoH と DoT は、この機械がインターネットへ出ていく手段として `auto` の段の一覧に現れていた。この節は逆向きの話 —— **LAN のクライアントへ提供する**暗号化 DNS であり、持ち主の違う別のリスナー群である。

三つとも開くのはインストールイメージの `rolodex.yml`（`../install` の `scripts/rolodex-config.sh`）であって、ここではない。rolodex はそれぞれの設定の節が存在することを見て、起動時に一度だけ各リスナーを開く。metrics のリスナーとまったく同じであり、したがってどれも gRPC で後から有効にはできない —— 前述の「rolodex.yml は起動用の設定にすぎない。設定は gRPC で書き込む」の項を参照。

**DoH の前には ingress が立つ。** rolodex は DoH をループバックの `127.0.0.2:4443`（`RolodexDohBackend`）にバインドし、ingress はそれを `dns.<tld>`（`DohHostLabel`）というごく普通の vhost として公開して、その内部のホップだけ検証を飛ばしてプロキシする（`dohIngressRoute`）。したがってクライアントが検証するのは ingress のリーフであり、この機械の CA が専用のリーフレポジトリ（`doh`/`resolver`/`current`）の下で発行するので、再発行が pages や gfeh のリーフを乱すことはない。この仕掛けの意義はそこに尽きる。rolodex は自己署名の証明書で自分の TLS を終端し、検証するどの DoH クライアントもそれを受け入れないが、受け入れてもらう必要は永遠にない —— それが終端しているのは、この機械から一歩も出ないホップだからである。クライアントが使うのは `https://dns.<tld>/dns-query`。プロキシされるのはその一つのパスではなく vhost 全体である。rolodex の DoH ルーターはそのパスだけを提供し、ほかはすべて 404 にするので、ここにパスのマッチャを足しても、パスを間違えられる場所が二つに増えるだけだからである。

`443` ではなく `4443` であることには重みがある。ingress は `0.0.0.0:443` で公開され、rolodex は `--net host` で動くので、一つの名前空間の中でワイルドカードの `:443` と具体的な `127.0.0.2:443` が並べば、後にバインドしたほうが EADDRINUSE になる —— 起動順によって、倒れるのは DNS か ingress のどちらかである。この数字はさらに、互いを読めない二つのリポジトリに書かれている。ここと、`../install` の `doh.bind` である。`TestRolodexDohBackendIsTheLoopbackPortInstallWrites` はこちら側をリテラルに釘づけにし、`TestRolodexDohBackendMatchesTheInstallScript` は両方のリポジトリが取得されている場所ではインストールスクリプトそのものを読み、`../install` の存在しないテストコンテナの中では飛ばす。

**vhost には名前が要り、それを与えられるのは DNS の二つの reconciler だけである。** `dns.<tld>` の背後にはパッケージもページもオブジェクトストレージのパーティションもないので、ほかの何かがこの名前を口にすることは永遠にない。`RebuildDNS` がその A レコードを明示的に公開し（デュアルスタックの機械では AAAA も。リーフがホストの二つのアドレスを SAN に持つからである）、`ReconcileDNS` はそれを望ましい集合に含める。そうしなければ、毎時の孤児レコードの掃除が、起動時に公開したものを一時間後に消してしまう。レコードがなければ、失敗は TLS より前、ingress へ届くより前に起きる —— ただの NXDOMAIN であり、それは作られたことのない機能とまったく同じに読める。回帰テストは `TestRebuildDNSPublishesTheDoHEndpointsName` と `TestReconcileDNSKeepsTheDoHEndpointsName`（ユニット）、`TestDohResolvesThroughTheIngress` と `TestDohEndpointResolvesOnRealDNS`（統合）。

**DoT と DoQ は ingress をまるごと迂回する。** これは設計ではなく既知の穴である。ingress は TCP しか公開せず、その Caddy は `protocols h1 h2` に固定されている（`renderCaddyfile`）ので、HTTP でないプロトコルがそこを通るレイヤ 4 の経路はそもそも存在しない。インストールイメージは両方を LAN 側の `0.0.0.0:853` にバインドする —— 標準の組であり、DoT が TCP、DoQ が UDP/QUIC である（rolodex 自身の DoQ の既定は `8853` だが、それを探すクライアントはいない）。アドレスが具体的なものではなく `0.0.0.0` なのは、これらの節が bind 文字列を**一つ**しか取らないからである。最初のグローバルアドレスを固定すれば、最初の DHCP のリースより前には何も提供せず、二つ目のアドレスでは永久に何も提供しない。`dns.bind` で同じことができないのは、`:53` が podman 自身の DNS リスナーと衝突するからである。`:853` にその衝突はない。代償として、podman のブリッジやオーバーレイを含むすべてのインターフェースにバインドされる。IPv6 は覆われていない —— 一つの bind 文字列で二つのアドレスファミリーを名指すことはできない。

**どちらも、この機体自身の CA が発行したリーフを提供する。** インストールイメージが `dot.tls`／`doq.tls` を `/data/tls/dot/{cert,key}.pem` に向け、`issueRolodexTransportLeaf` が `RebuildDNS` のたびにまさにその組を書く。SAN は DoH の vhost のリーフと同じ、`dns.<tld>` にこの機体の各アドレスを加えたものである。したがって検証する DoT や DoQ のクライアントは、自己署名証明書を渡されてピン留めするのではなく、家庭がすでに信頼している CA に対して検証する。

これが RPC ではなくファイルである理由は、順序にある。rolodex は systemcontroller よりも*先に*起動する —— 名前を解決するものがないうちはコントローラーはイメージを一つも取得できない —— ので、初回の起動では、rolodex が設定を読む時点で CA はまだ存在しない。rolodex は「名指されたが不在の証明書」を「生成したものを提供しつつ、本物を見張る」と解するので、リーフを発行することが**そのまま**引き継ぎになる。30 秒の巡回一回のうちに採用され、再起動は要らず、調整すべきことも何もない。これはまた、rolodex の証明書のホットリロードをこの機体で初めて生きたものにする。生成された材料は意図的に巡回されないので、ファイルを指すリスナーが現れるまで、その仕組み全体は届かないコードだった。

経路が `/data` の下にあるのは、それが rolodex のコンテナに見える唯一のものだからである —— そのユニットは `/town-os/rolodex:/data` だけをマウントし、他は何もしない —— ので、このリーフは他のすべてのリーフと並んで btrfs の TLS サブボリュームに置かれるのではなく、データディレクトリに入る。ここの `RolodexTLSSubdir` と `../install` の `scripts/rolodex-config.sh` の `cert_path` は、一つのマウントを二つのリポジトリに書いた二つの半分である。片方だけを変えれば、rolodex は誰も書かないファイルを見張ることになる。テスト: `TestRolodexTransportLeafLandsInTheMountedDataDir`、`…NamesTheHostAndItsAddresses`、`…IsSignedByTheBoxCA`、`…FollowsAnAddressChange`。

**この機体の CA をあらかじめ信頼していないクライアントでも、証明書を検証できます。** リーフが、rolodex 自身が権威を持つゾーンの中に固定されているからです。`collectRolodexTransportTLSA` は `RebuildDNS` のたびに、`_853._tcp.dns.<tld>` と `_853._udp.dns.<tld>` へ DANE-EE レコード——`3 1 1`、リーフの SPKI をハッシュしたもの——を、パッケージ・ページ・gfeh の固定と並べて公開します。これこそが、この家に一度も加わったことのない端末にとってリーフを発行する意味です：その端末はこのリゾルバーに到達できるので、固定を取得でき、したがって CA を先に導入しなくても手渡されたものを確かめられます。

一枚の証明書に二件のレコードです。TLSA レコードが属するのは証明書ではなくサービスの**エンドポイント**であり、DoT と DoQ はポートとリーフを共有しながらトランスポートだけが異なるからです。片方だけを公開するのは両方公開しないより悪くなります：DANE を確かめるクライアントが自分の選んだトランスポートのレコードを見つけられないと閉じる方向で失敗するので、欠けた側は「この機体の DoQ が壊れている」と読まれ、「この機体には DANE が無い」とは読まれません。そもそもそれを表現するには `rolodex.TLSAEntry` が `Proto` を持つ必要がありました——`_tcp` が直書きされていたため、DoQ の固定は存在しようがなかったのです。空は今も tcp を意味し、それは HTTP を供給するすべてのパッケージです。

リーフがディスク上に現れる前は何も公開しません。`collectGfehTLSA` がリーフ未発行のパーティションを飛ばすのと同じ理由です：まだ存在しない証明書のために固定を公開すると、それがついに現れたときにあらゆるクライアントが接続を拒みます。テスト：`TestRolodexTransportTLSAPinsBothEncryptedDNSEndpoints`、`…PublishesNothingWithoutALeaf`、そして所有者名の組み立てを見る `TestTLSANameProtocol`。エンドツーエンドでは、`TestDotAdoptsTheCAIssuedLeafWithoutARestart` が、rolodex が一回のポーリングのうちに、再起動なしで発行されたリーフへ切り替わることを示します——これは別のプロセスがファイルに気づくという話なので、どの単体テストにも観察できない受け渡しです。そして `TestRolodexPublishesTheDoQPinUnderUDP` が、生きた rolodex が `_853._udp` の下でその固定を保存して返すことを示します。モックのクライアントにはできないことです。

**そして箱がただ動いているあいだにも更新されます。** `IssueLeaf` は有効期限まで 30 日を切った証明書を再発行しますが、それは何かがそれを呼んだときだけであり、呼び手は起動と確認済みの内部 IP 変更だけでした——つまり更新は再起動の副作用でした。`reconcileRolodexTransportTLSA` はそれを毎時のドリフト修復パス（`ReconcileDNS`。まさにこのために CA と btrfs のベースを持つようになりました）に載せ、固定も同じ呼び出しの中で調停されます：再発行されたリーフは新しい鍵を持つので、古いものについて公開された関連データはファイルが書かれた瞬間に一致しなくなり、TLSA レコードを見つけて一つも一致しない DANE クライアントは接続を拒否します。新しい固定は古いものが降ろされる**前**に上がります——rolodex は 30 秒のポーリング一回のうちに更新後の証明書を採用し、両方のレコードが在ることだけが、どちらの証明書でも検証が通る唯一の状態だからです。同じパスは、ゾーンが失った固定（初期化し直された rolodex、ゾーンごと持って行った撤去）を更新を待たずに再公開し、証明書が有効なあいだは何も書き換えません。

**クライアントには、これがどこにあるかが二通りで伝えられる。** `publishDDRDesignation` は `_dns.resolver.arpa.` に SVCB レコードを置き（RFC 9462）、`dns.<tld>` のもとで DoH の URL、DoT のポート、DoQ のポートをその優先順で名指す —— DoH が先なのは、`:443` が `:853` を濾す DPI を生き延びるからである。DDR を解するクライアントは、その一つの名前をこの機体に対して解決すればよく、ほかに何も告げられる必要がない。この指定は `arpa.` に住み、rolodex がそれを上流へ解決しに行くことは決してない。そこが要点である。拒否はあらゆるローカルの照会の下に位置するので、あるリゾルバー自身の指定に答えられるのは、尋ねられているそのリゾルバーだけである。

ここの他のどの公開者とも違って、これは書き直す前に取り除かれる。`RebuildDNS` はまずその TLD のゾーンを取り壊すことから始まり、それが他を冪等にしている —— しかし `_dns.resolver.arpa.` はこの機体が持つどのゾーンにも属さないので、取り壊しはそこへ届かない。その除去が無ければ、再構築のたびにもう一部が積み上がるか、重複の失敗がログに出る。先に取り除くことは、TLD の変更を両方を広告するのではなく実際に効かせることでもある。テスト: `TestDDRDesignationIsIdempotentAcrossRebuilds`、`TestDDRDesignationFollowsATLDChange`。

**この指定には SVCB を知っている rolodex が要る。** すなわち `v0.6.0` 以降である —— このレコード型はそこで加わり、go.mod の `rolodex-dns/go` のピンもそれに合わせて動いた。それより古いサーバーはこのレコードを拒否し、`publishDDRDesignation` はそれを debug で記録して先へ進む。その結果、機械は何にも発見されない暗号化 DNS を提供する。動いてはいるが、見えない。往復を mock ではなく**実物の** rolodex に対して確かめている（`TestDDRDesignationRoundTripsThroughRealRolodex`）のはそのためである。mock は渡されたレコード型を何であれ受け取ってしまう。

**そしてもう一度、人が読める形で。** `GET /dns/status` は `encrypted` というオブジェクトを載せる —— DoH の URL、DoT／DoQ の名前とポート、そして探索用の名前 —— DNS の画面はそれを読み取り専用で描く。これらの接続先は TLD とイングレスから導かれるので、そこに設定するものは何も無い。欠けていたのは、それらを*見る*手立てのほうだった。DoH、DoT、DoQ をしばらく前から提供していながら、それがどこにあるかを誰にも告げるものが無かった機体で。公開される DoH のポートはイングレスの `443` であって、rolodex の内部の `4443` では決してない。`4443` へ送られたクライアントは、ループバックだけのリスナーに、自分が拒む証明書を持って行き当たることになる。

**DoT にはテストが一つあり、DoQ には一つもない。** `TestDotServesDNSOverTLS` は `dot:` の節を持つ本物の rolodex を起動し、それに対して RFC 7858 のやり取りを一度成立させる —— インストールイメージが書くその節が、ぶら下がるだけのソケットではなく、実際にそのプロトコルを話すリスナーを生むことの確認である。バインドするのは本番の `0.0.0.0:853` ではなくループバックの一時ポートである。`--net host` の下では、ここでのよく知られたポートは*ホストの*ポートであり、二つの `make test-full` がそこで衝突するからである。DoQ は依然としてテストされていない —— QUIC のクライアントはこのリポジトリが持たない依存であり、いずれそれをテストするものにも同じポートの規則がかかる。

### TLD の変更は暗号化 DNS も連れていかなければならない

暗号化 DNS は四つの場所で同時に TLD の名を負っており、そのどれもが別々の公開者である。

1. DoH のエンドポイントのアドレスレコード（`publishDohRecord`）、
2. それを告知する DDR の指定（`publishDDRDesignation`）、
3. DoT と DoQ が出す証明書の SAN（`issueRolodexTransportLeaf`）、
4. その証明書の DANE のピン（`collectRolodexTransportTLSA`）。

`rolodex.ChangeTLD` が動かすのはゾーンとパッケージのレコードであって、この四つは一つも動かさない。したがって `POST /dns/tld` は、機械の名を `home` から `lan` へ変えたあとも `dns.home` を告知しつづけ、`dns.lan` に対して `dns.home` と書かれた証明書を差し出し、クライアントがこれから実際にダイヤルする名前にはピンを一つも公開しなかった。

**最後のそれは、格下げではなく故障である。** DANE を検査するクライアントは、あるエンドポイントに到達してそれに対する TLSA レコードが見つからなければ、その接続を拒否する —— DANE はそのためにある。だから障害は「暗号化 DNS が検証されていない」ではなく「暗号化 DNS が動かなくなった」であり、次の起動の `RebuildDNS` がすべてを公開し直すまで、その状態が続く。月に一度しか再起動しない機械にとって、それは一か月である。

この四つは今や一つの呼び出し `publishEncryptedDNS` であり、二つの呼び出し側 —— `RebuildDNS` と TLD の変更 —— がどちらもそれを使う。一つの関数にまとめられているのは、呼び出し地点でそれらが結び付いていることが何も見えなかったからにほかならない。どれもが呼び出し側を一つだけ持つ自前の公開者として書かれ、二つ目の呼び出し側は四つとも忘れた。`retireEncryptedDNS` はピンの側から見たその逆である。古い TLD のレコードは `ChangeTLD` の中でゾーンごと倒れるが、ゾーンを残したままの改名は、もう誰も提供していない証明書のピンを取り残す。それは同じ「閉じて失敗する」を反対側から起こしたものである。

`POST /dns/tld` は今では `reprogramIngress` も呼ぶ。理由は同じで、vhost の名前には TLD が埋まっており、DoH のエンドポイントのものもその一つで、さもなければ ingress は再起動まで古い名前で応じつづける。

回帰テストは `TestTLDChangeRepublishesTheEncryptedDNSEndpoints` と `TestTLDChangeLeavesNoPinAtTheOldName`（ユニット）、`TestTLDChangeMovesTheEncryptedEndpointsOnRealDNS`（統合。実物の rolodex とその動いているリゾルバに対して）である。

### ネットワークの TLD、デュアルホーム、スプリットホライズンの解決

すべてのネットワークは TLD を所有し、`home_domain` がその TLD であるネットワークのスコープとして rolodex に登録される（`rolodex.EnsureNetworkScope`。`controller_networks_reconcile.go` の `applyNetworkTransport` から呼ばれる）。TLD を所有することが、それを**区切る**ものである。rolodex は、*別の*スコープに加わっている WireGuard の peer からは、そのスコープの TLD を隠す。既定／home のネットワーク（`account.DefaultNetworkName`。TLD は `dns_tld` の設定から、既定は `home`）は `home.` を **DNS だけの**スコープとして所有する —— WireGuard のインターフェイスも、オーバーレイのサブネットも、peer の関連付けも得ないので、home のスコープに送信元 IP が結び付けられることは決してない。したがって `.home` は LAN だけのものであり、すべての WireGuard の peer からは隠されるが、LAN の上では完全に解決できる。

**デュアルホーム。** 既定でないネットワークへインストールされたパッケージは二度公開される（`registerScopedPackageDNS`）:

- ネットワークの TLD の下、この機械の**オーバーレイの IP** における**スコープ付き**の A レコード —— 送信元 IP によって WireGuard のオーバーレイの peer へ提供される（`AddScopedRecord`）。そして
- 同じ FQDN について、この機械の **LAN の IP** における**グローバル**な A レコード（`RegisterPackageDNS`）—— ループバック／LAN のクライアントへ提供される。

どちらの側も、実際に経路のあるアドレスを受け取る。ネットワークの TLD についてグローバルな権威のゾーンは公開されない。裸のグローバルな A レコードはゾーン無しで LAN 上で解決され、rolodex の **LAN → 所有スコープのフォールバック**（rolodex-dns、解決の手順 5）はスコープの所有する TLD を LAN の送信元にとって権威あるものとして扱う —— したがってネットワークの TLD の下で一致しない名前は、私的な TLD を上流へ漏らすのではなく、LAN からの権威ある NXDOMAIN を返す。既定のネットワークのパッケージはグローバルな home のゾーンだけにとどまる（`registerPackageDNS`）。既定でないパッケージがそこに現れては決してならない（元の「`.home` として解決される」というバグである）。

**スプリットホライズンの要約。** LAN のクライアント（WireGuard 無し）は**すべての**ネットワークの TLD（`.home` と、すべての WireGuard のネットワークの TLD）に加えて公共のインターネットを解決する。一つのネットワークに加わった WireGuard の peer は、そのネットワークの TLD **だけ**と公共のインターネットを解決する —— 兄弟のネットワークの TLD も `.home` も NXDOMAIN を返す。LAN の眺めが区切られることは決してない。区切られるのはオーバーレイの peer だけである。`RebuildNetworkDNS`（`reconcile.go`。起動時に呼ばれる）は、既定でない各ネットワークのパッケージの LAN に面するグローバルなレコードを登録し直すので、既にインストールされているパッケージは再起動の後も LAN 上で解決され続ける。スコープ付きのレコードはそれとは独立に rolodex に残る。起動時のネットワークの reconcile には rolodex のクライアントが渡されるので、冷たい起動でも home のスコープ（およびすべてのネットワークのスコープ）が確立される。

### パッケージの FQDN は一つの文字列である —— A レコード、リーフの SAN、TLSA の所有者、ingress の vhost

**パッケージの DNS の名前は常にその*インストールのネットワーク*の TLD から導かれ、決してグローバルな `dns_tld` の設定からではない。** `packageFQDN(repo, name, tld)`（`src/svc/systemcontroller/controller_tls.go`）が唯一の真実の出所であり、TLD は `networkTLDValue(nm, settingsMgr, network)` から来る（これは既定のネットワークについてのみ `dns_tld` にフォールバックする）。四つのものが同一にパッケージを名指ししなければならず、そのどれか一つでも食い違えば、提供は黙って壊れる:

1. その **A レコード**、2. その**リーフの証明書の SAN**、3. その **DANE の TLSA の所有者**、
4. その**共有の :443 の ingress の vhost**。

**三つの公開者はすべて、その名前を一つの検証器を通して組み立てる。** パッケージ、page、オブジェクトストレージの区画は、それぞれネットワークの TLD の下で名前を得るが、以前はそれぞれが自分で組み立てていた —— 合法な名前が何かについて食い違いながら。`gfehFQDN` はラベルを正規化し、ドットで区切られた各要素を厳格な LDH の規則に照らして検証し、修飾すると 253 文字の上限を超える名前を拒んでいた。`packageFQDN` はどちらの検査も無い素の連結であり、`pageFQDN` は前後の除去より先は何も検査していなかった。今や `qualifyPublishedName`（`src/svc/systemcontroller/published_name.go`）が唯一の組み立て役であり、gfeh の規則を三つすべてに適用する。`validatePublishedName` は、検査は必要だが組み立ては不要な名前のための、修飾しない側の半分である。失敗した名前は**捨てられる** —— どの収集器も既に空の FQDN を飛ばすので、四つのすべてに対して壊れたものを寄与するのではなく、レコードも経路も証明書もディレクトリも寄与しない —— そしてその拒否は **Error** でログに出る。`LOG_LEVEL` の既定が `error` であり、黙って解決されなくなったサービスが、ログの水準を上げて初めて見つかるようなものであってはならないからである。

**page のドメインは、組み立ての時だけでなく API でも検証される。** page にとって、その名前は*五つ目*のものでもある。ディスク上のサブボリュームと webroot のシンボリックリンクである。pages の Caddy は `/srv/<host>` を根とするからである。`ValidatePageDomain` は `POST /pages/create` と `POST /pages/update` の両方で走り、400 を返す。効いてきたのは update の経路である。create は付随的に覆われていた。`CreateFilesystem` が `storage.ValidateFilesystemName` を走らせ、ハンドラはシンボリックリンクのコードに達する前に巻き戻すからである。一方 `migratePageDir` は `RenameFilesystem` の失敗をログに出しつつ、構わず `RemovePageSymlink` / `EnsurePageSymlink` へ進む。

込み入っているのは、**公開の FQDN は修飾からは免除されるが、検証からは免除されない**という点である。`isPublicFQDN` は、TLD で終わらないドット付きの名前をすべてオペレーター自身のドメインと読み、ACME を通じてそのまま提供されるべきものとする —— それは `blog.example.com` については正しく、同時に `../escape.example.com`、`site.example.com/../../etc`、`site.example.com other.example.com` が調べられないまま `filepath.Join` と Caddyfile へ届いた道でもある。「それはオペレーターのドメインである」は、それをこの機械の TLD の下で組み立てない理由にはなるが、それを検査しない理由には決してならない。

これらがずれないよう、FQDN は**一度だけ** —— リーフを発行するのと同じ行、`applyPackageTLS` の中で —— 計算され、`PackageNetworkState.FQDN`（パッケージごとのネットワークの状態の JSON の `fqdn`）として永続化される。ingress の経路の組み立て（`collectPackageIngressSites`）は名前を組み立て直すのではなくそのフィールドを読むので、vhost は構成上、証明書が有効である名前になる。`reconcileWriteNetworkState` は TLD を**呼び出し側から**取る（`reconcilePackage` であり、それはインストールのネットワークから解決している）。自分で `reconcileDNSTLD` を呼んでは決してならない。そうしていたのは本物のバグだった。起動のたびに `fart` のネットワークのパッケージのリーフを SAN `<pkg>.<repo>.home` で発行し直し、正しい `.fart` の SAN を潰す一方で、ingress は誰も接続しない `<pkg>.<repo>.home` の vhost を描いていた —— そのパッケージは LAN 上で解決されるのに、決して提供されなかった。空の `fqdn`（アップグレード前の状態ファイル、あるいは HTTP でないパッケージ）はグローバルな TLD にフォールバックし、次の reconcile で自ら癒える。

**ingress はインターフェイスに依らず、ネットワークごとのバインドを必要としない。** `-p 443:443` / `-p 80:80` をホストの IP なし（`0.0.0.0` なので LAN + WireGuard + ループバックのすべてが到達する）で公開し、その Caddyfile には **`bind` の指示が無い**ので、Caddy はすべてのインターフェイスで listen し、vhost は純粋に **SNI/Host** で選ぶ。バックエンドへは共有の `town-os-ingress` の podman のネットワーク上のコンテナ名で到達する。HTTP を前に置くすべてのパッケージは、自分の WireGuard のネットワークに関わらずそこへ加わる。したがって LAN のクライアントとオーバーレイの peer は同じ listener に当たり、SNI で同じ vhost を選び、同じローカル CA のリーフを得て、同じコンテナへプロキシされる。オーバーレイの IP に listen するソケットを結び付けるものは何も無い —— `BindOverlayAddress` は rolodex の *DNS のスコープの関連付け*であって、ソケットのバインドではない。ingress に `bind` の指示やネットワークごとの listener を追加しないこと。

パッケージのリーフは、そのネットワークにおけるこの機械の**オーバーレイの IP** も SAN として運ぶ（`networkOverlayIPValue`）ので、peer は名前だけでなく生の WireGuard のアドレス（`https://10.65.0.1`）でもパッケージへ到達できる。既定のネットワーク（WireGuard の輸送を持たない）では空であり、それが既定のネットワークのリーフを reconcile のたびにかき回さずに済ませている。

ネットワークのパッケージの DANE の TLSA は、**その A レコードと同じくデュアルホームである**。`RebuildNetworkDNS` はグローバルなピン（LAN → 所有スコープのフォールバックを通じて LAN の送信元へ提供される）*と*スコープ付きのピン（グローバルなレコードを決して見ないオーバーレイの peer へ提供される）を登録する。インストールだけではスコープ付きの半分しか書かれず、しかも再起動をまたいでどちらの半分も公開し直されていなかった。

### page もネットワークにスコープされる

page は `network` を運び（`PageSite.Network` の列。`""` は既定／home のネットワークを意味し、パッケージにおける `Installer.LoadNetwork` と同じ約束事である）、**パッケージとまったく同じ扱い**を受ける。その名前はそのネットワークの TLD から来て、デュアルホームであり（スコープ付きのオーバーレイのレコード + グローバルな LAN のレコード）、そのリーフはネットワークの FQDN とこの機械のオーバーレイの IP を運び、その DANE の TLSA はネットワークの TLD の下に（グローバルとスコープ付きで）固定され、*他の*すべてのネットワークの peer からは隠される。`pageFQDN`（`pages_tls.go`）は `packageFQDN` の page 側の双子であり、`pageNetworkTLD` は `networkTLDValue` の双子である。

page 特有のひねりは、page の FQDN が**ディスク上の btrfs のサブボリュームと webroot のシンボリックリンクの名前でもある**ことである（pages の Caddy は `/srv/<host>` を根とする）。したがって FQDN は単なるラベルではない —— 間違えれば、内容が ingress の提供する名前の下から動いてしまう。帰結は三つある:

- `reconcilePages` は `valid` の集合を `pageFQDN` で組み立てる。その集合が `pruneStalePageSymlinks` を動かすからである —— そこで `fart` の page を `blog.home` と名付ければ、その本物の `blog.fart` のディレクトリを取り逃がし、*しかも*生きているシンボリックリンクを刈ってしまう。
- page の**ネットワーク**を変えると、そのサブボリューム／シンボリックリンクの名前が変わる（`migratePageDir`）。既定のネットワークの page について `dns_tld` の変更がするのとまったく同じである。
- `migratePageDirsForTLD`（`dns_tld` の変更のハンドラ）は**既定でないネットワークの page を飛ばす** —— それらはグローバルな TLD の下で名付けられていないので、名前を変えれば動いていた page を壊してしまう。

page は今も、ingress の後ろの単一の共有された `town-os-system--pages` のコンテナが提供する。ネットワークは名前付け／DNS／証明書だけの関心事であり、ネットワークごとのコンテナや podman の配管は無い。

### DNS API

- `GET /dns/status`（認証が必要）-- 有効のフラグ、動作の状態、TLD、レコード数、`local_forwarders`（フォワーダーの一覧がホスト自身のリゾルバから取られているか）、`forwarders`（`rolodex.yml` が実際に持っているアドレス。[ローカルのフォワーダー](#ローカルのフォワーダー)を参照）を含む DNS の状態を返す。
- `GET /dns/records`（認証が必要）-- すべての DNS のレコードを一覧する。
- `POST /dns/records/add`（管理者が必要）-- DNS のレコードを追加する。名前、レコードの型、値、TTL を受け取る。
- `POST /dns/records/remove`（管理者が必要）-- 名前と型で DNS のレコードを取り除く。
- `GET /dns/tld`（認証が必要）-- 現在のトップレベルドメインを取得する。
- `POST /dns/tld`（管理者が必要）-- TLD を設定する。既存の TLD を変え、インストール済みのすべてのパッケージを登録し直し、新しい名前の下に暗号化 DNS のエンドポイントを公開し直し、vhost が追随するよう ingress を再度書き込む。「TLD の変更は暗号化 DNS も連れていかなければならない」の項を参照。
- `POST /dns/setup`（管理者が必要）-- DNS を初期化し、インストール済みのすべてのパッケージを登録する。
- `GET /dns/dnsbl`（認証が必要）-- DNSBL（ドメインのブロックリスト、正引きの名前）の設定を取得する。全体の有効のフラグ、**実効のものへ解決された**拒否コードを伴う提供者のゾーン、一覧全体の `refusal_cooldown_secs`、そして `rotated_out`（問い合わせを拒否したあと現在待機に入っている提供者。そのコードと残りの秒数を伴う）である。[拒否コード](#拒否コード-提供者のもう聞くなはこれは登録されているではない)を参照。
- `POST /dns/dnsbl`（管理者が必要）-- DNSBL の設定を置き換える。有効のフラグ、一覧全体の `refusal_cooldown_secs`、そして `{zone, enabled, refusal_codes, refusal_cooldown_secs}` の提供者の一覧を受け取る。ゾーンは完全修飾のホスト名として検証され、小文字化され、前後を除去され、重複を除かれる。拒否コードは `ValidateRefusalCodes` によって検証される（IPv4 のアドレスまたは `address/prefix`。プレフィックスへマスクされる。`"none"` は単独の項目としてのみ。重複は不可）。
- `GET /dns/rbl/local`（認証が必要）-- ローカルのブロックリストの項目（`{name, reason}`）を一覧する。この経路は歴史的な `rbl` の綴りを保っている。UI との公開された取り決めであり、項目そのものは変わっていない —— 消えたのは、かつてこの接頭辞を共有していた RBL の*提供者*への参照だけである。
- `POST /dns/rbl/local/add`（管理者が必要）-- ローカルのブロックリストの項目を追加する。名前（ドメインまたは IP）と任意の理由を受け取る。名前は（ドメインまたは IP として）検証され、小文字化され、前後を除去される。
- `POST /dns/rbl/local/remove`（管理者が必要）-- 名前でローカルのブロックリストの項目を取り除く。
- `GET /dns/dnsbl/allowlist`（認証が必要）-- DNSBL の許可リストの項目（`{name, reason}`）を一覧する。
- `POST /dns/dnsbl/allowlist/add`（管理者が必要）-- 名前に基づくブロックリストの検査から、ある名前を免除する。名前と任意の理由を受け取る。名前は小文字化され、前後を除去され、**ドメイン名としてのみ**検証される（`ValidateDnsblAllowlistName`）—— IP のリテラルは拒否される。この許可リストは名前とその下位のドメインに一致するものであり、アドレスに一致することは決してありえないからである。
- `POST /dns/dnsbl/allowlist/remove`（管理者が必要）-- 名前で許可リストの項目を取り除く。名前は正規化されるが検証はやり直されないので、検証の変更より前からある項目も取り除ける。
- `GET /dns/services`（認証が必要）-- インストール済みのパッケージのサービスを、その公開（DNS のゾーンにあるか）の状態とともに一覧する（`{repo, name, version, fqdn, domains, published}`）。repo／name で重複を除く。
- `POST /dns/services/set`（管理者が必要）-- DNS のゾーンにおいてパッケージのサービスを公開する、または公開をやめる。`{repo, name, published}` を受け取る。その選択を永続化し、直ちにレコードを登録／登録解除する。

DNS の読み取り専用のエンドポイント（`/dns/status`、`/dns/records`、`/dns/rbl/local`、`/dns/dnsbl/allowlist`、`/dns/services`、`GET /dns/tld`、`GET /dns/dnsbl`）は監査のログから除外される。許可リストの*書き込み*は監査される（すべてのブロックリストからある名前を免除することは、説明責任のある変更である）。それらが鏡写しにしているブロックリストの書き込みと同じく、`account.RouteActions` に名前の付いた操作は持たない —— パスがそれらを識別する。

### ブロックリスト

Rolodex（0.2.4 以降）は、迷惑メール／マルウェア／広告を遮断する二つの補い合う仕組みを提供し、加えて（0.4.3 以降）それらを取り消すための仕組みが一つと、問い合わせを拒否した提供者を信じないための仕組みが一つある。すべて DNS の API と `rolodex.Client` のラッパー（`SetDnsblConfig`/`GetDnsblConfig`、`AddLocalBlocklistEntry`/`RemoveLocalBlocklistEntry`/`ListLocalBlocklistEntries`、`AddDnsblAllowlistEntry`/`RemoveDnsblAllowlistEntry`/`ListDnsblAllowlistEntries`）を通じて公開される。これらはすべて **rolodex が必要に応じて問い合わせる** —— Town OS がブロックリストのフィードをダウンロードしたり、解析したり、あらかじめキャッシュしたりすることは決してない。

**RBL の側は退役した。** 逆引き IP の提供者への参照、`SetRblConfig`/`GetRblConfig`、そして `GET`/`POST /dns/rbl` のエンドポイントはすべて無くなった。rolodex は `src/rbl.rs` を `src/dnsbl.rs` へ改名し、名前に基づく検査だけを残した。その改名を生き延びたのは*ローカルの*ブロックリストであり、その HTTP の経路が今も `/dns/rbl/local*` と読めるのはそれが公開された取り決めだからである。もう一つは `dns_rbl_config` という設定の鍵で、今やそれを読むものは何も無く、`controller_dns_blocklists.go` がそれを名指すのは、その鍵が別のものに使い回されないようにするためだけである。

ラッパーの `SetDnsblConfig` は、一覧全体の拒否のクールダウンを末尾の引数として取る（`SetDnsblConfig(ctx, enabled, providers, refusalCooldownSecs)`）。これは上流の `SetDnsblConfigWithRefusalCooldown` へ対応付けられる。引数の数を保った上流の綴りは、内部のラッパーには不要な外部 API の互換性のために存在するからである。

- **DNSBL**（ドメインのブロックリスト）-- 引かれたドメインをゾーンの前に付けて必要に応じて問い合わせる、ドメインのブロックリストのゾーンである（例: `googleadservices.com` + `dbl.spamhaus.org`）。DNSBL の登録は、転送／反復による答えに優先する。`/dns/dnsbl` を通じて、`{zone, enabled, refusal_codes, refusal_cooldown_secs}` の提供者の一覧に、全体の有効のフラグと一覧全体の `refusal_cooldown_secs` を加えた形で設定される。
- **ローカルのブロックリストの項目** -- `/dns/rbl/local*` を通じて手作業で管理される、DB に裏打ちされた名前／IP の一覧であり、外部の提供者より先に検査される。**ドメイン名**のローカルの項目は、そのドメインの正引きの A/AAAA の参照を `NXDOMAIN` で遮断し、直ちに効き始める（rolodex は追加時にメモリ上のキャッシュを更新する）。
- **DNSBL の許可リスト**（rolodex 0.4.3 以降）-- 第三者のフィードの誤検知からのオペレーターの抜け道であり、`/dns/dnsbl/allowlist*` を通じて管理される。項目はその名前**とその下のすべての名前**を覆うので、`vendor.example` を許可リストに入れれば `cdn.vendor.example` も免除される。これは**名前に基づく検査の全体を短絡する**もので、設定された DNSBL の提供者にも、一致するローカルのブロックリストの項目にも勝ち、提供者への参照の*前*に走るので、免除された名前が参照を発することは決してない。これも DB に裏打ちされ、メモリ上のキャッシュを持つので、直ちに効く。

  これが無いと、家庭に必要な名前を載せてしまったフィードに対する唯一の手当ては、その提供者を丸ごと無効にすることだけになる。ローカルのブロックリストとの非対称性に注意すること。許可リストの項目は**名前だけ**であり、決して IP ではない。それが短絡する検査が名前に基づくものだからである —— IP に基づく RBL の側が退役した今、提供者への参照はその一種類しかない。

  **バージョンの下限:** 古い rolodex は三つの許可リストの RPC に gRPC の `Unimplemented` で答え、それは 500 として現れる。`make test` もモック化された統合テストもそれを捕まえない —— 固定されたイメージが十分に新しいことを証明するのは `TestRolodexDnsblAllowlistRoundtripReal` である。

#### 拒否コード: 提供者の「もう聞くな」は「これは登録されている」ではない

DNSxL は、登録と、問い合わせた側への苦情とを**同じ種類のレコード** —— `127.0.0.0/8` の下の `A` —— で答えるので、その二つを分けるのはアドレスだけである。`127.0.0.2` はその名前が登録されていることを意味し、`127.255.255.254` は問い合わせが公開のリゾルバ経由で届いたことを、`127.255.255.255` は問い合わせた側が上限を超えていることを意味する。二つ目の種類を登録として読めば、その提供者に対して検査される**すべて**の名前が `NXDOMAIN` になる。ブロックリストはブロックリストであることをやめ、障害になる。Spamhaus は家庭の機械が気づかずに越えうる無償利用の上限を公開しており、越えたときの症状はウェブ全体が暗くなることである —— それは利用制限ではなく壊れた DNS のように読める。

Rolodex はこれらのコードを認識し、拒否を受けたときはそれを信じるのではなく、**その提供者をクールダウンの間だけ参照の巡回から外す**。Town OS はその両方の半分を公開する:

- **`refusal_codes`**。提供者ごとに、DNSBL の提供者の一覧について。各項目は IPv4 のアドレスまたは `address/prefix` である —— プレフィックスなのは、提供者が範囲全体を文書化しているからであり、Spamhaus は `127.255.255.0/24` の全体をエラーのために予約して時とともにコードを増やすので、今日の三つを列挙すれば明日の四つ目を黙って登録として読み始めてしまうからである。
- **`refusal_cooldown_secs`**。提供者ごと、および一覧全体。提供者の `0` は一覧の値に委ね、一覧の `0` は rolodex の組み込みの既定（3600）を使う。
- **`rotated_out`**。`GET` に含まれ、現在どの提供者が問い合わせられていないか、それぞれが何のコードで拒否したか、残りの秒数を報告する。これがオペレーターに見える側の半分である。これが無いと、ブロックリストが参照されなくなったことを示す唯一の合図は、それが物を遮断しなくなったことだけになる。

**`ValidateRefusalCodes`（`controller_dns_validate.go`）は rolodex の `resolve_refusal_codes` をそのまま鏡写しにする。** 一覧はそのまま通されるので、項目の意味について食い違うのは、まったく検証しないより悪いからである。三つの場合がある:

- **空** ⇒ rolodex が自分の組み込みの集合を差し込むので、こうしたものが何も存在しなかった頃に書かれた設定も、編集されることなく安全な読みを得る。
- **ちょうど `"none"`** ⇒ 検出を切る。本物の登録が組み込みのコードと衝突する私的なブロックリストのためである。
- **それ以外** ⇒ ちょうどそれらのコードであり、組み込みのものは意図的に**混ぜられない**。

本物のコードと混ざった `"none"` は拒否される —— 検出を切ると同時に検出すべきコードを名指しする一覧には、選ぶべき読みが無い。コードはそのプレフィックスへマスクされ、**`/32` は裸で描かれる**。rolodex の `Display` に合わせるためである。たった今送ったものと違う形で読み返されるコードは、機械がオペレーターの入力を書き換えたように見えてしまう。

**`GET` はコードを解決済みで報告する**ので、何も名指ししなかった提供者は組み込みの集合を運んで読み返される —— それこそが要点である。オペレーターは、機械が実際に何に照合しているかを見られなければならない。それはまた、**クライアントが次の保存でそれをそのまま送り返しては決してならない**ことも意味する。そうすれば今日の一覧が保存された設定へ凍結され、あとから rolodex が追加するコードが登録として読まれ始める —— これが防ぐために存在するまさにその失敗が、一層上で再導入されるのである。`BlocklistsTab.jsx` の `toWire` は、解決された組み込みの集合を、フィールドが無い状態へ畳み戻す。UI が組み込みの一覧の写し（`BUILTIN_REFUSAL_CODES`）を持つのはただ一つの目的のためである。設定のダイアログがどのラジオボタンで開くかを決めることである。その写しがずれたら、ダイアログは実効のコードを先に埋めた「Custom」で開く —— 見た目の上での誤った既定であって、誤った設定ではない。オペレーターが保存しない限り何も変わらないからである。

**バージョンの下限:** 拒否の扱いより前の rolodex はこれらのフィールドを受け取り —— proto3 は未知のフィールドを無視する —— 何も保存しない。モック化されたテストはそれを成功と区別できない。モックは渡されたものをそのまま返すからである。`TestRolodexDnsblRefusalCodesRoundtripReal` は、**空の**設定された一覧が*解決済み*で読み返されることを主張する。古いイメージが落ちるのはその主張である。

フィードの取り込みや事前キャッシュは**無い**。提供者のゾーンが設定の単位であり、UI はよく知られた DNSBL のゾーンの精選された一覧をワンクリックの追加として提供するが、ユーザーは任意のゾーンを追加してよい。提供者のゾーンの書き込みは設定全体を置き換える（検証され、小文字化され、重複が除かれる）。

**ワンクリックの追加の一覧は推薦であり、その前提で精選されている**（`ui/src/routes/dns/BlocklistsTab.jsx` の `DNSBL_SUGGESTIONS`）。ゾーンがそこに載るのは、家庭の機械がそのままで使える場合だけである。今も運用されており、無償であり、登録の手順なしに自分で再帰するリゾルバに答えること、である。現在は Spamhaus DBL、SURBL、URIBL、NordSpam DBL、Spam Eating Monkey である。

三つは意図的に**入っていない**。`TestBlocklistsTab` の「廃止されたゾーンや登録の要るゾーンを提供しない」という場合が、それをそのまま保つ。`dnsbl.sorbs.net` は 2024-06-05 に廃止されそのゾーンは空にされたので、保護のように読める恒久的な無動作である。`b.barracudacentral.org` は問い合わせる IP をあらかじめ登録する必要があり、登録されていない機械はしばらく答えてもらえたあと切られうる。UCEPROTECT の水準 2／3 は AS 全体を登録するので、悪い隣人が一つあれば ISP 全体が遮断される。三つとも*黙って*失敗する —— オペレーターには設定されたゾーンが見え、それが働いていると思い込む。

また、DNSBL（ドメイン）のゾーンは広告や追跡ではなくメールの中の迷惑 URL に合わせて調整されていることにも注意すること —— 広告／追跡の遮断はフィードの領域であり、それは[意図的に対象外](#ブロックリスト)である。かつてそれらを補っていた逆引き IP のゾーンは RBL の側とともに消えたが、ブラウジングにとってはもともとほとんど関係がなかった。それらは逆引き DNS の問い合わせで見つかった IP についてしか参照されず、普通のブラウジングはそれをほとんど生まないからである。

### サービスごとの DNS の公開

公開はオプトアウトである。インストール済みのパッケージのサービスは、その `repo/name` の鍵が `dns_excluded_services` の設定（JSON の配列）に載っていない限り、すべて DNS のゾーンで公開される。`/dns/services/set` が所属を切り替え、直ちにレコードを登録／登録解除する。`RebuildDNS` と `ReconcileDNS` は除外されたサービスを絞り込む（`filterExcludedDNSInfo` + `loadDNSExcludedServices` を通じて）ので、その選択は再起動と reconcile を生き延びる。公開されていないサービスは動き続けるが、名前では解決できない。

### DNS の管理の UI

DNS の管理の画面は、深いリンクを張れる四つの下位タブ（`?tab=`）の上に DNS の状態（有効、動作中、TLD、レコード数）を示す:

- **Records** -- DNS のレコードのテーブルと、レコードの追加（型: A、AAAA、CNAME、MX、TXT、SRV、PTR）、レコードの削除、TLD の変更、DNS の初期設定のためのダイアログ。
- **Blocklists** -- DNSBL の提供者のゾーンの区画（全体の有効のスイッチ、ゾーンごとの有効化／削除、ゾーンごとの拒否コードの設定、推奨ゾーンのワンクリック追加、任意のゾーンの追加 —— すべて必要に応じて問い合わせられる）に加えて、手作業のローカルの項目のテーブル（追加／削除）。各区画は、問い合わせを拒否して現在待機に入っている提供者があるとき、それを先頭に示す。フィードも、適用も、キャッシュも無い。
- **Allow Lists**（`?tab=allowlists`、`ui/src/routes/dns/AllowListsTab.jsx`）-- DNSBL の許可リスト。免除されたドメインを理由とともに並べたテーブルと、追加と削除である。読み取りは `requireAuth` なのでこのタブは管理者専用ではない。追加／削除の操作は管理者専用である。これが Blocklists の中のカードではなく兄弟のタブなのは、免除とは、何かに到達できないときにオペレーターが名前で探しに行くものであって、提供者のゾーンをスクロールしながら見つけるものではないからである。
- **Services** -- インストール済みのパッケージのサービスと、公開のスイッチ（DNS のゾーンでの公開／非公開）。
## ステータスのエンドポイント

`GET /status/ping`（公開）はシステムの状態を返す。含まれるもの: ファイルシステムの数（ユーザー、インストール済み、アンインストール済み）、リポジトリとパッケージの数、インストール済みのパッケージの数、アカウントと管理者の数、サービスのユニットの数（合計、active、failed）、システムサービスのユニットの数（合計、active、failed）、直近の監査のエラー（過去 5 分）、設定の状態（`needs_setup` は有効な管理者のアカウントが一つも存在しないときにのみ真である。管理者が存在すれば、セッションの状態に関わらずログインのページが表示される）、外部 IP（ipinfo.io から毎時取得）、内部 IP（ループバックでない最初の IPv4 のアドレス）、ディスクの使用量の統計、アップグレードの有無、サーバーの UTC のオフセット（分）、現在のロケール、`proton_enabled`（このビルドが `proton` のビルドタグを持つか）、`boot_id`、そして有効なトークンが渡されていれば認証されたユーザー名である。

サービスのユニットの数は二つのフィールドに分かれる。`units` はパッケージのサービスのユニット（`town-os-package--*` に一致するもの）だけを数え、`system_services` はシステムサービスのユニット（`town-os-system--*` に一致するもの）を数える。アンインストールされたパッケージから残った systemd のユニットは、パッケージの数から除かれる。インストール済みのパッケージの一覧は、各パッケージの同一性から期待されるユニット名を組み立てて、発見された systemd のユニットと突き合わされる。

このハンドラはアカウントを一度だけ一覧し（`needs_setup`、合計、管理者の数に使う）、ボリュームの数には `ListFilesystems` ではなく `FilesystemNames` を使う。後者はサブボリュームごとに `btrfs qgroup show` と rootid の参照を走らせるので、サブボリュームが約 30 ある状態では、ping が決して読まないクォータのために ping の遅延の予算のおよそ一秒を費やしてしまう。

localhost 以外からの認証されていない要求は、`status`、`needs_setup`、`boot_id` だけを含む最小の応答を受け取る。そこでも `boot_id` が運ばれるのは、更新の流れがコントローラの再起動をまたいで ping をポーリングし、その間ブラウザが短く認証されない状態になるからである。これはプロセスごとのランダムな UUID であり、システムについて何も明かさない。認証された要求と localhost からのすべての要求は、上に挙げたすべてのフィールドを持つ完全な応答に加えて `repository_errors`（リポジトリごとの更新の失敗を追跡する、リポジトリ名からエラーの文字列へのマップ）を受け取る。

コントローラがまだ起動中の間、このパスは代わりに起動のスタブが提供し、`{booting, step, done, error, boot_id}` を伴う **503** を返す —— [起動ステータスと更新](#起動ステータスと更新)を参照。

### 外部 IP のポーリング

システムコントローラは、サーバーの公開（外部）IP のアドレスを `https://ipinfo.io/json` から取得する。このポーラーは、HTTP のハンドラが作られるとき（`NewHandler`）と Unix のソケットのサーバーが起動するときに自動的に開始される。起動時に直ちに IP を取得し、そのあと 1 時間ごとにポーリングする。各取得には 10 秒の HTTP のタイムアウトがある。結果はアトミックな値にキャッシュされ、認証された ping の応答に `external_ip` として含まれる。取得の失敗は debug の水準でログに出るだけで、システムの他の部分に影響しない。IP がまだ一度も取得されていないとき、このフィールドは応答から省かれる。

## 監視

統合された Prometheus + Node Exporter の監視のスタックがシステムのメトリクスを提供する。`monitoring.Manager` は、このスタックを `town-os-system--` の命名の接頭辞を使い `Restart=always` を伴う、systemd が監督する podman のコンテナ（システムサービス）として管理する。ダッシュボードのフロントエンドは `monitoring_backend` の設定で構成できる。

### 監視のポート

ポート **5308** が監視のダッシュボード専用のポートである（`TOWN_OS_MONITORING_PORT` がそれを移動させる。二つのループバックのポートについても同様に `TOWN_OS_PROMETHEUS_PORT` と `TOWN_OS_NODE_EXPORTER_PORT` がある —— [システムサービスのホストポート](#システムサービスのホストポート)を参照）。ポートは、空のフィールドが `withDefaults()` で埋められる単一の `monitoring.Ports` の値として三つのサービスへ届くので、既定値の当てはめは一か所にある。ダッシュボードのポートで何が listen するかは、有効なバックエンドが決める:

- **uPlot のモード**（既定）: socat のフォワーダー（`socat TCP-LISTEN:5308,fork,reuseaddr TCP:localhost:9090`）がポート 5308 で Prometheus の HTTP の API を公開する。React の UI は Prometheus の `/api/v1/query_range` を直接問い合わせ、uPlot でグラフを描く。
- **Grafana のモード**: Grafana が（podman のポートの対応付けを通じて）ポート 5308 で直接 listen する。React の UI は Grafana の iframe を埋め込む。

systemcontroller（ポート 5309）を通じたリバースプロキシは**無い**。ブラウザはすべての監視のデータについてポート 5308 と直接話す。

### 監視のバックエンドの設定

`monitoring_backend` のシステムの設定が、どのダッシュボードのフロントエンドを使うかを制御する:

- `"uplot"`（既定）-- uPlot（約 35 KB）を使って React の UI に描かれる軽量な組み込みのグラフ。socat のフォワーダーを通じてポート 5308 で Prometheus を問い合わせる。Grafana は pull も起動もされないので、最初の起動で約 771 MB を節約する。
- `"grafana"` -- 完全な Grafana のダッシュボード。Grafana のコンテナイメージが pull され、ポート 5308 で起動される。Prometheus のデータソースと登録簿のすべてのダッシュボードがあらかじめ用意される。

設定の変更は直ちに効く。`"grafana"` へ切り替えると Grafana のイメージが pull されコンテナが起動する（socat のフォワーダーは停止する）。`"uplot"` へ切り替えると Grafana が停止し socat のフォワーダーが起動する。

機械がまだ Grafana のイメージを持っていないとき、その pull は専用の goroutine で走り、イメージが揃ってから初めてユニットを入れ替える。この順序に三つのことが懸かっている。設定の要求が約 771 MB のために詰まらないこと、その間は前のバックエンドが `:5308` に答え続けること、そして —— ユニットが `Type=simple` であり systemd は `podman run` が fork した時点で起動したと見なすため —— まだダウンロード中の Grafana を「動いている」と報告しないことである。その窓の間 `/monitoring/status` は `grafana: false` を返し（`MonitoringUIPending`）、それがダッシュボードを「起動中」の画面に留め、前のバックエンドを出しているポートを iframe に入れさせないでいる。同時に二度目の保存をしても二度目の pull にはならず何もしない。pull が失敗した場合はその印を下ろし、ログに残す。

### 監視のコンテナ

- **Node Exporter**（`quay.io/prometheus/node-exporter:latest`、ホストのポート 9100）-- ホストのシステムのメトリクスを収集する。ホストの PID の名前空間、`SYS_TIME` の権限、そしてホストのルートのファイルシステムの `/host` への読み取り専用のバインドマウントを伴って動く。systemd のユニットは `--collector.diskstats.device-exclude=^(ram|fd)\d+$`（`monitoring.DiskstatsDeviceExclude` の定数）を渡して node_exporter の上流の既定（`^(ram|loop|fd|(h|s|v|xv)d[a-z]|nvme\d+n\d+p)\d+$`）を上書きする。上流の既定はパーティション（`sda3`、`nvme0n1p3`）と loop デバイスを除いてしまう —— まさに `monitoring.BtrfsDevices` が `/town-os` を支える btrfs のファイルシステムについて報告するデバイスの形である。この上書きが無いと Disk I/O のダッシュボードの問い合わせは黙って空の系列を返し、パネルは空で描かれる。`node_disk_*` から Disk I/O の問い合わせを移さない限り、このフラグを取り除いたり緩めたりしないこと。退行の検査: `TestNodeExporterUnitConfigDiskstatsExcludeAllowsRealDevices` がフラグと正規表現を固定し、`TestMonitoringNodeExporterEmitsDiskMetricsForFilteredDevices` は本物の node_exporter のコンテナを起動して、上流の既定で除かれるデバイスの少なくとも一つについて `node_disk_read_bytes_total` を出力することを確かめる。
- **Prometheus**（`quay.io/prometheus/prometheus:latest`、ホストのポート 9090）-- Node Exporter、自分自身、rolodex（ジョブ `rolodex`）、そしてシステムコントローラ（ジョブ `systemcontroller`。[システムコントローラのメトリクス](#システムコントローラのメトリクス)を参照）を 15 秒間隔で収集する。二つの任意のジョブは、そのアドレスが設定されていないとき、推測した既定へ向けるのではなく省かれる。誰も設定していない対象は永久に down のまま座り、不在のサービスではなく壊れたサービスとして読めてしまうからである。データは永続的なデータのディレクトリに 30 日の保持で保管される。設定とデータのボリュームは監視のデータのディレクトリからバインドマウントされる。systemd のユニットは、再起動時にボリュームのディレクトリをあらかじめ作る `ExecStartPre` の mkdir の指示を含む。
- **Grafana**（`docker.io/grafana/grafana:latest`、ホストのポート 5308）-- 任意のダッシュボードの UI であり、`monitoring_backend` が `"grafana"` のときにのみ起動される。明るいテーマ（`GF_USERS_DEFAULT_THEME=light`）を使う。匿名の閲覧が Viewer の役割で有効になっており、iframe への埋め込みが許可されている。systemd のユニットは、再起動時にボリュームのディレクトリをあらかじめ作る `ExecStartPre` の mkdir の指示を含む。Prometheus のデータソースと[ダッシュボード](#ダッシュボード)で述べるダッシュボードがあらかじめ用意される。それらがどうやってそこへ届くかは[ダッシュボードのプロビジョニング](#ダッシュボードのプロビジョニング)を参照。
- **socat のフォワーダー** -- uPlot の形をとった `monitoring-ui` のユニット（`town-os-system--monitoring-ui.service`）であり、`monitoring_backend` が `"uplot"` のときにのみ起動される。ポート 5308 をポート 9090 の Prometheus へ転送する。これは Grafana が使うのと*同じユニットの鍵*であって、二つ目のものではない。この二つは一つのサービスの代替となる本体であり、それが、バックエンドの変更を、両方あるいはどちらも動かないままになりうる一組の start/stop の呼び出しではなく、ユニットの書き換えと再起動にしているものである。

### ダッシュボード

ダッシュボードは三つあり、**どちらのバックエンドも同じ問い合わせから同じ三つを描く**。一つの長いページではなく分かれているのは、それらが異なる問いに答えるからである。System はこの機械が遅く感じるときにオペレーターが見るものであり、DNS は名前が解決しないときに開くもの、Controller は Town OS が動かしている何かが動いていないときに開くものである。八つの DNS のパネルと十四の controller のパネルを概観へ畳み込めば、そもそも人がそれを開く理由である四つのホストのパネルが埋もれてしまう。

**System**（Grafana の uid は `town-os-overview`、「Town OS Overview」）-- 四つのパネル:

1. **Disk I/O (/town-os)** -- btrfs のファイルシステムを支えるブロックデバイスにまたがって合計した読み書きのスループット。ファイルシステムがいくつのデバイスにまたがっていても、パネルには Read が一本、Write が一本の線が出る。デバイスの正規表現は `monitoring.BtrfsDevices` から差し込まれる。一覧が空のときは何にも一致しない `NoBtrfsDevicesSentinel` に解決されるので、パネルはホストのすべてのディスクを黙って合計するのではなく空で描かれる。
2. **Network (External)** -- 物理デバイスごとの受信／送信をビット毎秒で（`lo`、veth、podman、cni、tailscale、bridge、docker を除く）。`node_network_up == 1` と結合するので、かつて存在したが今は down のインターフェイスが、凡例から平坦なゼロの線を引くことはない。
3. **CPU Usage** -- モード（user、system、iowait、irq、softirq、steal、nice）ごとに積み上げ、Total の重ね線を伴う。0〜100%。
4. **Memory Usage** -- 合計、使用中、利用可能。

**DNS**（Grafana の uid は `town-os-dns`、「Town OS DNS」）-- `rolodex` の収集のジョブに対する八つのパネル:

1. **DNS Queries by Response Code** -- `rate(rolodex_dns_queries_total)` を `rcode` で合計し、積み上げる。この分割がドリルダウンではなくパネルそのものであるのは、素の問い合わせ数では、忙しいリゾルバと、すべてを SERVFAIL しているリゾルバを区別できないからである —— それらは同じ線になる。
2. **Query Latency** -- `rolodex_dns_query_duration_seconds_bucket` からの p50/p95/p99。バケットは `histogram_quantile` の*前*に `le` で合計される。生の系列は `proto` のラベルを運んでおり、集約せずに分位を取ると、この機械全体の遅延ではなく輸送ごとに一本ずつ線を引いてしまうからである。
3. **Answers by Source** -- どの解決の段が答えたか（キャッシュ、ローカル、スコープ付き、上流の段）を積み上げる。この機械が自分で答えているのか転送しているのかを言うのが、このパネルである。
4. **Cache Hit Ratio** -- ヒットと否定のヒットを、すべての参照で割ったもの。0〜100%。キャッシュされた NXDOMAIN もヒットに数える。肯定のものとまったく同じく、上流への往復を節約したからである。分母は意図的に切り詰められないので、暇な機械では、何も尋ねられていないキャッシュについて自信たっぷりの 0% を描くのではなく、線が途切れる。
5. **Cache Entries** -- 肯定、否定、ブロックリストのキャッシュの大きさ。
6. **Blocklist Activity** -- 種別ごとの遮断、許可リストによる免除、そして**拒否**。拒否が遮断と同じパネルを共有するのは意図的である。提供者が「登録されている」ではなく「もう聞くな」と答えることこそが、ブロックリストを黙って障害に変えるものであり（[拒否コード](#拒否コード-提供者のもう聞くなはこれは登録されているではない)）、それは、置き換わった元の遮断の率の横で見てはじめて異常として読めるからである。
7. **Upstream Tier Outcomes** -- 段ごとの成功と失敗、そしてすべての段を使い切った問い合わせ。
8. **DNS Traffic** -- 通信のバイト数の rx/tx。

**Controller**（Grafana の uid は `town-os-controller`、「Town OS Controller」）-- `systemcontroller` の収集のジョブに対する十四のパネルであり、この機械自身の [`townos_*` のメトリクス](#システムコントローラのメトリクス)を読む唯一のダッシュボードである。

並びは、何かがおかしいときに人が実際に読む順序である。何が落ちているか、API は何をしているか、何が失敗しているか、ディスクはどう見えるか、controller の処理自身がどう持ちこたえているか —— 目録はその後である。目録は UI の別の画面にあるものであり、ここではほとんど動かない一本の線でしかない。正常な機械の上では決して動かないパネルは、動くパネルを押しのける最初の数行ではなく、いちばん下に置いてある。

1. **Service Health** -- `townos_system_unit_active` と `townos_package_unit_active`。ユニットごとに一本の系列で、軸は 0--1 に固定する。これは何個落ちたかではなく*どれが*落ちたかを言うパネルであり、だから先頭に来る。軸を固定するのは、この指標が真偽値だからである。自動目盛りでは、完全に健全な機械が 1.0 の周りの雑音として描かれ、何も問題が無いときに限って不穏に見える。
2. **Service Units by State** -- `townos_system_units` と `townos_package_units` を state ごとに、一つのパネルの上に**積み上げずに**置く。両者は別々の合計であり、積み上げれば誰も管理していない合算の高さを描くことになる。
3. **API Requests by Status** -- `rate(townos_http_requests_total)` を `status` で合計し、積み上げる。あえて status で合計している。この族は `method` も運んでおり、それを残した状態のパネルは組み合わせごとに一本の線を描いてしまう。
4. **API Latency** -- `rate(townos_http_request_seconds_total)` を `rate(townos_http_requests_total)` で割る。どちらも `method` で合計した、要求あたりの平均の秒数である。これは「機械が忙しい」と「機械が詰まっている」を分けるパネルであり、3 番にはそれができない —— 毎秒二つの要求を返す制御面は、一つに 5 ミリ秒かかっていても 5 秒かかっていても同じ線を描く。分位数ではなく平均なのは、ここの露出の形式がヒストグラムを運ばないからである（[システムコントローラのメトリクス](#システムコントローラのメトリクス)を見よ）。何かが詰まり始めれば平均は動き、それが問うている事柄である。
5. **Recent Failures** -- `townos_audit_recent_errors`（ダッシュボードが赤い丸として描くのと同じ五分間の数）を `townos_repository_errors` と並べる。二つが一つのパネルにあるのは、「何か壊れていないか」を見るオペレーターが、どの下位の仕組みの下を見ればよいかを先に知っている必要は無いからである。どちらも近い窓の上のゲージなので、ゼロに戻ることは登るのをやめた計数器ではなく回復を意味する。
6. **Audit Events** -- `rate(townos_audit_events_total)` を `result` ごとに、積み上げ。
7. **Town OS Disk Usage** -- `townos_disk_used_bytes` と `townos_disk_available_bytes` の積み上げ。使用済みと合計ではなく使用済みと空きなのは、積み上げればその二つ*が*ファイルシステムの大きさだからで、三本目の系列はそれを言い直すだけである。
8. **Town OS Disk Fill** -- 同じディスクを `townos_disk_total_bytes` に対する百分率として、0--100 に固定して描く。7 番のパネルは「あとどれだけで一杯になるか」に答えられない。読む人が、機械ごとに目盛りの変わる軸に対して暗算をしない限りは。固定した軸は傾きも読めるようにする。自動目盛りでは、4% から 5% への上りと 90% から 99% への上りが同じに描かれてしまう。分母はあえて抑え込んでいないので、収集器が読めなかったファイルシステムは線が途切れる。自信たっぷりの 0% を描くよりそのほうが正直である。
9. **Controller CPU** -- `100 * rate(townos_process_cpu_seconds_total)`。下限は 0 に固定し、上限はあえて固定**しない**。これはコア秒あたりの値なので、二つのコアを使い切った controller は 200 を示す。100 で頭打ちにすれば、このパネルが存在する理由である暴走をちょうど切り落としてしまう。
10. **Controller Memory** -- `townos_memory_heap_bytes` を `townos_memory_rss_bytes` と並べる。二つの差が診断そのものである。ヒープが登るなら controller は手放すべき物を抱えており、ヒープが平らなまま RSS が登るなら、メモリは Go の割り当て器が数えない場所へ行っている。
11. **Controller Concurrency** -- `townos_goroutines`、`townos_open_files`、`townos_http_requests_in_flight`。健全な機械の上では平らで、漏れている機械の上では際限なく登る三つの数である。一つのパネルを分け合っているのは、たいてい一緒に動くからであり —— 決して戻らないハンドラは goroutine と記述子と処理中の要求を一つずつ抱える —— また、どれか一つだけでは、そのためにタブを開く人のいない一本の線でしかないからである。
12. **Controller Uptime** -- `time() - townos_start_time_seconds`。信号は高さではなくその鋸歯である。`Restart=always` の下で静かに落ち続けている controller は、ここの他のどのパネルの上でも健全に見える。
13. **Inventory** -- 導入済みのパッケージ、待っている更新、設定されたリポジトリ、そして名前空間ごとの `townos_filesystems`。かつての三つではなく一つのパネルにしてある。どれも数十の桁の数なので、一本の軸を読みやすく分け合い、「この機械には何があるか」に一箇所で答える。
14. **Accounts** -- `townos_accounts` を kind ごとに、`townos_accounts_granted` をその隣に置き、**積み上げない**。kind はたしかにアカウントの一覧を分割するが、権限を持つ数は user の*部分集合*である。積み上げれば、このパネルは実在するアカウントの数より大きな合計を描いてしまう。

9 番から 11 番のパネルは、「機械は何を走らせているか」ではなく「機械はなぜ遅いか」に答えるものであり、他のどのダッシュボードにもそれはできない。ここの他のすべての族は controller が*管理する*ものを描いており、controller が goroutine をスワップへ漏らしている間も、そのすべては健全に見える。Node Exporter もこの隙間を埋められない —— それが報告するのはホストであり、コンテナを走らせることが仕事のすべてである機械の上では、controller 自身の取り分はホストの合計の中に埋もれて見えない。

`townos_up` と `townos_packages_available` はあえて描**かない**。前者は収集の生存を示す定数であり、1 の平らな線はパネルにならない。後者は千の桁の目録の大きさであり、軸を共有すれば隣の数を底に貼り付いた一本の線に潰してしまう。そこからオペレーターが本当に欲しいもの ——「どこかのリポジトリが答えなくなっていないか」—— は `townos_repository_errors` であり、それには専用のパネルがある。`townos_disk_total_bytes` は 8 番のパネルの分母としてだけ現れ、系列としては決して現れない。7 番のパネルが、足せばそれになる二つの半分を既に積み上げている。

すべての DNS の問い合わせは `monitoring.RolodexJobName` から作られた `{job="rolodex"}` の選択子を運び、すべての controller の問い合わせは `monitoring.ControllerJobName` から作られた `{job="systemcontroller"}` の選択子を運ぶので、収集の設定が出力するラベルと、ダッシュボードが選択に使うラベルがずれることはありえない —— 食い違いはどこでもエラーにならず、働いている機械の上でタブ一つ分のパネルが空になるだけである。

二つのフロントエンドは別々の言語の別々のコードで同じダッシュボードを描いており、**唯一の**違いは率の窓である。Grafana はパネルごとに `$__rate_interval` を展開するが、uPlot のフロントエンドにはマクロの展開が無いので `RATE_INTERVAL`（`5m`）を固定している。uPlot の側にマクロが漏れると Prometheus の解析エラーになり、タブ全体が真っ白になる。

四種類のテストが両側を繋ぎ止めている。他に両者を繋ぐものが無いからである:

- `TestRolodexDashboardMirroredInFrontendQueries` と `TestControllerDashboardMirroredInFrontendQueries` は Go のテストから `ui/src/components/monitoring/queries.js` を読み、どちらかの側がもう一方に無いメトリクスの族を名指ししていれば失敗する —— `TestBootStepsFrontendInSyncWithBackend` が起動の段階に適用しているのと同じ、ずれの見張りである。
- rolodex の収集の統合テストは、**固定された rolodex のイメージが実際に** `monitoring.RolodexDashboardMetrics()` のすべての族を**出力すること**を主張し、`TestControllerDashboardMetricsAreServed` は `monitoring.ControllerDashboardMetrics()` について、controller 自身の端点への本物の収集に対して同じことを主張する。どちらも `# TYPE` の行で照合するので、ある族の名前が別の族の接頭辞であっても、欠けているほうの保証にはならない。この機械が出力しない族を名指しするパネルは空のグラフを描き、それは暇な機械と区別がつかない。
- `TestDashboardQueriesParseInPrometheus` は、すべてのダッシュボードのすべての式を本物の Prometheus に通す。JSON の中の壊れた PromQL は、どこでも構文エラーにならない。ファイルは用意され、ダッシュボードは読み込まれ、パネルは軸を描き、そして永遠に「No data」と言う。
- `MonitoringDashboard.test.jsx` は、どのタブも**それぞれ別の** uPlot のコンポーネントをマウントすることを主張する。タブの一覧が分岐に選ばせるのではなく自分でコンポーネントを名指ししているのは、分岐を書き忘れたタブが System のグラフへ落ちてしまい —— 誤った見出しの下に本物のダッシュボードが描かれ、それが動いているように読めてしまうからである。

controller のダッシュボードは、欠けうる収集のジョブに依存する唯一のものである。`ports.ControllerMetrics` は `-listen` の値から導かれ、`WritePrometheusConfig` は導けないときにアドレスを推測せずそのジョブを**省く**。省かれたジョブは、壊れたダッシュボードではなくデータの無いダッシュボードである。

### ダッシュボードのプロビジョニング

`monitoring.GrafanaDashboards(diskDevices)`（`src/monitoring/dashboard.go`）が登録簿であり —— ダッシュボードごとのファイル名、uid、タイトル、描き出された JSON —— `WriteGrafanaProvisioningFiles` がそれを反復する。ダッシュボードの追加はそこへの一項目だけで、他には何も要らない。プロビジョナ（`GrafanaDashboardProviderYAML`）は `dashboard-json` の**ディレクトリ**を指すので、その中のすべてのファイルが拾われる。この登録簿ができる前は、ファイルを書く側が事実上の一覧だった。つまり二つ目のダッシュボードを追加するには、ダッシュボードとは何の関係も無いコードを編集するしかなかった。

uid が定数（`OverviewDashboardUID`、`DNSDashboardUID`、`ControllerDashboardUID`）であるのは、ウェブの UI がそれに深いリンクを張るからである。ずれた uid はどこにもエラーを生まない —— Grafana は iframe の中に「dashboard not found」のページを提供する。

DNS と controller のダッシュボードは、古い概観のダッシュボードが今もそうしているような JSON のテンプレートへの連結ではなく、**パネルの仕様から組み立てられてマーシャルされる**（`src/monitoring/dashboard_dns.go`、`src/monitoring/dashboard_controller.go`）。ダッシュボードの中の壊れた JSON はパネル一つで済まない。プロビジョニングが失敗し、そのダッシュボードはそもそも現れない。パネルの対象はオブジェクト形式のデータソースの参照（`{"type":"prometheus","uid":GrafanaDatasourceUID}`）を運ぶ —— Grafana 13 以降は対象の中の旧来の文字列形式を解決できず、エラーを出さずに「No data」を描く。

### ライフサイクル

Prometheus と Node Exporter は起動時に常に開始される。Grafana か socat のフォワーダーのどちらも併せて開始されるかは、監視のバックエンドの設定が決める。起動の失敗は非致命的であり、システムは監視なしで続行する。再起動は systemd が `Restart=always` の方針で処理する。`Stop()` のメソッドは何もしない。システムサービスはコントローラの再起動をまたいで存続するからである。

### 監視 API

- `GET /monitoring/status`（認証が必要）-- `backend`（`"uplot"` または `"grafana"`）、サービスごとの動作のフラグ（`prometheus`、`node_exporter`、`monitoring_ui`、そして Grafana のモードのときだけ `grafana`）、そして `disk_devices`（btrfs のファイルシステムを支えるカーネルのデバイスの基底名。フロントエンドがこれを Disk I/O の問い合わせへ差し込む）を返す。`disk_devices` が空であることは、発見が失敗し、パネルが何にも一致しない正規表現へフォールバックすることを意味する。監視が設定されていないときは `{"status": "disabled"}` を返す。サービスごとのイメージとユニットのメタデータはここには無い —— それは `GET /system-services` である。
- `GET /metrics`（localhost または管理者）-- システムコントローラ自身の Prometheus のエンドポイント。[システムコントローラのメトリクス](#システムコントローラのメトリクス)を参照。

**スクレイプの健全性もここで報告される。ほかに報告するものが無いからである。** `/monitoring/status` はさらに `scrape_targets`（Prometheus 自身が見ている、スクレイプ対象のすべてのジョブ。それぞれ `job`、`instance`、`health`、`scrape_url` を持ち、失敗しているものは Prometheus が記録した `last_error` も持つ）、`down_jobs`（失敗しているターゲットを一つ以上持つジョブの名前だけ。ダッシュボードの帯が言うのはこれである）、`scrape_targets_error`（Prometheus が動いている機械で、なぜその一覧が空なのか）を運ぶ。`monitoring.FetchScrapeTargets` はループバックの Prometheus から `GET /api/v1/targets` を読む。3 秒で打ち切るので、止まった Prometheus が状態のポーリングを掴んだままにすることはない。そして**prometheus のユニットが active のときにだけ**問い合わせる —— ユニットが止まっているなら、その状態がすでにそう言っており、その上に接続のエラーを重ねれば、別の二つ目の障害のように読めてしまう。

Prometheus がまだスクレイプしていないターゲットが報告するのは `down` ではなく `unknown` である。再起動のあとの最初の間隔では、どのターゲットもその状態にあり、それを down と数えれば、どの起動も障害のように見えてしまう。

これが存在するのは、一度も成功したことのないスクレイプのジョブが、ほかのどこからも見えないからである。この機械はその形の不具合をすでに二度出荷している。`prometheus.yml` が `https` だと言っているのに `:5309` は平文を出していたこと、そして rolodex の `metrics:` の節が設定ファイルへ一度も届かなかったことである。どちらでも、すべてのユニットは active で、`systemctl --failed` は空で、影響を受けたパネルが描いたのは**エラーではなく空のグラフ**だった —— 語ることが何も無いサービスと見分けがつかない。回帰テストは `TestFetchScrapeTargets*` と `TestHTTPMonitoringStatus{ReportsDownScrapeJobs,SaysWhyItCouldNotAsk,SkipsTargetsWhenPrometheusIsStopped}`（ユニット）、および `TestMonitoringStatusReportsADownScrapeJob`（統合。実物の Prometheus に対し、ジョブの一つを誰も待ち受けていないポートへ向けたもの）である。

### システムコントローラのメトリクス

コントローラは自分の状態を Prometheus のテキストの公開形式で、自前のポートではなく**既存の listener**（`:5309`、`MetricsPath = "/metrics"`）の上に公開する。これは意図的である。そうすればこのエンドポイントは、ハーネスが既に `TOWN_OS_LISTEN` で移動させている listener に相乗りするので、`SYSTEM_PORT_FILES` へ追加すべきホストのポートも増えず、`make test-full` と `make dev` がそれで衝突する道も無い —— 鉄の掟である。

これは公開ではなく **localhost または管理者**である。この収集はアカウントの数、ディスクの使用量、どのサービスが落ちているかを集約する。何を攻撃するか、そしてこの機械がもっとも抵抗できないのはいつかの地図である。Prometheus は `--net host` で動くので、node-exporter の対象とまったく同じく、podman のネットワークを一つも跨がずにループバックへ到達する。

`src/metrics` は `prometheus/client_golang` に依存せず数百行でこの形式を描き出す。`errgroup` を入れなかったのと同じ理由である。あのライブラリの価値はその登録簿、コレクタのインターフェイス、ヒストグラムの仕組みにあるが、そのどれも使っていない —— ここでの値はどれもプロセスの生涯の集計か、収集ごとにマネージャから読むものだからである —— 一方でその推移的な依存の木（`prometheus/common`、`procfs`、protobuf）は現実であり、RAM から起動するイメージに載ることになる。

**ラベルの値のエスケープは防御的なものではなく、荷を負っている。** ラベルの値はオペレーターの入力（リポジトリ名、パッケージ名、systemd のユニット）を運ぶ。エスケープされない引用符は一行を壊すのでは済まない —— Prometheus に*収集全体*を拒否させるので、変な名前のパッケージが一つあれば、黙って監視のすべてを落としてしまう。

公開されるもの:

| メトリクス | 型 | 備考 |
|---|---|---|
| `townos_up` | gauge | 提供中は常に 1。提供していないときは存在しない |
| `townos_start_time_seconds` | gauge | 稼働時間は収集側の時計での `time() - this` |
| `townos_package_units{state}` | gauge | `active`/`failed`/`inactive`。インストール済みのパッケージに絞られる |
| `townos_system_units{state}` | gauge | `town-os-system--*`。NC とソケットのユニットを除く |
| `townos_package_unit_active{unit}` | gauge | ユニットごとの 1/0。オペレーターに*どの*サービスが落ちているかが見える |
| `townos_system_unit_active{unit}` | gauge | システムサービスについて同様 |
| `townos_packages_installed` / `townos_packages_available` | gauge | 在庫 |
| `townos_repositories` / `townos_repository_errors` | gauge | エラーは数えるが、名前でラベル付けはしない |
| `townos_upgrades_available` | gauge | |
| `townos_accounts{kind}` | gauge | `admin`/`user`/`disabled` |
| `townos_accounts_granted` | gauge | 少なくとも一つの権限付与を持つ、管理者でないもの |
| `townos_filesystems{state}` | gauge | `user`/`installed`/`uninstalled` |
| `townos_disk_total_bytes` / `_used_bytes` / `_available_bytes` | gauge | |
| `townos_audit_recent_errors` | gauge | ダッシュボードの赤い表示が描くのと同じ数 |
| `townos_audit_events_total{result}` | counter | `success`/`failure`。`auditMiddleware` が増やす |
| `townos_http_requests_total{method,status}` | counter | status は**分類**（`2xx` など）であり、正確なコードでは決してない |
| `townos_http_request_seconds_total{method,status}` | counter | 要求を返すのに費やした秒数。上の行と*同じ*ラベルを運ぶ。この二つは割ってはじめて役に立つからである |
| `townos_http_requests_in_flight` | gauge | いま処理中の要求の数。ゼロでも出力し、`/metrics` の要求そのものは除いてある |
| `townos_goroutines` | gauge | ホストではなく controller の処理のもの |
| `townos_memory_heap_bytes` | gauge | 生きているヒープの物（`HeapAlloc`）であり `HeapSys` ではない。何かが漏れているときに登るのは前者である |
| `townos_memory_rss_bytes` | gauge | `/proc/self/statm` の常駐ページ数を、4096 と決めつけずにページの大きさで換算したもの |
| `townos_open_files` | gauge | `/proc/self/fd` の項目の数。Go の実行時は記述子を追わないので、漏れは数時間後に `EMFILE` として現れる |
| `townos_process_cpu_seconds_total` | counter | `getrusage` から取ったユーザ時間とシステム時間の和 |

`townos_up` と `townos_packages_available` を除くすべては [Controller のダッシュボード](#ダッシュボード)が描いており、そのパネルの集合は `monitoring.ControllerDashboardMetrics()` に対して宣言されているので、二つの一覧がずれることはありえない。`townos_disk_total_bytes` はディスクの占有率のパネルの分母としてだけ現れる。

処理に関する族はこの中で最も新しく、controller が管理する物ではなく controller 自身を描く唯一のものである。`/proc` と `getrusage` は収集のたびにそれぞれ独立に読まれ、読めなかった族は収集全体を失敗させるのではなく省かれる —— 統合テストが単なる存在ではなく正の値を検査するのはそのためである。ゼロは、その読み取りが起きなかったことを意味する。

ここでのいくつかの選択は、付随的なものではなく要点である:

- **収集が丸ごと失敗することは決してない。** 各コレクタは nil のマネージャを許容し、エラーではログを出して飛ばす。一つのサブシステムが不調だからと 500 を返せば、他のすべてのメトリクスが、まさにそれらが欲しい瞬間に消えてしまい、この機械は部分的に劣化しているのではなく完全に死んでいるように読める —— そして起動中の収集は、上がっているものを報告すべきである。
- **ゼロのバケットも出力される。** ゼロで消える gauge は、この機械が報告をやめた gauge と区別がつかないので、「失敗したユニットは無い」が「ユニットの収集が壊れている」とまったく同じに見えてしまう。
- **ステータスは分類でまとめられる。** 個別のコードはすべて恒久的な系列になり、数十の経路にわたって 400/401/403/404/409/422 を返すコントロールプレーンでは、家庭の機械について誰も尋ねない問いのために掛け算で増えていく。正確なコードは既に監査のログと要求のログにある。
- **カウンタはメモリ上でプロセスごとである。** 再起動を生き延びるカウンタは、このプロセスではなくこの機械の歴史を記述してしまうし、Prometheus は既にリセットを理解する。これはまた、収集を —— そしてそれを養う監査のミドルウェアを —— データベースから完全に切り離してもいる。
- **`/metrics` は監査のログから除外され**、自分自身の要求のカウンタからも除かれる。そうしないと 15 秒ごとの収集が、オペレーターの操作でないものを記述する監査の行を一日に約 5,700 行書き、自分が提供しているカウンタを支配してしまう。
- **`metricsMiddleware` は三つのうち最も外側に登録される**（監査と権限付与の許可リストより前）ので、どちらの門で拒まれた要求も数えられる —— 説明のつかない 403 こそ、このカウンタが可視化するために存在するものである。ハンドラがエラーを返したときはそのエラーからステータスを取る。エラーを返したハンドラはまだ自分のステータスを書いていないからである。

**収集の対象がどこかで組み立て直されることは無い。** `MetricsScrapeTarget(listenAddr)` はサーバーがバインドするのと同じ文字列からそれを導き、`main.go` がその結果を `monitoring.Ports.ControllerMetrics` へ渡す —— `PackageNetworkState.FQDN` と `Manager.MetricsAddr()` が存在するのと同じ、唯一の真実の出所という理由である。ワイルドカードのバインド（`:5309`、`0.0.0.0:5309`、`[::]:5309`）は `localhost` へ書き換えられる。ワイルドカードは何かが接続できるアドレスではないからである。明示的に固定されたホストはそのまま放っておかれる。書き換えれば、コントローラが意図的にいないアドレスへ収集を向けてしまうからである。結果が空のときは、推測へ向けるのではなくそのジョブを省く。

**そして方式は、二度目の導出ではなくソケットに尋ねる。** `systemcontroller.ListenerSpeaksTLS` がコントローラ自身の listener に対して TLS のハンドシェイクを完了したとき `ControllerMetricsScheme` は `https` になり、そのジョブは `insecure_skip_verify` も運ぶ —— リーフはこの機械自身の CA が発行したものであり、Prometheus にはそれを信じる理由も、それを綺麗に渡される手立ても無く、そしてこの収集はホストの名前空間の中のループバックなので、他の何かがそれとして答えることはできないからである。

以前はその代わりに `TOWN_OS_TLS` を二度目に読んでいて、配備済みのある機械では二つの導出が食い違った。`prometheus.yml` は `scheme: https` を運び、`:5309` は平文を提供していたので、Prometheus は TLS の接続を開き、平文の HTTP の応答を受け取り、**その設定の一生のあいだ収集をすべて失敗させた**。`insecure_skip_verify` はこれに何の助けにもならない —— それが緩めるのは証明書の検証であり、問題はハンドシェイクがそもそも一度も無いことだからである。上に並ぶ二十いくつの `townos_*` のメトリクスは一つ残らず、書き出されては床に落とされていた。そして唯一の痕跡は Prometheus 自身の対象一覧の中で `down` に座るジョブであり、それをこの機械の上の何かが表に出すことはない。ハンドシェイクはソケットと食い違えない。それはまた `TOWN_OS_TLS_CERT`/`TOWN_OS_TLS_KEY` も覆う —— これらは古い導出が読んでいた変数を経ずに TLS を終端する。

この探りは三秒で区切られ、何も接続を受け付けないときは起動自身の見立てへ落ちる —— 尋ねるソケットが無く、ジョブを出さないことは誤った方式と同じだけ確実に収集を殺すからである。観測された方式と設定された方式が食い違うとき、`SchemeDisagreement` がそれを stderr へ一度だけ報告する。収集はどちらにせよ直るが、オペレーターが TLS を求めた後に login のパスワードを平文で提供している機械は、その修復が覆い隠してしまう安全上の問題である。`TestListenerSpeaksTLS*`（単体）と `TestPrometheusScrapesTheSchemeTheListenerActuallySpeaks`（統合）が覆っている。後者は本物の Prometheus を、平文の listener と TLS の listener の両方に対して走らせる。

### 監視の UI

サイドバーのナビゲーションの監視のタブは、**System / DNS / Controller の下位タブ**を持つダッシュボードのページを開く。他のすべての下位タブのある画面と同じく `?tab=system|dns|controller` として深いリンクを張れるので、障害の最中に誰かが見ているダッシュボードは再読み込みを生き延び、リンクとして渡せる。未知の `?tab=` の値は、何も描かないのではなく System へフォールバックする。タブの一覧は、マウントする uPlot のコンポーネントと、同じパネルを表示する Grafana の uid の両方を持つ一つの配列なので、片方のバックエンドにだけ存在するタブはありえない —— そしてタブの値で分岐させるのではなくそこにコンポーネントを名指ししていることが、書き忘れた分岐が System のグラフを別のタブの見出しの下に描いてしまうのを防いでいる。

描き方は状態の応答の `backend` のフィールドに依存する:

- **uPlot のモード**: uPlot を使って React で直接描かれるパネルであり、ポート 5308 で Prometheus を問い合わせる。System のグリッドは自分をビューポートに合わせる（四つのパネル、一行に二つ）。DNS と Controller のグリッドは**そうしない** —— 八つないし十四のパネルを一画面に押し込めば、それぞれの描画領域は約 100 ピクセルかそれ以下になり、その大きさでは遅延のグラフは飾りである。だからパネルは固定の高さを持ち、ページがスクロールする。
- **Grafana のモード**: 明るいテーマのキオスクのモードでポート 5308 を対象とする埋め込みの Grafana の iframe。タブを切り替えるとフレームがもう一方のダッシュボードの uid を指し直し、iframe はその uid を鍵にしているので、フレームは遷移するのではなく*置き換えられる* —— Grafana は自前の履歴を持つので、生きたフレームの src を差し替えると、ブラウザの戻るボタンがページを離れる代わりにダッシュボードを一つずつ遡ることになる。

パネルのタイトルは二つのバックエンドで同一である。切り替えたオペレーターが、どのパネルがどれになったのかを考える必要はないはずだからである。これらは英語でハードコードされている —— この画面には `t()` の呼び出しが無く、そもそも Grafana のパネルのタイトルは、それが用意された JSON の中にあるので翻訳できない。

必要なサービスが動いていないときは、代わりに警告の帯とプレースホルダのメッセージが表示される。

## UI のコンテナ

システムコントローラは、独立した UI のコンテナ（`quay.io/town/ui`）を `ui.Manager` を通じてシステムサービスとして管理する。イメージのタグはシステムコントローラのリリースのタグから導かれ（`quay.io/town/ui:<tag>`）、`UI_IMAGE` の環境変数で上書きできる。起動の失敗は非致命的であり、システムは UI のコンテナ無しで続行する。

## ウェブ UI の配置

### ダッシュボードのサービスのパネル

ダッシュボードのホームのページは、統計カードのグリッドの上に、幅いっぱいのインストール済みのサービスのパネルを表示する。このパネルは `GET /systemd/units` から取得したすべてのパッケージのサービスのユニットを並べる。各サービスの行が示すもの:

- 状態のアイコン: active には緑のチェックの丸、failed には赤い X の丸、inactive には灰色の丸。
- パッケージ名（`package_identifier` のフィールドから解析される）。
- active の状態のテキスト。
- パッケージの説明（あれば）。
- `POST /packages/installed/info` からのコンパイルされた note。型を意識したリンク（URL、メール、電話）とともにその場で描かれる。

サービスの行をクリックすると —— 状態のアイコンでもパッケージ名でも —— `/dashboard/system?search=<package_identifier>` へ、つまりサービスの画面におけるそのサービス自身の行へ移動する。サービスの画面は `?search=` から絞り込みの入力欄を先に埋め、その語を `GET /systemd/units-tree` へ渡す。その検索は根のノード自身のフィールドに一致するので、画面は一覧の全体ではなく、その一つのパッケージとその依存関係の部分木を開いた状態になる。この語は開始の値であって錠ではない。入力欄を消すか書き換えれば、一覧はまた広がる。このリンクが運ぶのは常に生の `package_identifier` であり、整えられた `display_identifier` では決してない —— 後者は木の検索が一致させられる語ではないので、それで組み立てたリンクは空の木に着地してしまう。サービスが一つもインストールされていないとき、このパネルは隠される。note はサービスごとに一度取得されキャッシュされる。

### 配置

ダッシュボードは二つのパネルの配置を使う。貼り付いた左のサイドバーと、貼り付いた上部のヘッダーの帯を持つ右の内容の領域である。

**サイドバー** -- 幅 256 ピクセル（`w-56`）の縦のパネルであり、上部の灰色の帯に Town OS のロゴとブランドのテキストがあり、その下に縦に積まれたナビゲーションのボタン（それぞれアイコンとラベルを伴う）が並ぶ。現在の経路は `variant="secondary"` を、そうでないものは `variant="ghost"` を使う。

**上部の状態の帯** -- 右寄せの横の帯であり、接続の状態の表示（読み込み中／オフライン／オンライン）、システムサービスの失敗の数（`system_services.failed > 0` のとき `/dashboard/system?expand=system` へリンクする赤い表示のバッジ）、管理者のバッジを伴うログイン中のユーザー名、ログアウトのボタンを示す。

## システムサービス

システムサービスは systemd が管理する基盤のコンテナである（ユーザーがインストールするパッケージのサービスとは別物である）。ユニット名の接頭辞 `town-os-system--` を使う。

その集合は: rolodex、ingress、pages、UI、node-exporter、Prometheus、monitoring UI（socat のフォワーダーまたは Grafana）、そして**ネットワークごとに一つの gfeh の区画**（`town-os-system--gfeh-<network>`）である。この一覧にあるものはすべて `collectSystemServices()` に登録されなければならない。`POST /system-services/refresh` がそれを pull し直して再起動するためである —— そこでの漏れは、アップグレードがそのサービスを黙って古いイメージのまま残すまで見えない。

### 自動更新

**インストーラが届けるイメージは二つで、残りは制御器が自分で取得する。** `install.sh` が配置するユニットに書き込まれるイメージの参照はちょうど二つ —— `quay.io/town/town`（systemcontroller）と `quay.io/town/rolodex` —— であり、squashfs にイメージの中身を焼き込むことは一切しない。それ以外のシステムサービスのイメージ（UI、ingress、ネットワーク制御器、オブジェクトストレージ）はすべて、**その機械の上で**制御器が pull する。起動時は `coreBootImages` を `parallelEnsureImages` に渡し、必要に応じては `POST /system-services/refresh` による。それらのリポジトリが匿名で読めなければならない理由がこれである。機械はレジストリの資格情報を一切持たず、制御器のどこにも `auth.json` を書いたり `podman login` を実行したりする箇所は無い。リポジトリが非公開なイメージとは、この設計では取得できないイメージのことであり、その現れ方は `unauthorized` でクラッシュループし続けるユニットであって、本当の原因を名指しするものではない。

**日次のタイマーが実行するのは、UI のボタンが実行するのと同じ更新の手順であり、それはインストーラが同梱する。** `town-os-update.timer` とその service は `../install/systemd/` にあり、イメージのビルド時に有効化される（`make/install.sh` の `ENABLE_UNITS` の一覧）。したがって機械は最初の起動時からそれらを持っており、先に動いていなければならない制御器から受け取るのではない。このタイマーは `04:23` に発火し、`/system-services/refresh` へ POST する —— 「すべてを pull する」処理をもう一つ実装するのではなく、あえて同じエンドポイントを使う。そうすれば予定された経路が手動の経路から乖離しえない。ユニットが決めるのは*いつ*であり、何をするかは制御器が決める。`Persistent=true` なので、04:23 に電源が落ちていた機械は次の起動時に追いつく。もう一日待つのではない —— 起動しているより止まっている時間のほうが長い機械では、その追いつきこそが唯一受け取れる更新である。時刻は podman prune の `03:17` をあえて外してある。参照されていないイメージを回収している最中に新しいイメージを pull するのは、得るもののない競合だからである。

**タイマーの POST は資格情報を持たない。ルートが `localhostOrAdmin` である理由がそれである。** このユニットには提示できるトークンが無いので、`/system-services/refresh` は**ループバックからのみ**認証無しの呼び出しを受け入れ、それ以外の場所からは依然として管理者を要求する —— `GET /metrics` が拠って立つのと同じ免除であり、根拠も同じである。送信元が `127.0.0.0/8` のパケットは、ネットワークからその機械へ経路づけられない。したがって生成されるユニットは明示的に `127.0.0.1` を指し、機械の経路可能なアドレスは決して指さない。後者は認証無しの POST を LAN へ送り出したうえ、受け側でも拒否される。ポートは制御器の `-listen` のアドレスに追随するので、ポートを移された制御器（統合テストのハーネスは実行ごとに固有のポートを与える）でも自分自身を更新できる。

**`auto_update_enabled` が縛るのはタイマーであって、操作者ではない。** タイマーは自分の呼び出しに `?scheduled=1` の印をつける。その印を伴う呼び出しだけが設定を参照する。管理者が更新ボタンを押したときは常に更新される。「自動的に更新する」と書かれたスイッチは、明示的な要求について何も言うことが無いからである。飛ばされた予定実行はエラーではなく `200` と `{"status":"skipped"}` を返す —— タイマーは更新すべきかを尋ね、「今はしない」という妥当な答えを得たのであって、それは `systemctl status` が赤く示すべき失敗ではない。設定が off の間もタイマーは導入されたまま動き続けるので、設定を切り替えれば次の発火から効き、ユニットに手を入れる必要も、ユニットの状態と設定とが食い違う余地も無い。

この設定の既定は**有効**であり、認識できない値も有効として読まれる。無効は閉じた一覧（`0`、`false`、`off`、`no`、大文字小文字は問わない）であり、それ以外のすべて —— 打ち間違いも、読み取れない設定の行も —— は更新を動かし続ける。この非対称は意図したものである。インストーラが二つのイメージしか届けない以上、pull をやめた機械とはサービスの大半を永久に手に入れられない機械のことであり、「無効」と誤って判断する代償は、余分に一度 pull する代償よりはるかに大きい。

### システムサービスのユニットの生成

`GenerateSystemServiceUnit` は `Restart=always` を伴う podman ベースの systemd のユニットを作る。ユニットの設定は、`ExecStartPre=/bin/mkdir -p <dir>` の行を通じてあらかじめ作るホストのディレクトリを並べる `VolumeDirs` のフィールドに対応しており、システムコントローラが走る前にコンテナが再起動で立ち上がったときのマウントの失敗を防ぐ。

### システムサービス API

- `GET /system-services`（localhost または認証）-- 生きたユニットの状態を伴ってシステムサービスを一覧する。各項目には鍵、表示名、イメージ、ポート、systemd のユニットの状態のフィールドが含まれる。監視が設定されていないときは空の一覧を返す。監査のログから除外される。
- `POST /system-services/status`（管理者が必要）-- システムサービスの状態を変える。鍵と動作（`start`、`stop`、`restart`）を受け取る。`enable` と `disable` の動作は拒否される。
- `POST /system-services/refresh`（管理者が必要）-- システムサービスの状態を更新する。

## ウェブ UI の本番用のイメージ

独立した UI のコンテナイメージ（`quay.io/town/ui`）が `Containerfile.ui` からビルドされる。二段階のビルドを使う。`oven/bun:latest` が UI の静的なファイルをビルドし、続いて `docker.io/library/caddy:latest` が SPA のルーティング（`try_files {path} /index.html`）とともにポート 80 でそれらを提供する。UI はホストの `:80` に直接居座るのではなく、共有の ingress を通じて到達される —— どの経路にも一致しないホストに対する ingress の既定の `:80` のバックエンドであり、裸の IP でのログインが働き続ける。

**キャッシュのヘッダは荷を負っている**（`Caddyfile.ui`）。`/assets/*` の下のものはすべて Vite によって指紋を付けられているので、資産の URL は永久にただ一つのビルドを名指しし、`public, max-age=31536000, immutable` で提供される。`index.html` は Vite が指紋を付け**ない**唯一のファイルであり、現在のバンドルを名指しするものである。`Cache-Control` をまったく付けずに提供すると、ブラウザは発見的な鮮度（RFC 9111 §4.2.2）を適用し、再検証せずにキャッシュした写しを使い回しうるので、アップグレードされた機械が前のリリースのバンドルを指す前のリリースの `index.html` を配り続けることになる。症状は、アップグレードが起きなかったように見えることである —— 新しい機能が、UI がそれを聞いたこともないかのように描かれる。資産でないパスはすべて `try_files` が `index.html` へ解決する SPA の経路なので、`no-cache` の規則はそれらすべてを覆うように書かれている（`@html not path /assets/*`）。

`make release-ui` は `--no-cache` でビルドするので、`push-rc` は層のキャッシュされたバンドルではなく、常に新しくビルドされた UI の資産を出荷する。

**テストが quay の UI のイメージを pull することは決してない** —— `ui-image` の make のターゲットが `Containerfile.ui` をローカルに `localhost/town-os-ui:<INSTANCE_ID>` としてビルドし（常にホストのアーキテクチャとリポジトリ内の UI のソースに一致する）、イメージのキャッシュへ保存し、テストのハーネスがそれをテストのコンテナへ読み込んで `UI_IMAGE` の環境変数を通じて注入する。`test-integration-build` と `test-ui-integration` は `ui-image` に依存する。quay.io/town/ui のタグは本番／リリースの push のためだけのものである。`integration/systemcontroller_ui_test.go` の `uiTestImage` は、`UI_IMAGE` が設定されていないとき quay のタグへフォールバックするのではなく、そのテストを飛ばす。

## Proton のランナーのイメージ

Proton のランナーのイメージ（`quay.io/town/proton`）は `Containerfile.proton` からビルドされる。二段階のビルドを使う。ダウンローダの段が GE-Proton のリリースの tarball を取得し（`GE_PROTON_VERSION` のビルド引数で固定される）、実行時の段が Wine/Proton の依存関係（64 ビット + 32 ビット）、ヘッドレスでの動作のための Xvfb、そしてアプリケーションを実行する前に仮想フレームバッファを起動して Proton の環境を構成する `/usr/local/bin/proton` のラッパースクリプトをインストールする。

make のパイプラインが提供するもの: `release-proton-image`（ビルド）、`push-proton-rc`（アーキテクチャ別のリリース候補のタグ `rc.<date>-<arch>` + `rc.latest-<arch>` を push）、`push-proton-release`（アーキテクチャ別のリリースのタグ `release.<date>-<arch>` + `latest-<arch>` を push）である。proton のイメージは、`PROTON_ENABLED=1` のとき、完全な `push-rc` / `push-release` の流れと `manifest-rc` / `manifest-release` の組み立てにも含まれる。

## ウェブ UI の API のクライアント

ブラウザは実行時に `window.location` から API のベースの URL を決める。現在のプロトコルとホスト名にポート 5309 を組み合わせたものである（例: `https://myhost:5309`）。サーバー側のプロキシは介在しない。ブラウザはシステムコントローラの API と直接話す。

`VITE_API_URL` の環境変数は、設定されているときブラウザが導いた URL を上書きする。API のサーバーが別のホストやポートで動く開発中に便利である。

監視のダッシュボードは、監視のポートの URL（ポート 5308）を現在のホスト名から導く。`VITE_API_URL` が設定されているときはそこからホスト名を取り出し、そうでなければ `window.location.hostname` を使う。

## ウェブ UI のアクセシビリティ

すべてのダイアログのコンポーネントは、そのダイアログの目的を簡潔に説明する `DialogDescription` の要素を含む。これはスクリーンリーダーのための Radix UI のアクセシビリティの要件を満たし、`aria-describedby` の警告を無くす。説明はダイアログのヘッダーの中、タイトルの後に置かれ、すべてのユーザーに見える。
## 国際化

ユーザーに見えるすべての文字列（UI のラベル、エラーのメッセージ、toast の通知、監査のログの操作の説明）は、メッセージカタログのパターンを通じて翻訳可能である。

### バックエンド

`i18n` のパッケージは、翻訳の鍵を解決する `T(locale, key, args...)` の関数を提供する。フォールバックの連鎖は、要求されたロケール、次に `en-US`、最後に生の鍵の文字列である。`args` が渡されたときは `fmt.Sprintf` の整形が適用される。メッセージの鍵はドットで区切られた名前空間を使う（`auth.login_failed`、`pages.toast_provisioned` など）。

### 填充されたカタログ

バックエンドのカタログは `src/i18n` にロケールごとに一ファイルある（`de_de.go`、`zh_cn.go` など）。フロントエンドの鏡は `ui/src/i18n` にある（`de-DE.js`、`zh-CN.js` など）。この二つの側は歩調を合わせて保たれる —— 填充されたバックエンドのカタログにはすべて、フロントエンドの双子がいる。

`PopulatedLocales()` が正典の一覧である（48 項目）: `en-US`、`ar-AE`、`ar-EG`、`ar-SA`、`bn-BD`、`bn-IN`、`cs-CZ`、`da-DK`、`de-AT`、`de-CH`、`de-DE`、`en-AU`、`en-CA`、`en-GB`、`en-IN`、`en-NZ`、`en-ZA`、`es-AR`、`es-ES`、`es-MX`、`fi-FI`、`fr-BE`、`fr-CA`、`fr-CH`、`fr-FR`、`hi-IN`、`hr-HR`、`hu-HU`、`it-IT`、`ja-JP`、`ko-KR`、`nl-BE`、`nl-NL`、`pl-PL`、`pt-BR`、`pt-PT`、`ro-RO`、`ru-RU`、`sa-IN`、`sk-SK`、`sl-SI`、`sv-SE`、`th-TH`、`tr-TR`、`uk-UA`、`vi-VN`、`zh-CN`、`zh-TW`。ここに無いものはすべて英語へフォールバックする。`IsPopulated(code)` は、UI が言語の選択器で填充されていない項目を無効にするために使うものである。

この一覧は**書き出されるのではなくカタログのマップから派生する**。`buildPopulatedLocales()` が init の時点で `catalogs` の鍵を読み、並べ替え、`en-US` を先頭に留める。`IsPopulated` は `catalogs` を直接引く。かつては手で維持するスライスのリテラルであり、失敗の仕方はちょうど一つ、しかもそれは無言だった —— `catalogs` に登録されているのにリテラルで忘れられたカタログは、翻訳され、出荷され、そして選択器に一度も提供されなかった。`PopulatedLocales()` はクローンを返す。この一覧は今や呼び出しごとの新しいリテラルではなくパッケージの状態であり、結果を並べ替えたり切り詰めたりする呼び出し側が、次の呼び出し側の見るものをかき乱せてはならないからである。

### 国別の変種

カタログは二つの種類のどちらかであり、その違いはファイルがどう書かれるかにあって、どう選ばれるかにはない —— どちらの種類も填充されており、どちらも選択器に現れる。

**言語のカタログ**は翻訳であり、完全に書き出される: `de_de.go`、`cs_cz.go`、`ja_jp.go` である。

**国のカタログ**は `derive(base, overrides)`（`src/i18n/derive.go`。`ui/src/i18n/derive.js` が鏡写しにしている）が、それが属する言語のカタログに、その国が別の言い方をする文字列だけを加えて組み立てる。オーストリアのドイツ語はドイツ語である。`de_at.go` が答える問いは「これをドイツ語でどう言うか」ではなく「これらの文のうち、オーストリアの人なら書かなかったのはどれか」である。`de-DE` を `de_at.go` へ複写して四行編集するというやり方だと、次に `de-DE` へ追加されるメッセージの鍵が黙って英語のままオーストリアへ届き、ドイツ語の文字列の修正は三つのファイルで見つけて繰り返さなければならなくなる。基底を継承して差異だけを並べれば、変種は既定で正しいままである。新しい鍵は、その基底の言語がそれを持った瞬間にあらゆる場所へ届く。

十八のロケールがこのやり方で派生している:

| 基底 | そこから派生するもの |
| --- | --- |
| `en-US` | `en-CA`、`en-GB` |
| `en-GB` | `en-AU`、`en-IN`、`en-NZ`、`en-ZA` |
| `de-DE` | `de-AT`、`de-CH` |
| `fr-FR` | `fr-BE`、`fr-CA`、`fr-CH` |
| `es-ES` → `es-latam` | `es-AR`、`es-MX` |
| `pt-BR` | `pt-PT` |
| `nl-NL` | `nl-BE` |
| `ar-SA` | `ar-AE`、`ar-EG` |
| `bn-BD` | `bn-IN` |

`es-latam`（`src/i18n/es_latam.go`、`ui/src/i18n/es-latam.js`）は唯一の中間層である。半島のスペイン語に対して、すべてのアメリカ大陸の変種が共有する差異 —— `no válido` ではなく `inválido`、`añadir` ではなく `agregar`、`« »` ではなく直の引用符 —— を持ち、`es-AR` と `es-MX` の両方がその上に建つ。**これは `catalogs` に登録されておらず、選択できない**。共有の断片であって誰かが暮らす場所ではないからであり、それを公示すれば、実在しない国コードを提供することになる。

上書きのマップには小さいものもあり、いくつか（バックエンドの `en-CA`、`de-CH`、そして `es-MX`）は空である。技術的なコントロールパネルにとって、それが正直な答えである —— カナダの英語はアメリカ式の `-ize` の綴りを保つし、`de_de.go` のどのメッセージにも、スイスの `ss` の規則が届く `ß` は含まれていない（フロントエンドの `de-CH.js` は本物の上書きを持つ。`de-DE.js` が `ß` を使うからである）。空の上書きのマップも、そのロケールが忘れられたのではなく意図して検討されたことを示している。

この仕組みは両側のテスト（`src/i18n/derive_test.go`、`ui/src/i18n/derive.test.js`）が保っている。すべての上書きの鍵はその基底に存在しなければならず、すべての上書きは、それが置き換える基底の文字列と実際に異なっていなければならず、すべての派生カタログはその基底の鍵の集合を丸ごと持たなければならず、そしてすべての派生カタログはテストの `variants()` の表に載っていなければならない —— したがって国のカタログは、これらの規則が適用されないまま出荷されることはありえない。

**すべてのロケールのコードは地域の副タグを持ち**、`TestLocaleCodesAreRegionQualified` がそれを保つ。シュメール語（`sux`）が唯一の例外 —— 裸の ISO 639-3 のコード —— であり、それはもう無い。取り除かれたのはその形ではなくその文字体系のためである。楔形文字は `U+12000`〜`U+1254F` にあり、そのフォントを同梱しているものはほとんど無いので、Noto Sans Cuneiform の無い機械では、そのロケールのすべての文字列が代替の四角として描かれた。カタログが括弧の中に持っていたローマ字転写は生き残ったので、事態は空白よりも悪くなった —— 穴の周りにラテン文字の断片と句読点が散らばるのである。それを正直に描くには、webfont を同梱し（そのカタログは 45 個の異なるコードポイントを使っていたが、書体一式は 462K あり、部分集合化にはビルドのホストに `fonttools` が要る）、UI がまったく持っていない `@font-face` の仕組みを加えることになる。話者のいない言語のためには、あまりに大がかりである。

### ロケールの一覧

BCP 47 のロケールのコードが全体で使われる。精選された一覧が二つ提供される:

- **CommonLanguages**（21 項目）-- アラビア語（ar-SA）、ベンガル語（bn-BD）、ドイツ語（de-DE）、英語（en-US）、スペイン語（es-ES）、フランス語（fr-FR）、ヒンディー語（hi-IN）、イタリア語（it-IT）、日本語（ja-JP）、韓国語（ko-KR）、オランダ語（nl-NL）、ポーランド語（pl-PL）、ポルトガル語（pt-BR）、ロシア語（ru-RU）、サンスクリット語（sa-IN）、スウェーデン語（sv-SE）、タイ語（th-TH）、トルコ語（tr-TR）、ウクライナ語（uk-UA）、ベトナム語（vi-VN）、中国語（zh-CN）。各項目は母語の文字での名前と英語での名前を含む。
- **ExtendedLocales**（89 項目）-- 国別のロケールの変種の網羅的な一覧（de-AT、en-GB、es-MX、fr-CA、pt-PT、zh-TW など）。

### フロントエンド

React の文脈のプロバイダ（`I18nProvider`）がアプリケーションを包み、`{ locale, setLocale, syncServerLocale, t }` を返す `useI18n()` のフックを公開する。`t` の関数は、バックエンドと同じフォールバックの連鎖で、フロントエンドのカタログに対して鍵を解決する。パラメータの差し込みは `{name}` のプレースホルダを使う（例: `t('greeting', { name: 'Alice' })`）。

それと並んで `translateIn(locale, key, params)` が公開されており、これは現在有効なロケールではなく名指しされたロケールで翻訳する。フォールバックの連鎖は同じである。これが存在するのは、言語の変更を確認するメッセージのためである。`t` は、それが呼ばれた描画のロケールを閉じ込めるので、言語のフォームから発せられる確認は*去ろうとしている*言語で書かれてしまう —— そのページで唯一、その言語がもう使われていないことを主題とするメッセージが、である。

### ロケールの検出、保存、同期

UI は**まずブラウザから**言語を選ぶのであって、全体の設定からではない。読み込み時に `navigator.languages` を読み、順序の付いた好みを出荷されているカタログと照合する。照合は大文字小文字を区別せず、すべての好みについてまず正確なタグを試し、そのあとで以下の順にフォールバックする:

1. **正確な一致。** `de-CH` は今やカタログを出荷しているので、`de-CH` は `de-DE` へ畳まれるのではなく `de-CH` に解決される。
2. **中国語を文字体系／地域で判別する。** `zh-Hant`、あるいは `TW`/`HK`/`MO` の地域 → `zh-TW`、そうでなければ `zh-CN`。文字体系はどんな既定値よりも強い合図なので、これは下の二つの規則より先に走る。
3. **名前の付いた地域の既定値。** カタログを出荷していないが、その言語の既定値ではなくある変種を読む国である。スペイン語圏のラテンアメリカ → `es-MX`、ポルトガル語圏のアフリカと東ティモール → `pt-PT`、アイルランド・アフリカ・南アジアと東南アジアの英語 → `en-GB`。これが無いと `es-CO` は半島のスペイン語を、`en-IE` はアメリカ英語を得てしまう。
4. **名前の付いた言語の既定値。** `ar` → `ar-SA`、`bn` → `bn-BD`、`de` → `de-DE`、`en` → `en-US`、`es` → `es-ES`、`fr` → `fr-FR`、`nl` → `nl-NL`、`pt` → `pt-BR`。
5. **主副タグを共有する任意のカタログ。**

手順 3 と 4 が存在するのは、かつてフォールバックが手順 5 だけであり、それが正しいのは各言語がちょうど一つのカタログを持つ間だけだったからである。今や八つの言語が複数を出荷している。素の `en` を、あるいは `en-PH` を求めるブラウザは、そうでなければ `catalogs` のオブジェクトで最初に宣言された英語に着地することになり、答えは誰かが下した決定ではなく import の順序の性質になってしまう。

優先順位は高いものから:

1. 明示的な選択。**ブラウザごとに** `localStorage` へ永続化される —— *固定される*
2. ブラウザが検出し、出荷されているカタログに一致した言語 —— *固定される*
3. サーバーの全体の `locale` の設定。あとで `syncServerLocale` を通じて適用される —— *固定されない*

いったんロケールが固定されると `syncServerLocale` は何もしない。それがこの分割の要点である。かつて 60 秒ごとの状態の ping が `setLocale` を呼んでいたので、ポーリングのたびに管理者の全体の `locale` の設定がすべてのブラウザへ押し付けられていた。`locale` の設定（システム全体。既定は `en-US`。今も ping の応答で報告される）は今や、Town OS がカタログを出荷していない言語のためのフォールバックにすぎない。

### ロケール API

- `GET /locales`（認証が必要）-- 現在のロケール、填充されたロケールの一覧、よく使われる言語、拡張ロケールを返す。監査のログから除外される。

### 設定の UI

システム設定のページは言語の選択器を含む。よく使われる言語は母語の文字の名前でドロップダウンに表示される。展開できる区画が拡張ロケールの一覧を現す。填充されていないロケール（翻訳のカタログを持たないもの）は末尾にアスタリスクを付けて表示され、選択器では無効になっていて選べない。

この選択器は**ページが実際に描かれているロケール** —— `useI18n()` が保持しているもの —— で開くのであって、`GET /locales` の `current` の値では開かない。この二つは通常の場合に食い違う。ブラウザがロケールを選んで固定する一方、全体の `locale` の設定は既定の `en-US` に留まっているからである（[ロケールの検出、保存、同期](#ロケールの検出保存同期)を参照）。`current` を先に選んでおくと、英語でないページの上でこの操作部が「English」と読めてしまっていた。現在のロケールが国の変種のとき、それは折り畳まれた拡張の一覧の中にあるので、選択器を、見える選択肢のどれも持たない値のまま残すのではなく、読み込み時にその一覧を展開する。再び折り畳んでもその項目は描かれ続ける。同じ理由である。`current` はフォールバックとしてのみ、つまりサーバーが提供していない現在のロケールのために使われる。

保存はその選択を**両方**と比較する。片方だけに一致していても、まだするべき仕事がある。サーバーと同じでページと違うなら、設定を書かずにページを切り替える（`setLocale`。このブラウザについてその選択を固定する）ことを意味する。ページと同じでサーバーと違うなら、設定を書くことを意味する。両方に一致したときにだけ、するべきことが無い。成功の通知は、その背後の UI が既に切り替わっているので、選ばれたばかりの言語で `translateIn` を使って書かれる。「するべきことが無い」の通知は、何も変わっていないので画面上の言語のままである。`current` だけと比較していたために、表示されている言語が選べなくなっていた —— それに対して保存を押すと「するべきことが無い」と報告されるので、英語へ戻るにはまず三つ目の言語を保存する必要があった。
## システムコントローラの構成

### 起動の順序

正典の手順ごとの起動の順序は[システムコントローラの起動シーケンス](#システムコントローラの起動シーケンス)にある。要約すると:

1. `setupPodmanEnv()` が `CONTAINER_HOST` をホストの podman のソケットへ向ける。
2. フラグの解析、そして `:5309` が起動ステータスのスタブとともに直ちにバインドされる。
3. ディレクトリの作成、btrfs のルートに残った古い DB の片付け、データベース、そしてアカウント（と旧来のサービスアカウントの除去）、セッション、監査、設定、pages、ネットワークの各マネージャ —— 最後のものが home ネットワークを投入する。
4. リポジトリの投入、リポジトリのルートの強制更新。
5. インストールのマネージャ、btrfs のストレージ、systemd のマネージャ。イメージのタグの解決。
6. Rolodex の設定の書き込みと準備完了の待機（rolodex 自体は systemd が監督する）。
7. コアのイメージの pull（NC、監視、UI）と監視のシステムサービスの起動。
8. ローカルの TLS の CA、ingress、pages のサービス。
9. オブジェクトストレージの reconcile（ネットワークごとに一つの gfeh の区画）。
10. バージョンの変更の検出、reconcile、更新後のコマンド。
11. DNS の再構築、ネットワークの reconcile、二度目の（冪等な）オブジェクトストレージの reconcile、ingress のプログラム、UI のコンテナの起動。
12. 鮮度の段階（更新のあとのパッケージごとの再起動）。
13. ハンドラの構築と、起動のスタブから完全なルーターへの原子的な差し替え。
14. 区画が応答したのちの、背景でのオブジェクトストレージの名前の公開。

監視、Rolodex の設定、コアのイメージの pull、TLS の CA、ingress、pages のサービス、オブジェクトストレージ、ネットワークの reconcile、UI のコンテナについての起動の失敗は非致命的であり、システムはそれら無しで続行する。すべてのコンテナイメージの pull は、以下に述べる `ensureImage` の補助関数（`systemcontroller.EnsureImage`）を使う。必須でないサービスの pull の失敗は stderr にログが出るだけで起動を妨げないので、ネットワークが一時的に使えないときでもシステムは起動できる。

**起動は流動的なイメージのタグを取り直す** —— `EnsureImage` は、参照をどう扱うかを決める前に `FloatingImageRef` でそれを分類する。

- **流動的** —— どの綴りであれ `latest` の一族。`latest`、`rc.latest`、`release.latest` の、`-x86_64` / `-aarch64` の接尾辞が付くものと付かないもの、そしてタグがまったく無い参照（podman はこれを `:latest` として解決する）。これらはイメージが既にローカルの保管庫にあっても**毎回**の起動で pull し直す。動くタグにとって「有る」ことと「最新である」ことは別の問いであり、変わっていないタグへの `podman pull` はダウンロードではなくマニフェストの確認で済む。
- **固定** —— 日付入りのタグ（`rc.<date>-<arch>`、`release.<date>-<arch>`）、digest による参照、インスタンスごとのテスト用のタグ、そして `localhost/` 以下のすべて（取り直す先のレジストリが無い）。無いときだけ取得する。

ローカルに*ある*イメージの取り直しに失敗しても、それは誤りではない。ローカルの複製をそのまま使い、失敗はログに残す。そうすることでレジストリが無くても機械は起動できる —— 自分自身のユニットが `--pull=always` ではなく `--pull=missing` で動く理由と同じである。

**コントローラは自分自身を更新する** —— `SelfUpdate` は `boot_services` の最初の一歩として走り、同じ問題のもう半分である。`EnsureImage` は機械が動かすすべてのサービスを最新に保つが、それを実行している当のプロセスにはどうにもできない。コントローラ自身のユニットは意図して `--pull=missing` である（クラッシュ後の再起動がレジストリの生死に依存してはならない）。そのため運用者が *refresh system services* を押すまで、動いているコントローラはその機械が導入された日のイメージのままだった —— 更新する当のものだけが更新されない機械は、自分自身への修正だけは決して届けられない唯一の部品である。

この一歩は次を行う。

1. *この*コンテナがどのイメージの参照から起動したかを podman に尋ねる（`{{.ImageName}}`）。`TOWN_OS_TAG` から導出はしない。導入時のビルドは与えられた `CONTROLLER_IMAGE` —— まったく別のレジストリかもしれない —— をそのままユニットへ差し込むので、ユニットが実際には動かしていない参照を取り直せば、無関係な二つのイメージを比べ、起動のたびに機械を再起動することになる。
2. その参照が固定されているとき（運用者が意図して固定した、あるいは `make dev` の `localhost/` のイメージ）は何もしない。
3. それに `EnsureImage` を行い、タグが今指しているイメージの id と、このプロセスが動かしている id を比べる。
4. 食い違ったときは、目標の id を `<btrfsPath>/town-os-self-update` へ書き、systemd に `town-os-systemcontroller.service` の再起動を求める。ユニットの `--pull=missing` は、取り直しがローカルへ置いたばかりのイメージを起動する。この起動は、捨てられることが決まっている作業を持って reconcile へ進むのではなく、そこで待つ。

その印が輪を止める仕掛けである。再起動して戻ってきてもなお同じ古いイメージで動いているとき —— ユニットのタグの解決がこちらと違う場合など —— 機械は一度だけ試し、あとはログを残して先へ進む。起動のたびに永遠に再起動したりはしない。タグがまた動けばそれは新しい目標であり、自分の分の一度を得る。systemd に拒まれた再起動は印を消す。一度も起きなかった再起動が次を抑え込んではならないからである。

既存の機械への帰結：使うにはまずこのコードを含んだコントローラが動いている必要があるので、渡るためにはやはり手動の *refresh system services*（または入れ直し）がちょうど一度だけ要る。その後は再起動で足りる。

`TOWN_OS_IMAGE_REFRESH=0`（`false`／`no`／`off` も同じ）は取り直しを止め —— 自己更新も含む。何も動いていないという結論になる —— 固定されたタグの規則がすべてに適用される。無いイメージは依然として取得する。`make dev` と二つのテスト用のコンテナはこれを設定する。そこで動かすイメージはどれも意図して local の image cache から読み込んでいるので、取り直しは起動のたびに quay.io へ行き、わざわざ置いたイメージを置き換えてしまうからである。認識できない値は取り直しを有効なままにする —— 機械が黙って自分のサービスを取得しなくなることの方が、余分な pull 一回より悪い。コンテナに設定するのは半分にすぎない。それらのコンテナの PID 1 は systemd なので、テスト用ユニットの `PassEnvironment` の許可リスト（`integration/testdata/town-os-systemcontroller.service`）にもその変数を書かなければならない。書かなければ PID 1 で止まり、そのために設定した唯一のプロセスであるシステムコントローラは決して見ない。実際に書かれておらず、テストと dev の起動のたびに取り直しが走っていた —— rolodex と Prometheus と node-exporter が起動時の pull 集合の中で quay.io から取り直され、その間ハーネスは `/status/ping` に 120 秒しか与えない。実行が失敗するかどうかは回線次第だった。`TestHarnessEnvironmentReachesTheController` は必要な名前を `make/test.sh`、`make/dev.sh`、`system_port_env` から導き、統合テストは動いているコントローラの `/proc/<pid>/environ` を読んで実際に届いたことを証明する。

この区別が効くのは、インストーラがイメージを一つも同梱せず、どの機械のタグも既定では流動的（`rc.latest-<arch>`）だからである。これが無いと「既に読み込まれている」が「最新である」を意味してしまい、どのシステムサービスもその機械が最初に起動した日に取得したイメージのままになる —— 入れ直した VM は何度再起動しても数か月前の UI を出し続け、それを先へ進める道は運用者による `POST /system-services/refresh` だけだった。

### バージョンのタグの検出

システムコントローラは、`resolveImageTag()` が解決する単一のタグから、すべての兄弟のサービス（UI、Rolodex、ネットワークコントローラ、ingress）について対応するイメージのタグを導く。`TOWN_OS_TAG` の環境変数が設定されていればそれ、無ければ `rc.latest-<arch>`（`defaultVersionTag()`。アーキテクチャは `runtime.GOARCH` を `archTag()` で `x86_64`/`aarch64` に対応付けたもの）である。コンパイル時の `Version` の固定も `/town-os.tag` のファイルも無い —— どちらも取り除かれた。そのどちらかに古い値が残っていると、コントローラが先へ進んだ後でもすべての兄弟のイメージが黙って古いタグに留め置かれてしまったからである。install のビルドシステムは、systemcontroller の systemd のユニットに `TOWN_OS_TAG` を設定して特定のタグを固定する（`../install/make/install.sh` がそれを `CONTROLLER_IMAGE` から導く）。上書きが無ければ、機械の群れは常に `rc.latest-<arch>` を追う。このタグが `quay.io/town/ui:<tag>` や `quay.io/town/rolodex:<tag>` のようなイメージの参照を組み立てる。push されるタグはアーキテクチャ別なので、導かれる兄弟のタグもすべてアーキテクチャの接尾辞を運ぶ。

### エラーの形式

すべての API のエラーは RFC 9457 の Problem Detail のオブジェクト（type、title、status、detail のフィールドを持つ構造化された JSON）として返される。独自の `ProblemDetailHTTPErrorHandler` が Echo のエラーのハンドラとして設定されている。

### 要求のログ

Echo の `RequestLogger()` のミドルウェアが全体で有効になっており、すべての HTTP の要求を stderr にログ出力する。詳しさは `LOG_LEVEL` の環境変数が制御する。

### ログインの絞り込み

`POST /account/authenticate` は公開であり、試行のたびに 64 MiB の argon2id のハッシュを一回費やす。それはパスワードのハッシュとしては正しい費用だが、認証されていない呼び出し側に際限なくそれを予約させてよいものではない。同時に数百回の試行は、RAM から動くことが設計の要点そのものである機械の上での数十ギガバイトの確保であり、その失敗は遅いログインではない —— OOM killer がコントローラを持っていくことである。

独立した二つの制限がある。異なる問いに答えるからである。`loginLimiter` は**送信元ごとの試行**を窓の中で抑える（5 分に 20 回）。これがオンラインのパスワードの推測を実行不能にする。送信元のアドレスごとに鍵付けされているので、一つの乱暴なクライアントが家庭全体を締め出すことはできない。`loginGate` は**すべての送信元にまたがる同時のハッシュ**を抑える（4。argon2 のメモリの尖頭を 4 分の 1 ギガバイト近くに抑える）。これは送信元ごとの制限だけではできないことである。どちらもメモリ上でプロセスごとである。これらが守るのはこのプロセスのメモリと CPU であり、永続化すれば失敗したログインがデータベースへの書き込みになってしまう。

どちらもハッシュの**前**に検査され、後ではない —— 守ろうとしている費用はハッシュそのものなので、ハッシュしてから拒否したのでは、拒否しようとした攻撃の代金を払ってしまう。門の枠は呼び出しの後ではなく、クロージャの中の `defer` を通じて解放される。panic によって漏れた枠はプロセスの生涯にわたって失われ、それが四つあればこの機械のすべてのログインが再起動まで動かなくなるからである。正しいと証明されたパスワードはその送信元の窓を消すので、一つの NAT のアドレスの後ろにいる家庭が、普通に使っているうちに締め出しに入ってしまうことはない。

### CORS

`DEBUG` のモードでは、すべてのオリジンが許される。そうでない場合、同じホスト名からのポートをまたぐ要求が許される（例: ポート 80 のブラウザがポート 5309 の API と話す）が、**それは Host のヘッダが、この機械が正当に呼ばれうる名前かどうか検査されてからである**。許されるメソッド: GET、HEAD、POST、PUT、PATCH、DELETE、OPTIONS。資格情報は 3600 秒の最大の有効期間とともに許される。

この検査が効いてくるのは、古い規則 —— 「Origin のホスト名が Host のヘッダのホスト名と等しい」—— が、どちらも同じ攻撃者の選んだ URL から来る二つの値を比べていたからである。`box.evil.example` をこの機械の LAN のアドレスへ向ければ、ブラウザは `Origin: http://box.evil.example` と `Host: box.evil.example:5309` を送り、それらは一致する。それが DNS リバインディングの形であり、`AllowCredentials` と組み合わさって、通りすがりのページにブートストラップの窓（有効な管理者がいない間、`POST /account/create` は認証なしで答える）を手渡していた。

そこで `originAllowed` は、Host のヘッダがこの機械を名指ししていることを要求する。自分のホスト名、`<hostname>.local`、`<hostname>.<dns_tld>`、答えているループバックと LAN のアドレス、あるいはオペレーターが `AllowedHosts` に設定したものである。これらの形は**接尾辞での照合ではなく列挙されている** —— 「最初のラベルがホスト名である任意の名前」のような規則は `townos.evil.example` を受け入れてしまい、それは攻撃者が単に登録できる。IP のリテラルはそれ単独で受け入れられる。アドレスは DNS で別名にできないので、`http://192.168.1.10/` が `http://192.168.1.10:5309` へ届くのは構成上同じ機械であり、これが実際によく使われるやり方である。

**Private Network Access は、CORS が受け入れるオリジンに対してのみ答えられる。** `Access-Control-Allow-Private-Network` のヘッダは以前は無条件に返されていたが、それはインターネット上のすべてのオリジンに、私的なアドレスへ到達するブラウザの許可を手渡すものである —— PNA が CORS の上に加えるために存在する唯一の保護が、それである。そのミドルウェアは CORS のミドルウェアより**前**に登録されているので、プリフライトでも走る。プリフライトは CORS が自分で答えてしまい、連鎖の先を呼ばないからである。

### グレースフルシャットダウン

SIGINT が context のキャンセルを引き起こす。HTTP のサーバーが停止し、背景のすべての goroutine は context のチャネルを通じて終了する。Rolodex は systemd が監督しており、systemcontroller が停止させることはない。

### CLI のフラグ

- `-db <path>` -- SQLite のデータベースへのパス（既定は一時ファイル）。
- `-btrfs <path>` -- btrfs のサブボリュームの操作のベースのパス。
- `-repo-dir <path>` -- git のリポジトリのベースのディレクトリ（既定は一時ディレクトリ）。
- `-network-state <path>` -- パッケージごとのネットワークの状態のファイルのディレクトリ（既定は `/run/town-os`、`DefaultNetworkStatePath`。systemcontroller のコンテナとホストが共有するパスでなければならない —— 決して `/var/run/...` や `/tmp` であってはならない）。
- `-listen <addr>` -- HTTP の listen のアドレス（既定は `:5309`）。

ネットワークコントローラのイメージもフラグではない。解決されたイメージのタグから導かれ、`NC_IMAGE` で上書きできる。

### 環境変数

- `CONTAINER_HOST` -- ホストの podman のデーモンへの unix のソケットの URL。起動時に自動的に `unix:///run/podman/podman.sock` へ設定される（`HostPodmanSocket` を参照）。すべての `podman` の呼び出し —— systemcontroller が fork する子プロセスも含めて —— はこれをプロセスの環境から継承し、systemcontroller のコンテナの隔離された podman のストレージではなくホストのソケットを経由する。install リポジトリの systemd のユニットも、`systemctl` の出力で見えるように `Environment=CONTAINER_HOST=...` を設定すべきだが、実行時の真実の出所は `setupPodmanEnv()` の呼び出しである。
- `TOWN_OS_LISTEN` -- `-listen` のフラグを上書きする。
- `TOWN_OS_SIGNING_KEY` -- 一時的な JWT の署名鍵を上書きする（セッションの管理を参照）。
- `TOWN_OS_TLS` -- コントロールプレーン自身の listener（`:5309`）を HTTPS で提供する。この機械のローカルの CA が、パッケージのものとまったく同じように発行したリーフで終端する。**既定は切であり、それは保険ではなく順序の問題である**。この機械の CA を渡されていないブラウザは、信頼されていない証明書への XHR を完了できず、ナビゲーションと違って押して進める中間のページも無い —— UI は単に動かなくなり、その理由を説明する画面へ到達する手立ても無くなる。UI は今日、素の HTTP でも提供されている（ingress の既定の `:80` のバックエンドである）ので、先に CA をインストールせずにこれを入にした機械は「暗号化されていない」から「落ちている」へ移る。オペレーターは CA をインストールし（`GET /tls/ca.crt`。公開である）、そのうえでこれを設定する。`1`/`true`/`yes`/`on` を受け付ける。listener がバインドする**前**に解決されるので、HTTP として始まった起動ステータスのストリームが、そのクライアントの下で HTTPS になることは決してない。そして失敗時は平文へフォールバックするのではなく**致命的**である。TLS を求めたのに黙って平文を与えられたオペレーターは、機械が起動を拒んで理由を言う場合よりも不利だからである。
- `TOWN_OS_TLS_CERT` / `TOWN_OS_TLS_KEY` -- オペレーターが用意した証明書と鍵。既に公に信頼された証明書を持つ名前を前に置いた機械のためのものである。**両方**を設定するとそれだけで TLS が有効になり、ローカルの CA は参照されない。片方だけを設定しても何も起きない。
- `TOWN_OS_TLS_SANS` -- 生成されるリーフのための、カンマ区切りの追加の名前や IP。コントローラが導けない名前（CNAME、ルーターが割り当てた DHCP の名前）で到達される機械のためのものである。
- `TOWN_OS_TEST` -- 設定されていれば、本番の既定値ではなくテスト用のリポジトリを使う。
- `DEBUG` -- 設定されていれば、すべての CORS のオリジンを許し、テスト用のリポジトリを既定の前に足す。
- `LOG_LEVEL` -- ログの水準: `debug`、`info`、`warn`、`error`（既定は `error`）。
- `TOWN_OS_REPO_USERNAME` / `TOWN_OS_REPO_PASSWORD` -- 最初の初期化ですべてのリポジトリに適用されるリポジトリの資格情報。
- `TOWN_OS_TAG` -- すべての兄弟のイメージが導かれる元のイメージのタグを固定する（[バージョンのタグの検出](#バージョンのタグの検出)を参照）。install のビルドシステムが systemcontroller の systemd のユニットに設定する。
- `ROLODEX_IMAGE` -- Rolodex のコンテナイメージを上書きする（既定は `quay.io/town/rolodex:<tag>`）。
- `UI_IMAGE` -- UI のコンテナイメージを上書きする（既定は `quay.io/town/ui:<tag>`）。**空文字列**に設定すると（明示的に存在するが空）、UI のコンテナを丸ごと飛ばす —— bun が UI を提供する dev のモードである。
- `NC_IMAGE` -- ネットワークコントローラのイメージを上書きする（既定は `quay.io/town/networkcontroller:<tag>`）。統合テストのハーネスがローカルにビルドした NC を注入するのに使う。
- `INGRESS_IMAGE` -- ingress のイメージを上書きする（既定は `quay.io/town/ingress:<tag>`）。空文字列に設定すると ingress と pages のサービスを飛ばす —— dev のモードである。
- `GFEH_IMAGE` -- オブジェクトストレージのイメージを上書きする（既定は `quay.io/town/gfeh:<tag>`）。**空文字列**に設定するとオブジェクトストレージを丸ごと飛ばす —— dev のモードである。ingress が無効のときもオブジェクトストレージは飛ばされる。四つの HTTP のビューはそれを通してしか到達できないからである。
- `GFEH_SMB_PORT_BASE` -- SMB の listener が始まるホストのポートを上書きする（既定は `4450`）。痕跡である。[どの区画も SMB を提供しない](#smb-のビューは無い)ので、ホストのポートは確保されない。ハーネスの設定が無害なままであるよう配線だけ残してある。
- `TOWN_OS_WG_SALT` -- この機械の WireGuard のインターフェイス名、listen するポート、オーバーレイのサブネットを、ネットワークの名前空間を共有する別の Town OS から分けるインスタンスのソルト。実機では設定されない。テストと dev のハーネスが設定する。[インスタンスのソルト](#インスタンスのソルト)を参照。

#### システムサービスのホストポート

すべてのシステムサービスは `--net host` で動くので、これらはすべてコントローラのいるネットワークの名前空間 —— つまり*ホスト*の名前空間 —— にバインドする。統合テストのハーネスの中でも同様である（そのコンテナも意図的に `--net host` で動く。ブリッジの DNS が壊れるキャプティブなネットワークでもビルドが動き続けるようにするためである）。したがって `make test-full` の機械と `make dev` の機械はこれらのポートのすべてを奪い合い、`Restart=always` の下で互いを永遠にクラッシュループさせる。

これらはそれぞれその一つを移動させ、**既定は本番のポート**なので、環境が未設定なら今日の起動をそのまま再現する。`make/lib.sh` の `system_port_env` が実行ごとにそれらを `SYSTEM_PORT_FILES` へ確保し、テストのコンテナへ渡す —— 鉄の掟である。`make dev` はそれらを**一つも**意図的に設定しない。dev は実機を模すものであり、そこでは `redirect_host_dns` が rolodex を `:53` に必要とし、ブラウザは ingress を `:443` に必要とするからである。解析できない値は stderr に報告され既定へフォールバックする。そうしないと、打ち間違いが未設定とまったく同じに見えてしまうからである。

ポート一つで*止められる*唯一のターゲットが `make dev` なのも、同じ理由による。一台の機械に `127.0.0.2:53` は一つしかなく、それを欲しがるものは一つではない —— install リポジトリのホスト rolodex、別のチェックアウトの dev 箱、手荒に殺された dev 実行が残したシステムサービスのコンテナ。だから `require_free_rolodex_dns`（`make/dns.sh`）は、このチェックアウト自身のコンテナを取り除いてもなおその番地が握られているとき、何を止めればよいかを名指しして起動を拒む。以前のリダイレクトはその番地への素の TCP 接続を待っていたが、応じているのが他人のリゾルバであっても接続は同じように成功する —— このチェックアウトの rolodex は bind に失敗し、ホストはこの dev 箱を何も知らない rolodex へ向けられる。ブラウザから見れば、dev 箱自身の DNS が壊れているのと見分けがつかない。`wait_for_rolodex_dns` はいま両方の半分を要求する。ユニットが active であること（リゾルバを*我々のもの*にしているのはこちらだ）と、番地が bind されていること（使えるものにしているのはこちらで、ユニットは `podman run` が立ち上がった瞬間に active になる —— rolodex がソケットを開くより前である）。そして警告ではなく**致命的**である。リゾルバが上がらなかった dev 箱は dev 箱ではないし、そのまま進めば、起動したように見えるのにホスト上のあらゆる `.home` 名が LAN のリゾルバへ行ってしまう箱ができあがる。リダイレクトはコントローラが用意でき次第、アカウントと UI サーバより前に行われる。その行より後にあるものは、すでに立ち上がった dev 箱だからである。`TestDevDNSRefusesToStartWhenTheRolodexAddressIsTaken` と `TestDevDNSWait*` の各テストが覆っており、`ss`、`resolvectl`、`${SUDO}` をスタブにしている —— ホストのリゾルバに触れてよいテストは一つもない。

残るのは、dev 箱が bind の拠り所とする `rolodex.yml` を誰が書くのか、という問いである。誰も書いていなかった。それが上の致命的な待機の見つけた不具合である。rolodex は DNS の listener を起動時にそのファイルから**一度だけ**開き、Town OS が gRPC で何を言おうと listener は開かない —— `ProgramRolodex` が持つのは forwarder と解決モードと二つの遮断リストであって、bind は意図的に持たない（`controller_rolodex_config.go`）。実機ではそれを install のイメージが書く（`../install/scripts/rolodex-config.sh`、唯一の書き手である）。`make dev` はインストーラを走らせないので `${STATE_DIR}/dev-rolodex` は空であり、`town-os-system--rolodex.service` —— その `ExecStartPre` が作るのは*ディレクトリ*である —— は一度も存在したことのないファイルに向けて `--config /data/rolodex.yml` で rolodex を起動していた。それでもユニットは active になる。コンテナ自体は問題なく起動し、欠けているのは listener だけだからで、あの待機がユニットだけでなく番地をも問うのはまさにそのためである。いまは `seed_dev_rolodex_config`（`make/dns.sh`）が、コンテナがそのディレクトリをマウントする前にホスト側で書く。ループバックだけ —— DNS の bind、メトリクスの listener、コントローラがダイヤルする gRPC のソケット、出発点となる forwarder 一組 —— であり、実行のたびに書き直す。`dev-rolodex` は `rolodex.db` を保つために残り続けるので、古いスキーマの設定はそれを書いたチェックアウトより長生きしてしまうからである。イメージに焼き込まずホストで播くのは意図的だ。同じユニットファイルは統合テストのイメージにも載っており、`--net host` のテストコンテナの中で `127.0.0.2:53` を bind する `rolodex.yml` は、実行のたびに**ホストの** `:53` を bind してしまう。これが無いとき、起動はさらに無条件の三十秒の待機を二つ払っていた —— `Manager.WaitForDNSReady`、続いて `reconcileDNSAndNetworks` の gRPC ソケットのダイヤルのループ —— しかもそれは、完全に終わるまで `/status/ping` に 503 を返し続ける起動に対して `require_controller_ready` が与える 120 秒の予算の内側でのことである。

同じ起動が、`make dev` がそれを起動する前にコンテナへ何を入れておかねばならないかをも決めている。生成されるユニットはどれも `podman run` を `--pull=never` **なしで**走らせるので、内側のストアに無いイメージは即座に落ちるエラーではない —— それは `podman run` の内側で起きるレジストリからの取得であり、その間ずっと `require_controller_ready` の 120 秒と、そのサービス自身の readiness の待機とが同時に減っていく。ingress と、pages のサービスが使う caddy は、どちらもその一覧に無かった（caddy は統合の工装にも無かった）。だから dev 箱は起動の最中に quay と Docker Hub からそれらを取りに行っていた —— tar はとうにこのチェックアウトのイメージキャッシュに置かれていたのに。いまは両方とも読み込まれ、ingress は `ingress-image` の前提ターゲットがローカルで作る。逆に、読み込むべきでないものが二つ読み込まれていた。**Grafana** はおよそ 1.17 GB あり、しかも既定*ではない*ほうの監視バックエンドである。保存されたバックエンドが `grafana` でない限り起動はそれを走らせないので、`DEV_LOAD_GRAFANA=1` でない限り飛ばす。そして **gfeh** はもう `dev` の前提ではない。内容がリポジトリの外から来る唯一のローカルイメージであり、それゆえ暦日ごとに `gfehd` の依存の木を丸ごと再コンパイルする —— 使う使わないに関わらず、全員がその代価を払っていた。`make gfeh-image` が dev のオブジェクトストレージを有効にする。`make/dev.sh` がずっとそう言ってきたとおりである。読み込み自体も、`podman cp` ではなく `/image-cache` に読み取り専用でマウントしたイメージキャッシュを通るようになり、tar ごとに一度ぶんの書き込みが消えた。マウントの無いコンテナは従来どおりコピーに退避する。最後に、systemcontroller は dev のイメージで**もはや enable されない**。systemd は `podman run -d` が返った瞬間にそれを起動していた。相手は空のストアであり、それは失敗の確定した起動を買うことだった —— まるまる 30 秒の DNS の待機、まだ読み込まれていないものすべての取得、存在しないイメージに向けたユニットの起動、パッケージのリポジトリすべてのクローン —— そのどれもが、読み込みを終えた `make/dev.sh` の発する起動によって捨てられ、しかもその読み込みと資源を奪い合っていた。rolodex は enable のままである。コントローラが起動するより前に応じていなければならず、そのユニットはこの待機を耐え抜くように作られているからである。

- `TOWN_OS_DNS_PORT` -- rolodex が DNS を提供するポート（既定は `53`、`DNSLoopback` の上）。**これが既定でないとき、systemd-resolved のルーティングは丸ごと飛ばされる**。ドメインごとの DNS のサーバーのアドレスはポートを持たないので、resolved を `DNSLoopback` へ向けると、`.tld` のすべての問い合わせを通常のリゾルバの経路に委ねる代わりに、黙ってブラックホールに落としてしまう。
- `TOWN_OS_ROLODEX_METRICS_PORT` -- rolodex が Prometheus の `/metrics` のエンドポイントを提供するポート。これも `DNSLoopback` の上である（既定は `9153`）。DNS のポートとは別の listener なので、自前の上書きが要る。`rolodex.Manager.MetricsAddr()` が `rolodex.yml` と Prometheus の収集の対象の両方を組み立てる唯一の文字列なので、これを移動させれば両方が動く。
- `TOWN_OS_NODE_EXPORTER_PORT` -- node-exporter のループバックのメトリクスのポート（既定は `9100`）。
- `TOWN_OS_PROMETHEUS_PORT` -- Prometheus のループバックの HTTP の API のポート（既定は `9090`）。
- `TOWN_OS_MONITORING_PORT` -- LAN に面する唯一の監視のポート（既定は `5308`）。
- `INGRESS_HTTPS_PORT` / `INGRESS_HTTP_PORT` -- ingress が公開するポート（既定は `443` / `80`）。
- `INGRESS_METRICS_PORT` -- ingress が Prometheus の `/metrics` エンドポイントを提供するポート。公開は `127.0.0.1` のみ（既定は `9146`）。上の二つに加わる三つ目の listener であり、rolodex のそれと同じ理由で自分専用の上書きが要る。`ingressctl.MetricsAddrFor()` は unit の `-p` の指定と Prometheus の収集対象がともに作られる唯一の関数なので、ここを動かせば両端が動く。[ingress のメトリクス](#ingress-のメトリクス) を参照。

## 設定

| 鍵                      | 既定値                          | 説明                                     |
| ------------------------ | -------------------------------- | ----------------------------------------------- |
| `default_quota`          | `53687091200`                    | 既定のボリュームのクォータ（バイト。50 GB）           |
| `max_archive_size`       | `1073741824`                     | アップロードの最大サイズ（バイト。1 GB）             |
| `archive_unpack_timeout` | `600`                            | 展開のタイムアウト（秒。10 分）              |
| `locale`                 | `en-US`                          | BCP 47 のロケールのコード（システム全体のフォールバック）       |
| `dns_tld`                | `home`                           | パッケージの DNS のレコードの既定のトップレベルドメイン|
| `dns_resolution_mode`    | `auto`                           | Rolodex の上流の解決: `auto`、`recursive`、`forward` |
| `dns_local_forwarders`   | `false`                          | フォワーダーの一覧を、公開の既定値ではなく、この機械自身のネットワークが渡したリゾルバから取る |
| `peer_ttl`               | `7200`                           | WireGuard の peer の登録の寿命（秒。2 時間） |
| `gfeh_partition_quota`   | `0`                              | オブジェクトストレージの各区画のクォータ（バイト。0 は無制限） |
| `proton_image`           | `quay.io/town/proton:latest`     | Proton のランナーのイメージ —— **`proton` のビルドタグの下でのみ登録される** |

`DefaultSettings`（`src/account/settings.go`）は最初の初期化で投入され、既存の値が上書きされることは決してない。

いくつかの鍵は**読まれるが決して投入されない** —— 何かが書くまで行が存在せず、その既定値は空文字列に対するフォールバックとして読む側の箇所にある。他に何も変わらないだろうと思って `DefaultSettings` へ加えないこと。投入された行はオペレーターの選択と区別がつかず、ブロックリストの設定にとってそれは「一度も設定されていない、放っておけ」と「明示的に空に設定された、押し込め」の違いだからである（[ブロックリスト](#ブロックリスト)）。

| 鍵 | 無いときの既定値 | 書く主体 |
| --- | --- | --- |
| `monitoring_backend`     | `uplot` | `POST /settings/set` |
| `dns_dnsbl_config` | 未設定（空とは同じでない） | `POST /dns/dnsbl` |
| `dns_excluded_services`  | 空の一覧（公開はオプトアウト） | `POST /dns/services/set` |
| `dismissed_upgrades_hash` | 無し（何も却下されていない） | `POST /packages/upgrades/dismiss` |

**`object_storage_enabled` は無く、サービスアカウントのパスワードも無い。** オブジェクトストレージは入にする機能ではなく（[起動と reconcile](#起動と-reconcile)）、デーモンは Town OS の資格情報を持たない（[サービスアカウントは存在しない](#サービスアカウントは存在しない)）。アップグレードされた機械に残されたどちらの行も、何にも読まれない。

`proton_image` は基本のマップに入っていない。`src/account/settings_proton.go` は `//go:build proton` であり `init()` でその既定値を登録するので、このタグ無しのビルドには Proton の設定も、Proton のインストールの経路も無く、状態の ping では `proton_enabled: false` を報告する。公開された `Register` の関数ではなくビルドタグによる登録を使うのは、呼び出し側が `DefaultSettings` に対する呼び出し順序の依存を抱えないようにするためである。
